# 프로젝트 설계 전 입력 완전성 분석

## 1. 분석 목적과 범위

- 목적: 현재 `AGENTS.md`만으로 MVP 설계를 시작할 수 있는지 확인하고, 설계 전에 확정하거나 임시 결정으로 명시해야 할 누락 사항을 식별한다.
- 프로젝트 루트: `E:\Work\20260806\party_night`
- 확인 자료:
  - `AGENTS.md`: 현재 프로젝트 공통 개발 규칙 및 MVP 기준
  - `__AGENTS_original.md`: 현재 문서와의 차이 및 빠진 장기 요구사항을 확인하기 위한 비교 자료
- 최초 분석 당시에는 위 두 Markdown 문서만 있었으나, 현재는 `implements.md`에 따른 MVP 코드·설정·테스트가 구현되어 있다. 현재 구현 및 검증 상태는 제10절을 따른다.

## 2. 결론

현재 `AGENTS.md`는 **MVP의 상위 아키텍처와 구현 계획 수립을 시작하기에는 충분하다.** 목표, 포함·제외 범위, 기술 스택, 계층별 책임, 핵심 게임 규칙, 모바일 기준, 최소 구조, 필수 테스트 및 완료 기준이 정의되어 있다.

설계를 전면 중단해야 하는 필수 입력 누락은 없다. 문서가 미확정 세부사항에는 가장 단순한 기본값을 적용하고 `임시 결정`으로 기록하도록 허용하기 때문이다. 다만 실제 구현이 가능한 수준의 `implements.md`를 작성할 때는 아래 항목을 명시적으로 선택해야 한다. 사용자 입력이 없으면 “권장 임시 결정”을 적용할 수 있으나, 제품 의도와 직접 관련된 항목은 설계 승인 시 확인하는 것이 안전하다.

## 3. 현재 확정된 설계 기준

### 3.1 제품 및 범위

- 모바일 웹 브라우저에서 실행되는 4인 파티 기반 탐사·다이스 턴제 전투 게임이다.
- 첫 산출물은 준비부터 탐사, 조우, 전투, 결과, 준비 화면 복귀까지 한 사이클을 제공한다.
- 서버, 계정, 복수 기기 동기화, PWA 완성, 복잡한 성장·경제·AI·상태 효과는 MVP에서 제외한다.

### 3.2 기술 및 책임 경계

- Vite, React, TypeScript, Phaser, HTML/CSS, Vitest를 사용한다.
- React는 일반 UI와 명령 입력, Phaser는 탐사·전투 표현과 연출, 순수 TypeScript 엔진은 모든 게임 판정을 담당한다.
- 단일 원본 상태, 시드 기반 난수, 결과 선확정 후 연출 원칙을 지킨다.

### 3.3 핵심 규칙 및 검증

- 4인 파티, HP/ATK/DEF/AGI, AGI 내림차순 행동, d6 합산, 선택 리롤, 최소 피해 1, 전멸 판정 및 보상 기준이 있다.
- 논리 해상도는 `640 × 360`, 모바일 가로 화면, nearest-neighbor 렌더링을 사용한다.
- 결정론적 난수, 리롤, 행동 순서, 피해, 승패, 벽 충돌은 자동 테스트 대상이다.

## 4. 구현 설계 전에 선택해야 할 누락 사항

| 우선도 | 항목 | 현재 문서 상태 | 설계에 필요한 결정 | 권장 임시 결정 |
|---|---|---|---|---|
| 높음 | 동료 구성 방식 | 사전 정의 또는 단순 무작위 중 미선택 | 동료 3명의 생성 방식과 고정 능력치 | 재현성과 UI 단순화를 위해 사전 정의 3명 사용 |
| 높음 | 조우 방식 | 고정 또는 단순 확률 중 미선택 | 조우 발생 조건, 재조우 여부 | 고정 셀 1곳에서 세션당 1회 조우 |
| 높음 | 콘텐츠 수치 | 종족·직업 목록만 있고 능력치 및 스킬 수치 없음 | 4명 능력치, 적 능력치, 스킬별 다이스·보정·대상·리롤 | MVP에 실제 사용하는 4개 직업만 최소 수치표 작성, 나머지는 선택 가능하되 임시 공통값 적용 |
| 높음 | 리롤 제공 주체 | 스킬 또는 장비가 제공한다고만 정의 | 누가, 언제, 몇 회, 어느 다이스를 리롤하는지 | 메인 캐릭터의 직업 기본 스킬에 전투당 1회, 굴린 직후 다이스 1개 선택 리롤 |
| 높음 | 전투 입력 진행 | 캐릭터 단위 턴만 정의 | 행동 선택 시점, 적 턴 자동 처리, 리롤 생략, 유효 대상 규칙 | 현재 행동자 차례에 즉시 입력하고 적 턴은 자동 처리; 리롤은 사용/건너뛰기 선택 |
| 높음 | 맵 및 퀘스트 내용 | 소형 단일 층, 시작·조우·종료점만 요구 | 격자 크기, 벽 배치, 완료 조건, 전투 후 이동 흐름 | 작은 고정 맵, 고정 조우 승리 후 출구 도달 시 퀘스트 완료 |
| 중간 | 보상 종류 | 골드 또는 경험치로 표현된 곳과 둘 다 지급한다는 규칙이 혼재 | 골드·경험치 중 실제 지급 항목과 수치 | 승리 시 골드와 경험치를 모두 고정량 지급 |
| 중간 | 패배 후 흐름 | 준비 화면 복귀와 다시 시작은 있으나 정확한 상태 복구 범위가 불명확 | 보상 여부, 위치·전투 상태 초기화, HP 회복 시점 | 패배 보상 없음; 복귀 시 탐사·전투 상태 초기화 및 전원 완전 회복 |
| 중간 | 브라우저 저장 | 간단한 브라우저 저장 허용, 완료 기준에는 기본 저장이 포함됨 | 저장 대상·매체·시점·초기화 | `localStorage`에 메인 캐릭터 설정과 누적 골드·경험치만 결과/준비 복귀 시 저장 |
| 중간 | React–Phaser 연결 계약 | 명령/이벤트 방향만 정의 | 명령, 이벤트, 상태 스냅샷, Scene 생명주기 및 입력 중복 방지 | 엔진 store를 단일 원본으로 두고 Phaser는 상태·이벤트 구독, 모든 입력은 명령으로 환원 |
| 중간 | 모바일 검증 환경 | Android Chrome과 iOS Safari가 명시됨 | HTTPS 배포본에서 두 환경의 실제 동작 조건 확인 | Android 태블릿 Chrome과 iOS Safari 가로 화면을 v0.1.0 검증 근거로 사용 |
| 낮음 | 임시 시각 자산 | 사각형·단색·공개 에셋 허용 | 에셋 출처와 교체 경계 | 코드 생성 도형 또는 프로젝트 내부 임시 이미지 사용, 외부 라이선스 의존 회피 |
| 낮음 | 접근성·UI 수치 | 손가락으로 구분 가능한 크기만 요구 | 최소 터치 영역, 글자 크기, Safe Area 처리 | 최소 터치 영역 44 CSS px, `env(safe-area-inset-*)` 반영 |

## 5. 문서 내부의 모호성 및 충돌

### 5.1 현재 `AGENTS.md` 내부

1. **보상 표현**: 필수 기능에는 “골드 또는 경험치”, 규칙에는 “골드와 경험치” 지급으로 표현되어 있다. 설계에서는 둘 다 지급하는 더 구체적인 규칙을 우선 적용하는 것이 자연스럽다.
2. **입력 책임**: React가 전투 명령과 리롤 UI를 담당하고 Phaser도 게임 화면 내부 터치 입력을 담당한다. 어떤 입력을 어느 계층이 소유하는지 경계를 설계에 명시해야 중복 처리를 막을 수 있다.
3. **기본 저장 범위**: MVP 제외 목록에는 서버 저장만 제외되어 있고 구현 순서에는 “기본 저장”이 있으나, 저장 데이터·매체·시점이 없다.
4. **스킬 범위**: 캐릭터별 기본 공격과 추가 클래스 스킬 1개를 우선 구현한다고 되어 있으나, 6개 직업 모두의 스킬을 구현해야 하는지 실제 파티에 포함된 직업만 필요한지 명시되지 않았다.
5. **퀘스트 종료 흐름**: 맵 종료점은 요구하지만 조우 승리 직후 결과로 가는지, 출구 도달 후 결과로 가는지 정해져 있지 않다.

### 5.2 비교 자료와의 관계

- `__AGENTS_original.md`에는 장기 범위의 상점, 길드, 계약, 영구 사망, 서버 권위 판정, 자동 이동, 함정 등이 포함되어 있으나 현재 `AGENTS.md`에서는 다수가 MVP 제외 항목이다.
- 현재 파일명이 `AGENTS.md`이고 내용도 MVP 우선 원칙을 명시하므로, 본 분석에서는 현재 `AGENTS.md`를 기준 문서로 사용했다. `__AGENTS_original.md`의 장기 요구를 MVP에 다시 포함하지 않는다.
- 비교 자료에 기재된 플랫폼 최소 버전과 광범위한 상태·저장 규칙은 현재 기준에서 확정 요구사항으로 간주하지 않았다.

## 6. 추가 입력 권고안

설계 착수 전에 사용자에게 반드시 새 문서를 요구할 필요는 없다. 다만 아래 세 가지 제품 선택을 받으면 임시 결정의 양을 크게 줄일 수 있다.

1. 동료는 **고정 3명**인지 **시드 기반 무작위 생성**인지.
2. 첫 조우는 **고정 셀 1회**인지 **이동 시 확률 조우**인지.
3. 퀘스트 완료는 **전투 승리 즉시**인지 **승리 후 출구 도달**인지.

응답이 없더라도 각각 “고정 3명 / 고정 셀 1회 / 승리 후 출구 도달”을 `임시 결정`으로 기록하면 MVP 설계를 진행할 수 있다.

## 7. 위험 및 미확정 사항

- 현재 코드와 빌드 설정이 있으며 사용자가 PC에서 전체 테스트·production build·성공 플레이 루프를 확인했다.
- 6개 직업 전체의 서로 다른 스킬과 능력치를 첫 단계부터 만들면 MVP 범위가 커질 수 있다.
- React와 Phaser 양쪽에서 터치 입력을 받는 구조는 명령 중복과 상태 불일치 위험이 있으므로 구현 사양에서 소유권을 분명히 해야 한다.
- Android 태블릿 Chrome과 iOS Safari의 HTTPS 배포본 검증은 완료되었다. 브라우저 버전과 소형 Android 스마트폰 화면비는 확인되지 않았다.
- `__AGENTS_original.md`의 장기 설계를 향후 다시 사용할 계획이라면 MVP 이후 문서 우선순위와 재도입 시점을 별도로 정의해야 한다.

## 8. 설계 착수 판정

- 판정: **착수 가능**
- 차단 사항: 없음
- 적용 상태: 사용자가 미확정 항목을 임시 결정하여 설계를 진행하도록 지시했다. 상세 임시 결정과 실행 사양은 `implements.md`에 기록한다.

## 9. 설계 시 채택한 임시 결정 요약

다음 결정은 MVP 전용이며 사용자 확정 규칙이 아니다. 이후 변경 시 `implements.md`의 데이터·흐름·테스트를 함께 갱신해야 한다.

- 동료는 전사·사제·궁수로 고정하고, 메인 캐릭터만 사용자가 생성한다.
- 조우는 고정 셀에서 세션당 1회 발생하며 승리 후 같은 셀에서는 재발생하지 않는다.
- 고정 `7 × 7` 맵에서 조우 지점을 통과해 출구에 도달하면 승리 결과를 표시한다.
- 종족과 성별은 MVP 수치에 영향을 주지 않고, 능력치는 직업별 고정값을 사용한다.
- 모든 직업에 기본 공격과 직업 스킬 1개를 제공한다. 궁수의 직업 스킬이 선택 리롤을 보장한다.
- 행동 순서는 전투 시작 시 한 번 정하고, 동률은 시드 RNG로 결정한다. 한 라운드가 끝나도 순서를 다시 굴리지 않는다.
- 승리 보상은 골드 100, 경험치 50으로 고정한다. 패배 보상은 없다.
- 저장은 `localStorage`에 메인 캐릭터 설정과 누적 골드·경험치만 보관한다. 진행 중 탐사·전투는 저장하지 않는다.
- 전투 명령·리롤·메뉴는 React가, 탐사 방향 입력은 Phaser가 표시상 소유하되 모두 동일한 `GameCommand` 디스패치 경로를 사용한다.

## 10. 현재 구현·지원·검증 상태

### 10.1 정식 MVP 우선 지원 대상

- **지원 대상**: Android Chrome 및 iOS Safari 기반 모바일·태블릿 가로 화면.
- **보조 확인 환경**: PC 브라우저의 개발·테스트·production build 및 플레이 검증.
- **iOS 표시 조건**: Safari 가로 모드에서 사용자가 `도구 막대 가리기` 옵션을 활성화해야 기준 화면 전체를 확인할 수 있다.
- 로컬 HTTP 접속 문제와 달리 공인 인증서가 적용된 HTTPS `/pn/` 배포본에서 iOS Safari 동작이 확인되었다.

### 10.2 확인된 상태

| 항목 | 상태 | 근거 |
|---|---|---|
| 전체 자동 테스트 | 통과 | 사용자 실행 `npm run test` 완료 보고 |
| Production build | 통과 | Vite 6.4.3, 44 modules, 6.83초 성공 |
| PC 성공 플레이 루프 | 통과 | 설정 → 탐사 → 전투 → 출구 → 결산 확인 |
| Android 태블릿 성공 플레이 루프 | 통과 | 화면·입력·전투·리롤·복구·저장·초기 로딩 확인 |
| 퀘스트 성공 후 탐사 복귀 | 통과 | 사용자 수동 확인 |
| 승리한 고정 조우 재발생 방지 | 통과 | 조우 칸 전진·후진 반복 시 재조우 없음 확인 |
| 파티 전멸 규칙 | 자동 테스트 통과 | `combat.test.ts`에서 파티 전멸 판정 확인 |
| 파티 전멸 결과·복귀 | 자동 통합 테스트 통과 | 결정론적 패배 상태로 결과 화면, 무보상, 준비 복귀, 다음 세션 회복 확인 |
| 텍스트 롱터치 선택 방지 | 통과 | 전체 화면 CSS 수정 후 Android 실기기 수동 확인 |
| iOS Safari HTTPS 배포본 | 통과 | 가로 모드에서 `도구 막대 가리기` 활성화 후 전체 화면과 동작 확인 |

### 10.3 전멸 검증 판단

- 코드에는 파티 전멸 시 `lost` 판정과 패배 결과 화면 전환이 존재한다.
- 자동 테스트는 모든 파티원의 HP가 0일 때 패배로 판정되는 규칙을 확인한다.
- 현재 플레이에는 행동 건너뛰기·자해·테스트 난이도 선택이 없고 적보다 파티가 강해, 사용자가 의도적으로 전멸을 재현하기 어렵다. 이는 패배 규칙의 부재가 아니라 **수동 검증 가능성의 누락**이다.
- MVP 완료 판정에서는 전멸 핵심 규칙과 결과 화면 전환·무보상·준비 복귀·다음 세션 회복의 결정론적 자동 통합 테스트 통과를 인정한다.
- 수동 검증만을 위해 production 게임에 패배 버튼이나 치트 UI를 추가하지 않는다. 필요하면 테스트 코드에서 결정론적 패배 상태를 주입한다.

### 10.4 남은 위험 및 제외 범위

- 소형 Android 스마트폰의 좁은 가로 화면은 기기 미보유로 검증하지 않았으며, 사용자 지시에 따라 현재 완료 판정에서 제외한다. Android 태블릿 Chrome 검증을 모바일 근거로 사용한다.
- iOS Safari는 브라우저 도구 막대가 표시된 상태에서 가용 높이가 부족할 수 있으므로 사용자 안내가 필요하다.
- 500 kB 초과 번들 경고는 Android 초기 로딩 문제가 확인되지 않아 현 단계 최적화 대상에서 제외한다.

## 11. v0.1.0 Production 배포 기준선

### 11.1 배포 구성

- Vite base는 사용자가 `'/pn/'`으로 확정하고 `vite.config.ts`에 반영했다.
- Production URL은 공인 인증서가 적용된 HTTPS 도메인의 `/pn/` 하위 경로를 사용한다.
- Apache2 또는 Nginx는 `dist/index.html`과 `dist/assets/`를 `/pn/` 경로에 대응시켜 정적 서비스한다.
- Production 실행에는 Vite 개발 서버와 Node.js 애플리케이션 서버가 필요하지 않다.

### 11.2 해결된 배포 문제

- 초기 build는 기본 base `/`로 인해 `/assets/...`를 요청했고, `/pn/` 하위 배포 경로와 불일치하여 CSS·JS 404가 발생했다.
- `base: '/pn/'` 적용 후 에셋 요청 경로가 `/pn/assets/...`로 생성되어 하위 경로 배포와 일치한다.
- HTTPS 배포본은 PC 브라우저와 iOS Safari에서 로드 및 동작이 확인되었다.

### 11.3 버전 기준

- 현재 package 버전은 `0.1.0`이다.
- 현재 코드·설정·문서·검증 상태를 Git 태그 `v0.1.0`의 기준선으로 관리한다.
- 기준선에는 Android 태블릿 Chrome과 iOS Safari의 가로 화면 검증, 전체 자동 테스트 15개, production build, 성공 루프 및 패배 통합 테스트가 포함된다.
- iOS Safari는 `도구 막대 가리기`가 전체 화면 확인 조건이다.
- 소형 Android 스마트폰 검증과 번들 분할 최적화는 v0.1.0 완료 기준에서 제외한다.

## 12. 기본 능력치와 전투 파생 수치 설계

### 12.1 목표와 적용 상태

- 캐릭터 원본 능력치를 `STR`, `DEX`, `INT`, `CON`, `AGI`, `LUCK`으로 구성한다.
- 현재 직접 저장된 HP·ATK·DEF·전투 AGI를 기본 능력치와 직업 설정에서 계산되는 파생 수치로 전환한다.
- 본 설계는 사용자가 검토안을 문서에 반영하도록 승인한 임시 설계다. 코드에는 아직 적용되지 않았다.
- 첫 적용에서는 기존 v0.1.0의 직업별 HP·ATK·DEF·AGI 값을 정확히 재현해 플레이 밸런스를 바꾸지 않는다.

### 12.2 능력치 책임

| 항목 | 책임 | 현재 판정 사용 |
|---|---|---|
| STR | 근력 기반 직업의 공격, HP·DEF 보조 | 사용 예정 |
| DEX | 기민 기반 직업의 공격, HP·DEF 보조 | 사용 예정 |
| INT | 마법·신성 계열 직업의 공격 | 사용 예정 |
| CON | HP의 주 능력치, DEF의 주 능력치 | 사용 예정 |
| AGI | 전투 행동 순서용 AGI 산출 | 사용 예정 |
| LUCK | 향후 크리티컬 등 운 판정 | 현재 저장·표시만 하고 판정 미사용 |

- 현재 규칙에는 추가 기본 능력치가 필요하지 않다.
- `WIS`는 신성 능력을 INT에서 분리할 때, 명중·회피·크리티컬·마법 방어는 해당 규칙을 실제 도입할 때만 검토한다.
- `currentHp`, `level`, `experience`, 직업 파생 설정, 장비·상태 보정은 기본 능력치와 별도 데이터다.

### 12.3 원본과 파생 데이터 경계

```text
직업 기본 능력치 또는 성장된 캐릭터 능력치
{ str, dex, int, con, agi, luck }
              │
              ▼
직업 파생 설정
{ attackBasis, attackModifier, defenseModifier }
              │
              ▼
deriveCombatStats
{ maxHp, atk, def, battleAgi }
              │
              ▼
전투 런타임
{ currentHp, maxHp, atk, def, agi }
```

- 기본 능력치가 원본이며 파생 수치를 별도 콘텐츠 원본으로 중복 관리하지 않는다.
- 전투 런타임의 `agi`는 기본 능력치 `attributes.agi`와 구별되는 행동 순서용 파생값이다.
- `currentHp`는 파생 수치가 아니라 세션 중 변하는 런타임 상태다.

### 12.4 파생 공식

```text
maxHp = 11 + (CON × 2) + floor((STR + DEX) / 10)

atk = max(1, floor(attackBasis / 2) + classAttackModifier)

def = max(
  1,
  floor(((CON × 2) + STR + DEX) / 10) + classDefenseModifier
)

battleAgi = max(1, floor((AGI + 2) / 2))
```

- 모든 계산은 정수이며 중간 소수점은 공식에 명시된 위치에서 내림한다.
- LUCK은 파생 공식에 넣지 않는다.
- 장비·상태 보정의 적용 순서는 해당 시스템 설계 전까지 확정하지 않는다.

### 12.5 직업별 공격 기준과 보정

| 직업 | attackBasis | ATK 보정 | DEF 보정 |
|---|---|---:|---:|
| 전사 | STR | 0 | +1 |
| 도적 | DEX | 0 | 0 |
| 궁수 | DEX | 0 | 0 |
| 성기사 | max(STR, INT) | 0 | +3 |
| 사제 | INT | -2 | +1 |
| 마법사 | INT | +1 | 0 |

- 보정값은 현재 직업 밸런스를 재현하기 위한 임시 직업 숙련값이다.
- 성기사의 DEF 보정은 중장갑 방어 숙련, 전사·사제의 DEF 보정은 직업 방어 숙련으로 취급한다.
- 보정값은 기본 능력치에 합산하지 않고 파생 계산 단계에서만 적용한다.

### 12.6 직업별 임시 기본 능력치

| 직업 | STR | DEX | INT | CON | AGI | LUCK | 합계 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 전사 | 10 | 5 | 3 | 9 | 3 | 6 | 36 |
| 도적 | 4 | 9 | 4 | 5 | 10 | 4 | 36 |
| 궁수 | 5 | 10 | 4 | 6 | 8 | 3 | 36 |
| 성기사 | 8 | 4 | 7 | 8 | 3 | 6 | 36 |
| 사제 | 4 | 5 | 10 | 6 | 5 | 6 | 36 |
| 마법사 | 3 | 7 | 10 | 3 | 7 | 6 | 36 |

- 초기 직업 템플릿은 합계 36으로 맞춘다.
- 각 값의 초기 입력 범위는 1~10이며 성장 후 절대 상한을 의미하지 않는다.
- 종족과 성별은 현 단계에서 기본 능력치를 변경하지 않는다.

### 12.7 기존 수치 호환성

| 직업 | 계산 HP | 계산 ATK | 계산 DEF | 계산 AGI | v0.1.0 일치 여부 |
|---|---:|---:|---:|---:|---|
| 전사 | 30 | 5 | 4 | 2 | 일치 |
| 도적 | 22 | 4 | 2 | 6 | 일치 |
| 궁수 | 24 | 5 | 2 | 5 | 일치 |
| 성기사 | 28 | 4 | 5 | 2 | 일치 |
| 사제 | 23 | 3 | 3 | 3 | 일치 |
| 마법사 | 18 | 6 | 1 | 4 | 일치 |

### 12.8 제약과 위험

- 같은 INT 10을 가진 사제와 마법사의 공격력 차이를 보존하기 위해 직업 ATK 보정이 필요하다.
- 성기사의 높은 DEF를 보존하기 위해 기본 능력치 외에 +3 직업 DEF 보정이 필요하다.
- 기본 AGI와 전투 AGI를 UI·타입·로그에서 혼동하지 않아야 한다.
- LUCK을 표시하면서 효과가 없는 상태임을 UI 또는 설명에 명시해야 한다.
- 이 변경은 성장·장비·크리티컬을 함께 구현하는 승인이 아니다.

## 13. 종족 기본 능력치와 직업 보정값 전환 설계

### 13.1 적용 관계와 현재 상태

- 이 절은 12절 중 `직업 기본 능력치가 원본`, `종족은 수치에 영향 없음`, `LUCK 효과 없음 안내 표시`, `기존 v0.1.0 전투 수치 정확 재현` 조건을 대체한다.
- 현재 코드는 `RACE_DATA.baseAttributes`와 `CLASS_DATA.attributeModifiers`를 합산한다. 이번 조정은 해당 구조를 유지하면서 하플링·도적·마법사 수치와 성별 도메인을 변경한다.
- 종족이 6개 기본 능력치를 소유하고 직업은 같은 6개 항목의 정수 보정값을 소유한다.
- 사용자 승인에 따라 기존 인간 캐릭터의 최종 능력치와 전투 파생값을 정확히 재현하지 않아도 된다. 최신 조정에서는 모든 조합의 총량을 37로 통일한다.
- 사용자 화면과 신규 설계 문서의 표기는 `LUCK`이 아니라 `LUK`로 통일한다. 현재 판정에는 계속 사용하지 않지만 `(현재 효과 없음)` 안내 문구는 표시하지 않는다.
- TypeScript 내부 필드 `luck`은 UI 표시명이 아니며 현재 저장 호환성과 불필요한 전면 이름 변경을 피하기 위해 유지할 수 있다.

### 13.2 종족별 기본 능력치

| race_id | 종족 | STR | DEX | INT | CON | AGI | LUK | 합계 | 성향 근거 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---|
| `human` | 인간 | 5 | 5 | 5 | 5 | 6 | 5 | 31 | STR·DEX·INT·CON 균등, 나머지도 중립 |
| `elf` | 엘프 | 3 | 7 | 6 | 4 | 7 | 4 | 31 | 낮은 STR·CON, 높은 DEX, 조금 높은 INT·AGI |
| `dwarf` | 드워프 | 7 | 6 | 3 | 6 | 4 | 5 | 31 | 높은 STR, 약간 높은 DEX·CON, 낮은 INT·AGI |
| `halfling` | 하플링 | 2 | 5 | 5 | 3 | 8 | 8 | 31 | 낮은 STR·CON, 높은 AGI·LUK, 중립 DEX·INT |

- 네 종족 모두 합계를 31로 고정해 종족 선택 자체가 총 능력치 우위를 만들지 않게 한다.
- 인간의 STR·DEX·INT·CON은 모두 5로 동일하다.
- 종족 기본값은 1 이상의 정수이며 직업 보정 적용 전 원본 데이터다.

### 13.3 직업별 능력치 보정값

| class_id | 직업 | STR | DEX | INT | CON | AGI | LUK | 보정 합계 | 역할 근거 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---|
| `warrior` | 전사 | +4 | -1 | 0 | +3 | 0 | 0 | +6 | 높은 STR, 낮은 DEX, 중간 수준 CON 강화 |
| `paladin` | 성기사 | +2 | 0 | +2 | +2 | 0 | 0 | +6 | STR·INT·CON을 균등하게 중간 수준 강화 |
| `rogue` | 도적 | -1 | +4 | 0 | 0 | +2 | +1 | +6 | 낮은 STR, 높은 DEX, 약간 높은 AGI·LUK |
| `archer` | 궁수 | 0 | +3 | 0 | 0 | +3 | 0 | +6 | DEX·AGI를 함께 높게 강화 |
| `priest` | 사제 | 0 | +2 | +4 | 0 | 0 | 0 | +6 | 중간 수준 DEX, 높은 INT |
| `mage` | 마법사 | -1 | 0 | +5 | 0 | +1 | +1 | +6 | STR을 낮추고 INT를 매우 높게, AGI·LUK을 소폭 강화 |

- 보정값은 음수·0·양수 정수를 허용한다.
- 직업 보정은 종족 원본을 변경하지 않고 캐릭터 생성 시 합산한다.
- 도적과 마법사는 LUK +1 보정을 가지지만 LUK 관련 판정은 아직 추가하지 않는다.

### 13.4 최종 능력치와 파생 흐름

```text
종족 기본 능력치
{ str, dex, int, con, agi, luck }
          +
직업 능력치 보정
{ str, dex, int, con, agi, luck }
          │ 항목별 정수 합산
          ▼
최종 캐릭터 능력치
{ str, dex, int, con, agi, luck }
          │
          ├─ Actor.attributes
          ▼
기존 직업 파생 설정 + deriveCombatStats
          ▼
{ maxHp, atk, def, battleAgi }
```

- 최종 능력치는 `final[key] = raceBase[key] + classModifier[key]`로 계산한다.
- 확정된 24개 조합의 최종 합계는 모두 37이고 각 항목은 1~11이다. 기존 초기값의 1~10은 직업 단독 템플릿 기준이므로 최종 결합값의 절대 상한으로 사용하지 않는다.
- HP·ATK·DEF·전투 AGI 공식과 `attackBasis`, ATK 보정, DEF 보정은 12.4~12.5절의 현재 구조를 유지한다.
- 고정 동료 종족은 `raw_data_table.md` 16절의 최신 사용자 입력을 따른다. 브람은 `dwarf`, 세라는 `human`, 로웬은 `elf`이며 상세 적용·호환성은 20절을 따른다.

### 13.5 인간 기준 최종 능력치 확인

| 직업 | STR | DEX | INT | CON | AGI | LUK | 합계 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 전사 | 9 | 4 | 5 | 8 | 6 | 5 | 37 |
| 성기사 | 7 | 5 | 7 | 7 | 6 | 5 | 37 |
| 도적 | 4 | 9 | 5 | 5 | 8 | 6 | 37 |
| 궁수 | 5 | 8 | 5 | 5 | 9 | 5 | 37 |
| 사제 | 5 | 7 | 9 | 5 | 6 | 5 | 37 |
| 마법사 | 4 | 5 | 10 | 5 | 7 | 6 | 37 |

### 13.6 충돌·누락·위험

- `raw_data_table.md` 5~6절은 본 설계의 최신 하플링·도적·마법사 값과 함께 갱신한다.
- 종족 선택이 전투 수치에 실제 영향을 주므로 기존 클래스별 HP·ATK·DEF·전투 AGI 회귀 기대값은 더 이상 단일 값이 아니다.
- 기존 저장은 종족·직업 설정만 저장하고 능력치를 세션 생성 시 계산하므로 저장 버전 변경은 필요하지 않다. 같은 저장도 새 버전에서 계산 결과가 달라지는 것은 의도된 밸런스 변경이다.
- LUK은 화면에 설명 없이 표시되지만 판정에는 사용되지 않는다. 이는 사용자가 안내 문구 삭제를 명시한 결과이며 크리티컬 구현 승인을 의미하지 않는다.
- 성별은 계속 능력치에 영향을 주지 않는다.

### 13.7 성별 도메인 축소

- 사용자 선택 성별은 `남성`, `여성` 두 값만 허용하고 `기타`를 제외한다.
- 설정 UI, `MainCharacterConfig` 타입, 저장 검증과 한글 표시값→에셋 토큰 변환이 같은 두 값만 처리해야 한다.
- 권장 타입 경계는 UI·저장용 `Gender = '남성' | '여성'`, Actor·에셋용 `AssetGender = 'male' | 'female'`이다.
- 기존 save version 1에 `기타`가 저장되어 있으면 프로필의 이름·종족·직업·누적 골드·경험치를 보존하고 성별만 `남성`으로 정규화한다. 이는 저장 전체를 무효화하지 않기 위한 임시 호환 결정이다.
- `neutral` 캐릭터 PNG와 과거 에셋 문서·이력은 삭제하지 않는다. 런타임 선택과 신규 Actor 생성에서만 접근 불가능한 미사용 에셋으로 남긴다.

## 14. MVP 이후 7개 퀘스트·레벨 10 확장 사전 분석

### 14.1 분석 목적과 상태

- 이 절은 확장 구현 설계 전에 사용자가 제시한 목표와 현재 코드·문서의 차이, 추가 확정이 필요한 항목을 정리한다.
- 본 요청은 사전 확인 단계이므로 `implements.md`에 확장 구현 사양을 추가하지 않는다.
- 아래의 퀘스트 명칭·등장 적·최대 레벨·스킬 해금 레벨은 사용자 확정 요구사항이다. 조우 수량, EXP, 장비 목록과 세부 효과는 아직 확정되지 않았다.
- 현재 작업 트리에서 13절의 최신 성별·하플링·도적·마법사 조정은 문서에만 반영되어 있고 코드에는 아직 적용되지 않았다. 확장 설계의 코드 기준선을 먼저 맞춰야 한다.

### 14.2 확정된 확장 목표

#### 퀘스트 구성

| 구분 | 순서 | 퀘스트 | 일반 적 | 중간보스 | 보스 | 현재 확인 상태 |
|---|---:|---|---|---|---|---|
| 순차 진행 | 1 | 훈련 폐허 | 고블린, 총 3개 파티 조우 | 없음 | 없음 | 기존 단일 조우를 3개로 변경 |
| 순차 진행 | 2 | 고블린 소굴 | 고블린 | 없음 | 홉고블린 | 역할 명시됨 |
| 순차 진행 | 3 | 유적지 | 고블린, 오크 | 없음 | 오우거 | 역할 명시됨 |
| 순차 진행 | 4 | 지하 던전 | 고블린, 코볼트 | 놀 | 미노타우르스 | 역할 명시됨 |
| 순차 진행 | 5 | 옛 고성 | 스켈레톤, 좀비 | 구울 | 리치 | 역할 명시됨 |
| 반복 진행 | - | 화산 동굴 | 임프, 코볼트 | 오우거 | 사이클롭스 | 사이클롭스의 보스 역할은 문맥상 추정이므로 확인 필요 |
| 반복 진행 | - | 깊은 숲 폐허 | 스켈레톤, 오크 | 레이스 | 스켈레톤 킹 | 역할 명시됨 |

#### 성장·상점·스킬

- 캐릭터는 레벨 1에서 시작한다.
- 순차 퀘스트 5개를 차례로 완료한 시점의 목표 레벨은 6이다.
- 이후 반복 퀘스트로 성장하며 최대 레벨은 10이다.
- 장비 및 아이템 상점을 추가한다.
- 퀘스트 1·3·4·5 클리어를 장비 등급 해금에 사용한다.
- 직업별 고유 스킬은 기본 액티브 1개와 추가 액티브 또는 패시브 2개, 총 3개다. 공통 기본 공격은 이 수에 포함하지 않는 것으로 해석되나 확인이 필요하다.
- 레벨별 해금 목표는 다음과 같다.

| 레벨 | 해금 목표 |
|---:|---|
| 1 | 직업 기본 액티브 스킬 1개 |
| 2 | 두 번째 직업 액티브 또는 패시브 스킬 1개 |
| 3 | 커스텀 스킬 슬롯 1개 |
| 5 | 세 번째 직업 액티브 또는 패시브 스킬 1개 |
| 7 | 커스텀 스킬 슬롯 1개 추가 |
| 10 | 커스텀 스킬 슬롯 1개 추가, 최대 레벨 |

### 14.3 현재 구현과의 차이

| 영역 | 확인된 현재 코드 | 확장에 필요한 변화 |
|---|---|---|
| 퀘스트 | `START_QUEST`에 quest ID가 없고 훈련 폐허만 시작 | 퀘스트 데이터·선택·해금·완료 이력 |
| 맵·조우 | `MAP_ROWS` 1개, `ruins_goblins` 1회 고정 조우 | 퀘스트별 맵과 복수 조우, 중간보스·보스 연결 |
| 적 | 고블린 정찰병·경비병 2종만 구현 | 요청된 적 13종 내외의 데이터·스킬·역할 분류 |
| 성장 | `Profile.totalExperience` 누적값만 존재 | 캐릭터별 level·EXP·성장치와 최대 레벨 판정 |
| 파티 영속성 | 퀘스트마다 Actor를 재생성하고 결과 복귀 시 세션 제거 | 파티원별 성장·장비·스킬 원본과 전투 스냅샷 분리 |
| 스킬 | 기본 공격과 직업 공격 스킬 1개씩만 구현 | 액티브·패시브, 해금 레벨, 장착 슬롯, 대상·쿨다운·효과 확장 |
| 상점·장비 | 타입·상태·명령·화면 없음 | 아이템·장비·인벤토리·구매·장착·등급 해금 전체 추가 |
| 저장 | version 1에 설정·누적 골드·총 EXP만 저장 | 퀘스트·캐릭터·장비·스킬을 포함한 신규 버전과 마이그레이션 |
| UI | 시작·준비·탐사·전투·결과 5화면 | 퀘스트 선택, 상점, 장비, 스킬 준비, 레벨업·해금 결과 UI |

### 14.4 직업별 스킬 목록 확인

`raw_data_table.md`에 입력된 직업별 3개 스킬은 다음과 같다. `current` 또는 `input` 표시는 원시 문서의 상태이며 실제 구현 완료를 뜻하지 않는다.

| 직업 | 레벨 1 후보 | 레벨 2 후보 | 세 번째 후보 | 코드와의 충돌·확인 사항 |
|---|---|---|---|---|
| 전사 | `power_strike` 강타 | `taunt` 도발 | `ability_reinforcement` 능력 강화(Lv5) | 최신 레벨 배치 확인됨 |
| 도적 | `quick_stab` 빠른 찌르기 | `seek_trap` 함정간파 | `wound_break` 상처 절개(Lv5) | 함정·비밀문 콘텐츠 포함 여부는 별도 확인 필요 |
| 궁수 | `aimed_shot` 조준 사격 | `find_leak` 약점노출 | `head_shot` 헤드 샷(Lv5) | ID 수정 확인됨 |
| 성기사 | `holy_strike` 신성한 일격 | `protection_pledge` 보호 서약 | `sacred_rage` 성스러운 분노(Lv5) | 최신 레벨 배치 확인됨 |
| 사제 | `heal` 성스러운 치료 | `smite` 징벌 | `celestial_shroud` 천상의 수의(Lv5) | Lv1 치료·Lv2 징벌과 ID 정규화 확인됨 |
| 마법사 | `arcane_bolt` 비전 화살 | `lightning_bolt` 전격 화살 | `fire_ball` 화염구(Lv5) | ID 수정 확인됨 |

- 현재 코드에는 `power_strike`, `quick_stab`, `aimed_shot`, `holy_strike`, `smite`, `arcane_bolt`만 있으며 모두 단순 공격 또는 리롤 규칙이다.
- 원시 목록은 기절·출혈·도발·함정 탐지·피해 증가·전체 아군 패시브·치료·마비·상태 강화·전체 공격 등 현재 엔진에 없는 효과를 요구한다.
- 원시 문서의 직업 스킬 해금 레벨은 1·2·5로 수정되었고 사제 Lv1은 `heal`로 확정되었다.
- 커스텀 스킬 목록은 대부분 해금 레벨 5로 적혀 있어 첫 슬롯이 열리는 레벨 3과 맞지 않는다. 확인된 ID·대상·사용 제한 오기는 14.8절에서 정규화했다.

### 14.5 설계 전에 반드시 확인할 항목

이 절은 최초 사전 확인 목록이다. 사용자 후속 답변으로 해소된 항목과 남은 확인 사항은 14.7절이 우선한다.

#### A. 퀘스트·조우

1. 훈련 폐허의 고블린 3개 파티 각각의 적 종류·수량·좌표와 조우 순서를 확정해야 한다.
2. 나머지 6개 퀘스트도 일반전 횟수와 각 파티 구성, 중간보스·보스의 단독/부하 동반 여부를 정해야 한다.
3. 화산 동굴의 사이클롭스를 최종 보스로 확정할지 확인해야 한다.
4. 각 퀘스트를 별도 맵으로 제작할지, 맵 크기·시작점·출구·조우 배치와 시야 거리를 정해야 한다.
5. 완료 조건을 `필수 조우 전부 승리 후 출구 도달`로 통일할지, 보스 승리 즉시 완료할지 정해야 한다.
6. 전투 사이 HP·상태·스킬 쿨다운을 유지할지, 중간 회복 지점을 둘지 정해야 한다.
7. 순차 퀘스트 1~5의 재도전·반복 가능 여부와 반복 퀘스트 2개의 해금 시점·무제한 반복 여부를 확정해야 한다.
8. 실패 시 획득 EXP·골드·아이템과 완료 조우 상태를 모두 폐기할지 정해야 한다.

#### B. 레벨·EXP·성장

9. 퀘스트 최초 클리어마다 정확히 1레벨을 올려 5개 후 레벨 6을 보장할지, EXP 표로 같은 결과를 만들지 선택해야 한다.
10. 레벨 2~10 누적 필요 EXP, 퀘스트·조우별 EXP, 레벨 6에서 10까지 필요한 반복 퀘스트 예상 횟수를 확정해야 한다.
11. EXP를 파티원 4명에게 동일하게 전액 지급할지, 분배할지, 전투불능 캐릭터도 받을지 정해야 한다.
12. 레벨업 시 증가할 `STR/DEX/INT/CON/AGI/LUK` 값과 성장 방식이 공통인지 직업별인지 정해야 한다.
13. 레벨업 HP 처리와 원정 중 즉시 레벨업 여부, 최대 레벨 이후 EXP 누적 여부를 정해야 한다.
14. 기존 `totalExperience`를 통계로 유지할지, 캐릭터 EXP로 변환할지, 신규 성장에서는 사용하지 않을지 결정해야 한다.

#### C. 장비·아이템·상점

15. `일반 → 고급 → 희귀 → 레어 → 에픽`을 서로 다른 5등급으로 사용할지 확인해야 한다. `희귀`와 `레어`의 의미가 겹치므로 내부 rarity ID도 필요하다.
16. 해금 순서를 `초기 일반 / 퀘스트1 고급 / 퀘스트3 희귀 / 퀘스트4 레어 / 퀘스트5 에픽`으로 해석해도 되는지 확인해야 한다.
17. 이번 확장에 포함할 장비 슬롯, 양손·보조무기 규칙, 직업 제한, 초기 장비를 정해야 한다.
18. 등급별 실제 장비·소비 아이템의 ID, 가격, 판매가, 능력치, 특수 효과와 재고 규칙이 필요하다.
19. 장비 보정을 기본 능력치에 먼저 더할지, HP·ATK·DEF·전투 AGI 파생 후에 더할지 적용 순서를 확정해야 한다.
20. 구매 후 자동 장착, 판매 기능, 인벤토리 수량·중첩·보유 한도를 정해야 한다.

#### D. 스킬·커스텀 슬롯

21. 14.4절의 직업별 3개 목록과 이름·ID·효과를 그대로 사용할지 승인해야 한다.
22. 사제의 Lv1 스킬을 `heal`과 `smite` 중 무엇으로 할지, 나머지를 Lv2에 배치할지 정해야 한다.
23. 전사·도적·궁수·성기사·사제의 세 번째 스킬을 Lv7에서 Lv5로 옮기고 마법사 화염구와 같은 단계로 맞출지 확인해야 한다.
24. 공통 `basic_attack`이 직업 스킬 3개와 장착 슬롯 수에 포함되는지 확인해야 한다.
25. 레벨 3·7·10 커스텀 슬롯은 누적 1·2·3칸인지, 액티브·패시브가 같은 슬롯을 사용하는지 정해야 한다.
26. 커스텀 스킬의 습득 방법과 해금 레벨, 직업 제한, 교체 장소, 중복 장착 여부를 정해야 한다.
27. 쿨다운 단위, 패시브 적용 시점, 상태 효과 중첩·지속·해제, 전체 대상, 치료, 아군 선택을 이번 확장에 모두 구현할지 단계적으로 제한할지 결정해야 한다.

#### E. 저장·UI·개발 순서

28. 메인과 고정 동료 3명의 레벨·EXP·장비·스킬을 각각 영속 저장할지 확정해야 한다.
29. 퀘스트 최초 완료·반복 횟수·상점 등급 해금을 저장할 신규 version 2 구조와 version 1 마이그레이션 기본값을 정해야 한다.
30. 준비 화면 내 탭 방식과 별도 화면 방식 중 퀘스트 선택·상점·장비·스킬 준비 UI 구조를 선택해야 한다.
31. 결과 화면에서 레벨업, 신규 퀘스트, 장비 등급, 직업 스킬과 커스텀 슬롯 해금을 표시하는 순서를 정해야 한다.
32. 전체 범위를 한 번에 구현하지 않고 `최신 능력치·성별 기준선 → 다중 퀘스트 → 성장·저장 → 스킬 → 상점·장비 → 반복 퀘스트`의 플레이 가능한 단계로 나눌지 승인해야 한다.

### 14.6 확인된 위험

- 현재 `implements.md`의 MVP 범위는 복수 퀘스트, 레벨업, 상점, 인벤토리, 장비, 보스 페이즈를 제외하므로 신규 확장 설계가 승인되면 별도 후속 절로 명시적으로 대체해야 한다.
- 현재 하드코딩된 맵·조우·적 생성 함수를 그대로 복제하면 7개 퀘스트에서 분기와 중복이 급증한다. 최소한 `QuestData → MapData → EncounterData → EnemyData` 참조 구조가 필요하다.
- 캐릭터별 성장을 도입하면서 Actor를 영속 원본으로 직접 사용하면 전투 중 HP·상태와 장기 성장 데이터가 섞인다. 영속 캐릭터와 세션 Actor 스냅샷의 분리가 필요하다.
- 원시 스킬 효과를 모두 첫 단계에 구현하면 상태 효과·대상 선택·쿨다운·패시브 엔진이 동시에 필요해 MVP 우선 원칙과 충돌할 수 있다.
- 퀘스트·장비·성장 수치가 확정되지 않으면 레벨 6·10 도달 속도와 상점 경제를 검증할 수 없다.

### 14.7 사용자 후속 확정과 남은 확인 사항

#### 확정된 사항

- 모든 직업의 세 번째 고유 스킬 해금 레벨은 5다.
- 사제의 Lv1 기본 직업 스킬은 `heal`, Lv2 스킬은 `smite`다.
- 직업 스킬의 상태 효과·치료·패시브·쿨다운·전체 대상 등을 지원하도록 전투 엔진을 확장 설계한다.
- 화산 동굴의 사이클롭스는 최종 보스다.
- 보스가 있는 퀘스트는 보스 처치 즉시 완료한다. 훈련 폐허는 보스가 없으므로 세 번째 필수 조우 승리 시 완료하는 것으로 설계한다.
- 전투 종료 후 HP는 유지하고 전투 상태 효과와 스킬 쿨다운은 초기화한다.
- 순차 퀘스트는 1회씩 완료할 때마다 레벨이 하나씩 올라 퀘스트 5 완료 후 레벨 6이 되게 한다.
- 레벨 6→7, 7→8, 8→9는 반복 퀘스트 1회당 1레벨, 9→10은 반복 퀘스트 2회가 필요하게 한다. 따라서 레벨 6에서 10까지 필요한 적정 반복 횟수는 총 5회다.
- EXP는 파티 공동 보상으로 분배하고 전투불능 캐릭터도 포함한다. 고정 파티가 같은 시점에 같은 레벨에 도달하도록 각 캐릭터의 실수령 EXP를 동일하게 설계한다.
- 레벨마다 직업별 6능력치 상승량 테이블을 둔다.
- 장비 등급은 `일반(흰색) → 고급(녹색) → 희귀(파랑) → 영웅(보라) → 전설(노랑)`이다.
- 해금은 `초기 일반 / 퀘스트1 고급 / 퀘스트3 희귀 / 퀘스트4 영웅 / 퀘스트5 전설`로 적용한다.
- 퀘스트 보상은 해당 단계 장비를 구매하고도 일부 골드가 남도록 가격과 함께 역산한다.
- 커스텀 슬롯은 Lv3·7·10에 하나씩 열려 최대 3칸이다.
- 커스텀 스킬은 훈련 폐허부터 퀘스트 클리어 보상으로 3개씩 제공하고, 슬롯 해금 단계마다 아이템 상점에 3종씩 추가한다.
- 전체 엔진 구조와 에셋 ID를 먼저 설계하고, 각 순차 퀘스트를 하나씩 추가하면서 자동·수동 테스트로 확장한다.

#### 설계자가 수치로 구체화할 수 있는 항목

- 공통 EXP 단위를 100으로 두면 누적 레벨 기준을 `0/100/200/300/400/500/600/700/800/1000`으로 구성할 수 있다. 순차 퀘스트와 반복 퀘스트 1회 보상을 캐릭터당 100 EXP로 맞추면 확정된 진행 횟수를 정확히 만족한다.
- 퀘스트별 전체 EXP 보상을 400으로 두고 4명에게 100씩 균등 분배하면 공동분배와 동일 레벨 진행을 함께 만족한다.
- 적 파티 수·배치는 권장 레벨, 전투 후 HP 유지, 보스 즉시 완료 조건을 기준으로 단계별 난이도가 증가하도록 설계할 수 있다.
- 직업별 레벨 성장표, 적 수치, 퀘스트 골드, 장비 가격은 예상 전투 라운드와 단계별 1~2회 구매 목표를 기준으로 산출할 수 있다.
- 적·배경 최종 에셋이 없어도 콘텐츠 ID별 asset ID와 fallback을 먼저 배정할 수 있다.

#### 아직 사용자 확인이 필요한 항목

1. **장비 슬롯 범위**: 첫 확장에 주무기·보조무기·머리·몸통만 포함할지, 손·발·망토·목걸이·반지까지 모두 포함할지 필요하다.
2. **장비 운영 규칙**: 양손 무기, 직업 제한, 구매 후 자동 장착, 판매 기능·판매율, 재고·인벤토리 한도를 정해야 한다.
3. **장비 보정 순서**: 장비가 `STR/DEX/INT/CON/AGI/LUK`를 보정한 뒤 파생 수치를 다시 계산할지, HP·ATK·DEF·전투 AGI를 직접 보정할지 선택해야 한다.
4. **소비 아이템 범위**: 회복·상태 회복·전투·탐사 아이템 중 이번 확장에 포함할 종류와 전투 중 사용 허용 여부가 필요하다.
5. **커스텀 스킬 지급 의미**: 퀘스트마다 3개를 전부 자동 지급하는지, 3개 후보 중 1개를 선택하는지 확인해야 한다. 순차 5개면 보상 후보가 15개이고 상점 추가분은 9개이므로 풀의 중복·고정·무작위 여부도 필요하다.
6. **커스텀 슬롯 규칙**: 액티브와 패시브가 같은 3칸을 공유하는지, 같은 스킬 중복 장착을 금지하는지 확인해야 한다.
7. **함정간파의 콘텐츠 범위**: `seek_trap`을 유지하려면 7개 맵에 함정·비밀문을 실제 추가할지, 탐사 관련 다른 패시브로 교체할지 결정해야 한다.
8. **전투 효과 공통 규칙**: `cooldown_2/5`의 감소 단위를 행동 턴 또는 라운드 중 무엇으로 할지, 상태 효과 적용·틱 시점·중첩 상한·보스 저항을 정해야 한다.
9. **원정 종료 회복**: 전투 사이 HP 유지와 별개로 퀘스트 성공·실패 후 준비 화면 복귀 시 전원 완전 회복하는 기존 규칙을 유지할지 확인해야 한다.
10. **실패 보상**: 파티 전멸 시 해당 원정의 EXP·골드·아이템을 모두 잃고 퀘스트 진행을 초기화할지 정해야 한다.
11. **기존 순차 퀘스트 재플레이**: 최초 완료 후 퀘스트 1~5를 다시 선택할 수 있는지, 가능하다면 반복 EXP·골드·스킬 보상을 다시 지급할지 정해야 한다.
12. **version 1 마이그레이션**: 기존 누적 골드는 보존하는 것으로 볼 수 있으나 `totalExperience`를 캐릭터 EXP로 변환할지, 통계로만 남기고 4명을 Lv1/0 EXP로 시작할지 확정해야 한다.
13. **남은 원시 ID 정규화**: `celestial shroud → celestial_shroud`, `all_enemy → all_enemies`, `kobolt_killer → kobold_killer`, `bobe → none`으로 수정해도 되는지 확인해야 한다.

- 위 13개 중 1~13은 데이터 구조·저장 호환성·콘텐츠 양을 바꾸므로 확장 설계 지시 전에 확인하는 것이 안전하다.

### 14.8 2차 후속 확정과 최종 잔여 확인사항

이 절은 14.7절의 `아직 사용자 확인이 필요한 항목`에 대한 최신 답변을 반영하며, 잔여 확인 목록은 이 절이 우선한다.

#### 추가 확정 사항

- 첫 확장 장비 슬롯은 무기류, 머리, 몸통만 포함한다. 무기류는 주무기와 필요한 보조무기·방패 표현을 포함한다.
- 직업별 무기 제한은 다음과 같다.

| 직업 | 허용 무기·보조 장비 |
|---|---|
| 전사 | 모든 무기류, 방패 |
| 도적 | 편수류 |
| 성기사 | 날붙이류, 둔기류, 방패 |
| 궁수 | 활 |
| 사제 | 지팡이 |
| 마법사 | 지팡이, 로드 |

- 장비 구입 시 신규 장비를 자동 장착하고 교체된 장비는 거점 공용 창고에 넣는다. 창고 용량은 100칸이다.
- 장비 보정은 종족 기본값·직업 보정·레벨 성장값에 더한 뒤 HP·ATK·DEF·전투 AGI를 다시 계산한다.
- 회복·상태 회복·전투·탐사 소비 아이템을 모두 포함하고 전투 중 아이템 사용을 허용한다.
- 퀘스트 클리어 보상은 임의의 커스텀 스킬 3개를 지급한다. 상점도 임의의 스킬 목록을 제시하며 한 번의 보상 또는 판매 목록 안에서는 같은 스킬을 중복 제시하지 않는다.
- 액티브·패시브는 공통 커스텀 슬롯을 1칸씩 점유하고 같은 스킬의 중복 장착은 금지한다.
- 맵 시스템에 함정과 비밀문을 추가한다.
- 스킬 쿨다운은 라운드마다 1씩 감소하고 전투 종료 시 초기화한다.
- 출혈은 상태 이상 제거 전까지 유지되며 대상의 턴 시작에 현재 중첩 수만큼 피해를 준다. 최대 5중첩이다.
- 보스 저항 정보는 각 스킬 데이터에 별도 표시한다.
- 퀘스트 성공·실패 후 준비 화면으로 돌아오면 파티 HP를 전부 회복한다.
- 전멸 시 해당 원정에서 획득한 EXP·골드·아이템·진행 상태를 모두 폐기한다.
- 순차 퀘스트 1~5는 최초 클리어 시 다음 단계가 열리는 1회성 직선 진행이며 완료 퀘스트는 재플레이하지 않는다.
- version 1 저장 마이그레이션은 지원하지 않는다. 신규 확장 저장은 별도 기준으로 시작한다.
- 원시 스킬 ID의 명백한 오기는 Planner가 직접 정규화할 수 있다. 이번 확인에서 `celestial_shroud`, `all_enemies`, `all_allies`, `kobold_killer`, `none`으로 수정했다.

#### 설계 시 기본값으로 결정 가능한 항목

- 맵 크기·조우 수·적 파티 구성은 권장 레벨, 전투 후 HP 유지와 레벨별 전투 수를 기준으로 산출한다.
- 직업별 레벨 성장량, 적·보스 수치, 장비 능력치·가격, 퀘스트 골드와 소비 아이템 가격은 단계별 전투 라운드와 구매 후 잔여 골드 목표를 기준으로 함께 조정한다.
- 소비 아이템 목록과 효과는 회복·상태 회복·전투 보조·탐사 보조에서 최소 1종씩 시작하고 실제 퀘스트 테스트로 늘린다.
- 적·배경은 최종 이미지가 없어도 콘텐츠 ID와 asset ID, fallback 종류를 먼저 배정한다.
- version 1 마이그레이션을 하지 않으므로 version 2는 파티 레벨 1, EXP 0, 기본 장비·기본 스킬, 훈련 폐허만 해금된 신규 프로필로 시작한다.

#### 추가 확인이 필요한 항목

1. **편수류의 의미**: 도적 장비 제한의 `편수류`를 `한손 무기류`로 해석할지, 단검·한손검 등 특정 종류만 허용할지 확인해야 한다.
2. **양손·방패 규칙**: 전사·성기사가 양손 무기를 장착하면 방패를 자동 해제할지, 활·지팡이·로드가 양손 슬롯을 점유하는지 확정해야 한다.
3. **창고 가득 참 처리**: 자동 장착으로 기존 장비를 보관할 때 100칸이 가득 차면 구매를 거부할지, 신규 장비를 창고로 보내거나 기존 장비를 처분할지 정해야 한다.
4. **판매 기능**: 상점에서 장비·아이템을 판매할 수 있는지와 판매가 비율을 정해야 한다.
5. **전투 아이템 행동 비용**: 아이템 사용이 해당 캐릭터의 행동을 소비하는지, 한 턴에 사용할 수 있는 수량과 소모품 최대 중첩 수가 필요하다.
6. **커스텀 스킬의 전역 중복**: 한 번의 3개 목록 안에서만 중복을 금지하고 과거에 습득한 스킬은 다시 등장할 수 있는지, 이미 보유한 스킬이 나오면 재추첨·골드 전환·중복 보유 중 무엇으로 처리할지 확인해야 한다.
7. **상점 스킬 갱신**: 슬롯 해금 시 추가되는 3종을 고정 재고로 유지할지, 퀘스트 완료마다 무작위로 갱신할지 정해야 한다.
8. **함정·비밀문 규칙**: 기본 발견 가능 여부, `seek_trap` 보유 시 효과, 실패 시 함정 피해·상태와 비밀문 보상 범위를 정해야 한다.
9. **쿨다운 사용 가능 시점**: `cooldown_2` 스킬을 사용한 라운드를 포함해 감소시켜 2라운드 뒤 재사용할지, 다음 라운드부터 감소시킬지 확정해야 한다.
10. **출혈 이외 상태 효과**: 기절·마비·수면·도발·약점노출·능력 상승/하락의 지속, 중첩, 행동 제한과 해제 규칙이 필요하다.
11. **보스 저항 값**: 각 상태 스킬의 보스 적용 확률을 개별 입력할지, 보스 공통 저항률을 두고 스킬별 예외만 기록할지 선택해야 한다.
12. **전멸 시 소비 처리**: 원정 전에 보유하던 소비 아이템을 전투에서 사용한 뒤 전멸하면 사용분도 복구할지, 사용한 상태로 유지할지 정해야 한다.
13. **공통 기본 공격과 직업 스킬 슬롯**: `basic_attack`은 직업 고유 스킬 3개와 커스텀 슬롯 3개 밖의 항상 사용 가능한 명령으로 유지하는지 최종 확인이 필요하다.

- 위 13개가 확정되면 현재 확인 범위에서는 확장 전체 설계를 시작할 수 있다.

### 14.9 3차 후속 확정과 경계 조건 재검토

이 절은 14.8절의 13개 질문에 대한 사용자 답변을 반영한다.

#### 추가 확정 사항

- 도적의 허용 무기는 단검과 한손검이다.
- 양손 무기 장착 시 방패를 자동 해제한다. 활·지팡이·로드는 양손 장비다.
- 장비 창고가 100칸으로 가득 차면 장비 상점 구매를 거부한다.
- 장비와 아이템을 판매할 수 있고 판매가는 구매가의 50%다.
- 회복·상태 이상 치료·직접 피해 아이템은 사용자의 행동을 소비한다.
- 버프 물약은 행동을 소비하지 않지만 같은 이름의 버프는 중첩되지 않는다.
- 과거 획득한 커스텀 스킬도 이후 보상·상점 목록에 다시 등장할 수 있다. 한 번 생성하는 3개 목록 내부에서만 중복을 금지한다.
- 스킬 상점 목록은 퀘스트 완료마다 갱신한다.
- `seek_trap`은 함정과 비밀문 발견에 사용한다. 발견 실패 시 모든 파티원이 2 피해를 받고, 비밀문 너머 방에서는 아이템을 획득한다.
- 쿨다운은 스킬을 사용한 라운드부터 감소한다. `cooldown_2`는 사용 라운드 종료 시 1이 되고 다음 라운드 종료 시 0이 되어 한 라운드를 건너뛴 뒤 다시 사용할 수 있다.
- 기절·마비·도발·약점노출은 스킬에 명시된 턴 동안 유지한다.
- 수면은 공격받기 전까지 유지한다.
- 능력치 변화는 명시된 턴 동안 유지하며 현재 직업 스킬에서는 지속시간보다 쿨다운이 길어 같은 효과를 중첩할 수 없다.
- 보스에게 확률형 효과를 적용할 때 스킬에 명시된 일반 확률의 50%를 사용한다. 예를 들어 수면 50%는 보스에게 25%다.
- 원정 중 사용한 소비 아이템은 전멸해도 복구하지 않는다.
- `basic_attack`은 직업 고유 스킬과 커스텀 슬롯 밖의 항상 사용 가능한 명령이다.

#### 엔진 설계에서 바로 확정 가능한 해석

- 쿨다운은 라운드 종료 시 감소시키되 해당 라운드에 사용된 스킬도 감소 대상에 포함한다.
- 기절과 마비는 대상의 다음 행동 기회를 명시 횟수만큼 건너뛰고, 도발은 적 AI의 대상 선택을 제한하며, 약점노출은 대상이 받는 피해에 명시 보정을 적용하는 별도 효과로 모델링한다.
- 수면을 깨우는 공격은 피해를 먼저 적용한 후 수면을 제거한다. 지속 피해인 출혈은 직접 공격으로 보지 않는다.
- 확률이 없는 패시브·고정 효과에는 보스 50% 규칙을 적용하지 않고, 스킬 효과에 명시된 모든 성공 확률에 적용한다.
- 판매가는 `floor(구매가 × 0.5)`의 정수 골드로 처리할 수 있다.
- 원정 실패 시 신규 보상은 폐기하지만 원정 전에 보유한 아이템 중 실제로 사용한 수량은 이미 소비된 상태로 유지한다.

#### 아직 확인이 필요한 경계 조건

1. **함정·비밀문 발견 판정**: 기본 발견 확률 또는 다이스 기준과 `seek_trap`이 이를 자동 성공으로 만드는지, 성공 확률을 높이는지 확정해야 한다.
2. **함정으로 인한 전멸**: 모든 파티원 2 피해로 HP가 0이 될 수 있는지, 최소 HP 1을 보장할지 정해야 한다.
3. **중복 커스텀 스킬 획득**: 이미 보유한 스킬이 목록에 다시 나오고 실제 지급·구매되면 중복 사본을 보유하는지, 효과 없이 소모되는지, 골드·다른 스킬로 바꾸는지 필요하다.
4. **무료 버프 물약 제한**: 행동을 소비하지 않으므로 한 캐릭터 턴에 서로 다른 버프 물약을 여러 개 사용할 수 있는지, 턴당 1개로 제한할지 정해야 한다.
5. **소비 아이템 보유 규칙**: 종류별 최대 중첩 수와 장비 창고와 별개인 아이템 인벤토리 총 용량이 필요하다.
6. **창고 만료 범위**: 100칸이 가득 찼을 때 장비 구매만 거부하고 소비 아이템·스킬 구매는 허용하는 것으로 해석해도 되는지 확인해야 한다.
7. **상점 스킬 중복 구매 UI**: 보유 스킬이 다시 판매 목록에 등장했을 때 재구매를 허용할지, 보유 표시 후 구매만 막을지 정해야 한다.

- 위 7개를 확정하면 현재 확인된 요구사항에는 추가 사용자 입력 없이 전체 확장 설계를 진행할 수 있다.

### 14.10 4차 후속 확정과 최종 데이터 경계

#### 추가 확정 사항

- 강타의 일반 기절 확률 50%는 보스에게 절반인 25%로 적용한다. 기절에 한정하지 않고 스킬 효과에 명시된 모든 확률은 `bossChance = normalChance × 0.5` 원칙을 따른다.
- 함정 피해로 파티원이 HP 0이 될 수 있으며 전원 HP 0이면 원정 실패로 처리한다.
- 이미 보유한 커스텀 스킬을 다시 획득하면 별도 중복 사본으로 보유한다.
- 버프 물약은 행동을 소비하지 않으므로 같은 캐릭터 턴에 서로 다른 이름의 버프 물약을 여러 개 사용할 수 있다. 같은 이름의 버프만 중첩할 수 없다.
- 캐릭터 개인 인벤토리는 기본 10칸이며 STR에 따라 소폭 증가한다. 한 칸에는 같은 소비 아이템을 최대 10개 중첩한다.
- 거점 장비 창고가 100칸으로 가득 차면 장비·소비 아이템·스킬을 포함한 모든 상점 구매를 차단한다.
- 보유한 커스텀 스킬도 상점에서 다시 구매할 수 있다. 스킬은 수량을 가진 사본으로 관리하고, 같은 공통 스킬 사본을 서로 다른 파티원에게 하나씩 장착할 수 있다.
- 한 캐릭터는 동일 skill ID를 여러 슬롯에 중복 장착할 수 없지만, 파티 전체에서는 보유 사본 수만큼 같은 skill ID를 여러 캐릭터가 각각 장착할 수 있다.

#### 설계 시 산출 가능한 기본값

- STR 인벤토리 보너스는 낮은 레벨에서 직업 간 0~2칸 정도 차이가 나고 최대 레벨에서도 과도하게 증가하지 않도록 단계형 공식과 상한을 둔다. 정확한 공식은 직업별 Lv10 STR 성장표와 함께 계산한다.
- 소비 아이템은 소유 캐릭터의 개인 인벤토리 슬롯과 중첩 수를 사용하고, 장착 장비는 개인 소비 인벤토리 용량을 차지하지 않는 구조가 가장 단순하다.
- 중복 커스텀 스킬은 단순 수량이 아니라 고유 instance ID를 가진 스킬 아이템 목록으로 관리하고, 각 instance는 동시에 최대 한 캐릭터 슬롯에만 연결한다.

#### 아직 확인이 필요한 항목

1. **`기본 발견`의 정확한 의미**: 모든 파티가 함정·비밀문을 자동 발견한다는 뜻인지, 기본 확률 판정을 한다는 뜻인지 필요하다. 기본 판정이라면 확률 또는 다이스 기준과 `seek_trap`의 자동 성공/보너스도 정해야 한다.
2. **개인 인벤토리 이전**: 소비 아이템을 거점에서 파티원 사이에 자유롭게 옮길 수 있는지, 원정 중에도 이전할 수 있는지 확인해야 한다.
3. **커스텀 스킬 사본 저장 위치**: 스킬 사본을 파티 공용 스킬 보관함으로 관리할지, 획득 즉시 특정 캐릭터 개인 소유로 지정할지 정해야 한다.
4. **스킬 상점 구매 대상**: 공용 보관함에 사본을 추가한 뒤 준비 화면에서 배분할지, 구매 시 장착 대상 캐릭터를 바로 선택할지 확인해야 한다.

- 위 4개가 확정되면 확장 전체 설계를 시작하기 위한 사용자 확인 항목은 모두 해소된다.

### 14.11 자동 발견과 고유 스킬 인스턴스 확정

#### 추가 확정 사항

- 보스 확률 절반 규칙은 강타 기절만이 아니라 스킬 데이터에 명시된 모든 확률형 효과에 적용한다. 다이스 명중·피해나 확률이 없는 고정 효과는 대상이 아니다.
- `seek_trap`의 `기본 발견`은 확률이나 다이스 없이 함정과 비밀문을 자동 발견한다는 의미다.
- `seek_trap`이 파티에 장착되어 있으면 함정은 발동 전에 표시되고 비밀문은 진입 가능 상태로 표시된다. 장착되어 있지 않으면 함정은 진입 시 발동하고 비밀문은 발견하지 못하는 단순 규칙으로 설계할 수 있다.
- 소비 아이템은 거점에서 파티원 개인 인벤토리 사이에 이전할 수 있고 원정 중에는 이전할 수 없다.
- 획득하는 각 커스텀 스킬 사본은 고유한 `skillInstanceId`를 가진 아이템이다. 동일한 `skillId`라도 서로 다른 instance ID를 가진다.
- 스킬 인스턴스는 거점 공용 스킬 보관함에 들어가며 준비 화면에서 캐릭터의 개인 커스텀 스킬 슬롯으로 이전·장착한다.
- 구매한 스킬도 먼저 공용 스킬 보관함에 추가한 뒤 같은 배분 절차를 사용한다.
- 예를 들어 `first_aid` 인스턴스 2개를 보유하면 서로 다른 instance ID를 파티원 1과 파티원 2의 슬롯에 각각 장착할 수 있다.
- 장착 해제한 스킬 인스턴스는 거점 공용 스킬 보관함으로 돌아가며 원정 중에는 장착·해제·이전할 수 없다.

#### 권장 데이터 경계

```text
SkillDefinition
{ skillId, name, effect, ... }

SkillInstance
{ skillInstanceId, skillId }

Profile.skillStorage
{ skillInstances[] }

PersistentCharacter.customSkillSlots
{ slotIndex, skillInstanceId | null }[]
```

- `skillId`는 효과 정의를 참조하고 `skillInstanceId`는 소유·장착의 유일성을 보장한다.
- 한 instance ID는 공용 보관함 또는 한 캐릭터 슬롯 중 정확히 한 위치에만 존재해야 한다.
- 캐릭터별 동일 skill ID 중복 장착 금지는 instance가 달라도 적용한다. 서로 다른 캐릭터는 별도 instance가 있으면 같은 skill ID를 각각 장착할 수 있다.

#### 추가 확인이 필요한 항목

1. **스킬 보관함 용량**: 스킬 인스턴스가 장비 창고 100칸을 함께 사용하는지, 별도 무제한 또는 별도 용량의 스킬 보관함을 사용하는지 확인해야 한다.
2. **구매 소비 아이템의 최초 배치**: 소비 아이템 구매 시 받을 캐릭터를 선택할지, 거점 공용 임시 보관함에 넣은 뒤 개인 인벤토리로 이전할지 정해야 한다.

- 위 2개가 확정되면 현재 확인된 확장 요구사항의 사용자 입력은 모두 충족된다.

### 14.12 통합 공용 창고와 수동 배분 구조 확정

#### 권한과 최신 우선 규칙

- 사용자는 Planner가 본 프로젝트의 `architecture.md`와 `changelog.md`를 수정하도록 명시적으로 허가했다.
- 이 절은 14.8절의 `구매 시 자동 장착`과 장비 중심 창고 해석을 대체한다.

#### 확정된 창고·구매·배분 흐름

- 거점 공용 창고는 장비 전용이 아니라 장비, 소비 아이템, 고유 스킬 인스턴스를 함께 저장하는 통합 100칸 보관소다.
- 장비와 소비 아이템을 구매하면 캐릭터에게 자동 장착·지급하지 않고 공용 창고로 이동한다.
- 스킬 구매와 퀘스트 스킬 보상도 고유 `skillInstanceId`를 생성해 공용 창고에 넣는다.
- 구매 시 자동 장착 규칙은 폐기한다.
- 거점 준비 화면에서 다음 수동 작업을 수행한다.
  - 공용 창고의 장비를 선택해 호환되는 파티원에게 장착한다.
  - 공용 창고의 소비 아이템을 파티원의 개인 인벤토리로 분배한다.
  - 공용 창고의 스킬 인스턴스를 해금된 개인 커스텀 스킬 슬롯에 장착한다.
- 장착 장비 교체, 개인 인벤토리 회수, 스킬 장착 해제 시 반환 대상은 공용 창고다.
- 원정 중에는 공용 창고 접근, 캐릭터 간 아이템 이전, 장비·스킬 교체를 허용하지 않는다.
- 공용 창고가 가득 차면 장비·아이템·스킬 상점 구매를 모두 차단한다.

#### 권장 데이터 경계

```text
Profile.storage
├─ capacity: 100
├─ equipmentInstances[]
├─ itemStacks[]
└─ skillInstances[]

PersistentCharacter
├─ equipmentBySlot
├─ inventorySlots
└─ customSkillSlots
```

- 저장 위치는 공용 창고와 캐릭터 보유 영역을 분리하되 모든 항목은 고유 instance 또는 stack ID로 추적한다.
- 동일 instance가 창고와 캐릭터에 동시에 존재하지 않도록 이동 명령은 원본 위치 제거와 대상 위치 추가를 하나의 원자적 상태 변경으로 처리해야 한다.
- 장비 호환성, 개인 인벤토리 용량, 스킬 슬롯 해금·중복 검증을 통과한 경우에만 이동한다.

#### 추가 확인이 필요한 항목

1. **공용 창고 슬롯 계산**: 장비와 스킬 인스턴스는 각각 1칸으로 볼 수 있다. 소비 아이템도 개인 인벤토리처럼 같은 item ID 최대 10개를 1칸에 중첩할지 확인이 필요하다.
2. **퀘스트 보상 초과 처리**: 성공 보상을 받을 때 공용 창고에 빈칸이 부족하면 보상을 임시 보관할지, 선택해서 버릴지, 퀘스트 완료를 보류할지 정해야 한다.
3. **반환 시 창고 만료**: 장비 해제·교체, 개인 아이템 회수, 스킬 해제 시 창고가 가득 차 있으면 작업을 거부할지 정해야 한다.
4. **판매 가능 위치**: 공용 창고에 있는 항목만 판매할지, 장착 장비·개인 인벤토리·장착 스킬도 확인 후 직접 판매할 수 있게 할지 정해야 한다.

- 위 4개가 확정되면 통합 창고와 소유권 이전 구조의 사용자 확인도 완료된다.

### 14.13 창고 용량·퀘스트 보상 초과 처리 확정

#### 추가 확정 사항

- 공용 창고의 소비 아이템은 같은 item ID 기준으로 최대 10개를 한 칸에 중첩한다.
- 장비 instance와 스킬 instance는 각각 한 칸을 사용하며 중첩하지 않는다.
- 퀘스트 진입 전에 공용 창고의 빈칸이 2칸 이하이면 보상 일부를 잃을 수 있다는 경고를 표시한다.
- 창고 경고는 퀘스트 진입을 강제로 차단하지 않는다.
- 퀘스트 성공 보상을 넣을 공간이 부족하면 수용 가능한 보상만 창고에 저장하고 나머지는 포기한다.
- 장비 해제·교체, 개인 인벤토리 회수, 스킬 장착 해제로 공용 창고에 반환해야 할 때 공간이 부족하면 해당 작업을 거부하고 기존 장착·보유 상태를 유지한다.
- 판매는 공용 창고에 있는 장비·아이템·스킬 instance만 허용한다. 캐릭터가 장착·휴대한 항목은 먼저 창고로 반환해야 한다.

#### 창고 슬롯 계산 순서

1. 소비 아이템은 기존의 같은 item ID 미완성 stack을 먼저 채운다.
2. 남은 소비 아이템 수량에 대해 10개당 한 칸을 계산한다.
3. 장비·스킬 instance는 각각 한 칸으로 계산한다.
4. 현재 사용 칸과 신규 필요 칸의 합이 100을 넘는 부분은 초과 보상으로 분리한다.
5. 골드와 EXP는 창고 칸을 사용하지 않으므로 창고가 가득 차도 정상 지급한다.

#### 추가 확인이 필요한 항목

1. **포기 보상 선택 방식**: 창고에 들어갈 보상을 사용자가 결과 화면에서 선택하고 나머지를 포기할지, 시스템이 아이템→장비→스킬 등의 고정 우선순위로 자동 수용할지 정해야 한다.
2. **진입 경고 조작**: 빈칸 2칸 이하 경고에서 `계속 진입`과 `창고로 돌아가기`를 사용자가 선택하게 할지, 단순 안내 후 바로 진입할지 확인해야 한다.

- 위 2개가 확정되면 현재 확인된 확장 설계 선행 질문은 모두 해소된다.

### 14.14 보상 선택·진입 경고 확정 및 사전 확인 완료

#### Markdown 작업 권한

- 사용자는 Planner가 프로젝트 내 Markdown 문서에 대해 편집, 쓰기, 패치와 다중 수정을 수행하도록 허가했다.
- 이 권한은 Planner 역할의 산출물과 사용자 지정 문서에 적용한다.
- 루트 `AGENTS.md`는 정적 공통 규칙이므로 구체적인 내용 변경 요청이 있을 때만 수정한다.
- 외부 LLM용 프롬프트 경로와 분석 대상 원본 문서는 기존 상위 제약에 따라 계속 읽기 전용으로 취급한다.

#### 퀘스트 진입 경고

- 공용 창고 빈칸이 2칸 이하일 때 퀘스트 진입 확인 UI를 표시한다.
- 사용자는 `계속 진입` 또는 `창고로 돌아가기`를 선택할 수 있다.
- `계속 진입`은 용량 부족 가능성을 수락하고 퀘스트를 시작한다.
- `창고로 돌아가기`는 퀘스트를 시작하지 않고 창고 관리 화면으로 복귀한다.

#### 초과 보상 선택

- 퀘스트 성공 시 골드와 EXP는 먼저 전액 지급한다.
- 장비·소비 아이템·스킬 인스턴스가 창고 용량을 넘으면 결과 화면에서 보관할 보상을 사용자가 선택한다.
- 기존 미완성 소비 아이템 stack을 채우는 수량도 실제 필요 칸 계산에 반영한다.
- 선택 결과의 필요 칸이 빈칸 이하일 때만 확정할 수 있다.
- 선택하지 못한 초과 보상은 명시적인 확인 후 영구 포기한다.
- 보상 선택이 끝나기 전에는 준비 화면 복귀를 완료하지 않는다.

#### 사전 확인 완료 판정

- 14.5~14.13절에서 식별한 사용자 확인 항목은 모두 해소되었다.
- 직업별 성장량, 퀘스트·적·장비·소비 아이템 수치, 맵·조우 배치와 에셋 ID는 사용자가 적정값 산출을 Planner에게 위임한 설계 항목이다.
- 다음 사용자 지시에서 전체 확장 설계를 시작할 수 있으며, 그때 `implements.md`에 신규 확장 사양과 단계별 완료·검증 기준을 기록한다.

## 15. v0.2.0 전체 확장 목표 아키텍처

### 15.1 목표와 우선순위

- 목표 버전은 `0.2.0`, 확장 이름은 `7개 퀘스트와 레벨 10 성장`으로 정한다.
- 순차 퀘스트 5개, 반복 퀘스트 2개, 캐릭터별 Lv1~10 성장, 직업·커스텀 스킬, 장비·소비 아이템·상점·통합 창고를 하나의 영속 프로필에서 연결한다.
- 전체 구조를 먼저 고정하되 구현은 훈련 폐허부터 퀘스트 하나씩 플레이 가능한 상태로 늘린다.
- 현재 React → 순수 TypeScript 엔진 → React/Phaser 이벤트 표현 의존 방향을 유지한다.
- 범용 효과 DSL, 플러그인 시스템, 서버, 계정, 온라인 동기화는 도입하지 않는다.

### 15.2 최상위 책임 경계

```text
React UI / Phaser input
          │ GameCommand
          ▼
순수 TypeScript gameEngine
          │ EngineResult { state, events, persistence }
          ├───────────────┐
          ▼               ▼
gameStore 저장 adapter   React/Phaser 표현
```

- `gameEngine`은 명령 검증과 상태 전이를 담당한다.
- `gameStore`는 구독, dispatch, `localStorage` v2 읽기·쓰기만 담당한다.
- React는 거점·상점·장착·보상 선택 UI, Phaser는 탐사·전투 연출을 담당한다.
- 게임 판정은 메시지 문자열을 파싱하지 않고 구조화 이벤트 payload를 사용한다.

### 15.3 영속 프로필과 원정 세션 분리

```text
ProfileV2 (영속)
├─ gold
├─ characters[4]
├─ storage
├─ questProgress
├─ shop
├─ random sequences
└─ pendingReward

ExpeditionSession (비영속)
├─ questId / seed / rngState
├─ party Actor snapshots
├─ exploration / combat
├─ completedEncounterIds
├─ traps / secrets
├─ pendingLoot
└─ logs
```

- 영속 캐릭터는 레벨·EXP·성장·장비·개인 인벤토리·커스텀 스킬 슬롯의 원본이다.
- Actor는 원정 시작 시 영속 캐릭터에서 만든 HP·ATK·DEF·전투 AGI·상태용 스냅샷이다.
- 현재 HP, 탐사 좌표, 조우 진행, 전투 상태·쿨다운은 저장하지 않는다.
- 원정 중 소비한 아이템 수량은 즉시 프로필에서 차감해 실패해도 복구하지 않는다.
- 성공 정산 중 창고 초과 선택은 `pendingReward`로 저장해 새로고침 후 복구한다.

### 15.4 저장 v2

- 저장 키는 `party_night_profile_v2`, envelope version은 `2`다.
- version 1 저장은 읽거나 변환·삭제하지 않는다.
- v2 저장이 없으면 이름·종족·직업·성별을 입력해 신규 4인 파티를 생성한다.
- 신규 프로필은 파티 전원 Lv1/0 EXP, 골드 300, 일반 대표 무기 장착, 훈련 폐허만 해금된 상태다.
- 손상된 v2 저장은 부분 복구하지 않고 새 프로필 생성을 제공한다.
- 저장 검증은 외부 schema 라이브러리 없이 명시적 type guard와 불변식 검사로 처리한다.

### 15.5 캐릭터 능력치와 성장

```text
종족 기본 능력치
+ 직업 보정
+ 레벨 누적 성장
+ 장착 장비 보정
= 최종 STR/DEX/INT/CON/AGI/LUK
→ 기존 deriveCombatStats
→ maxHp/atk/def/battleAgi
```

- EXP 누적 기준은 Lv1~9까지 100 간격, Lv9→10만 200으로 한다: `0,100,200,300,400,500,600,700,800,1000`.
- 각 퀘스트 성공은 파티 총 400 EXP이며 4명에게 100씩 지급한다. 전투불능 캐릭터도 받는다.
- 순차 5회 후 Lv6, 반복 3회 후 Lv9, 반복 2회 추가 후 Lv10이 된다.
- Lv10 EXP는 1000에서 고정한다.
- 모든 직업은 레벨업 9회에 총 18 능력치 포인트를 얻고 직업별 성장표로 분배한다.
- 개인 인벤토리 칸은 `10 + min(3, floor(finalSTR / 5))`, 최대 13칸이다.

### 15.6 통합 창고와 소유권

```text
Profile.storage (100칸)
├─ equipmentInstances   # instance당 1칸
├─ itemStacks           # 같은 itemId 최대 10개당 1칸
└─ skillInstances       # instance당 1칸
```

- 모든 구매와 퀘스트 실물 보상은 통합 창고로 들어간다.
- 거점에서만 창고→캐릭터 장비, 개인 아이템 인벤토리, 개인 스킬 슬롯으로 이전한다.
- 동일 instance는 창고 또는 정확히 한 캐릭터 위치에만 존재한다.
- 이동은 최종 용량·호환성·슬롯을 먼저 검증한 뒤 원자적으로 처리한다.
- 장비·아이템·스킬 판매는 창고 항목만 가능하고 가격은 `floor(구매가 × 0.5)`다.
- 창고가 100칸이면 모든 구매와 반환 작업을 거부한다.

### 15.7 장비·아이템 구조

- 장비 슬롯은 `weapon`, `offhand`, `head`, `body`다.
- 단검·한손검·둔기·방패는 한손/보조 장비이며 활·지팡이·로드는 양손 장비다.
- 양손 장비 장착 시 offhand를 창고로 반환하고, 반환 공간이 없으면 장착을 거부한다.
- 장비는 6기본 능력치만 보정하고 파생 수치를 직접 보정하지 않는다.
- 등급은 `common/uncommon/rare/heroic/legendary`, 색상은 흰/녹/파랑/보라/노랑이다.
- 초기 일반, 퀘스트1 고급, 퀘스트3 희귀, 퀘스트4 영웅, 퀘스트5 전설을 누적 해금한다.
- 소비 아이템은 회복·상태 치료·직접 피해·탐사·무료 버프를 포함한다.
- 회복·치료·피해 아이템은 행동을 소비하고, 서로 다른 무료 버프는 같은 턴에 여러 개 사용할 수 있다.

### 15.8 스킬 정의와 인스턴스

```text
SkillDefinition { skillId, class/source, target, roll, cooldown, effects }
SkillInstance { skillInstanceId, skillId }
Character.customSkillSlots[3] { skillInstanceId | null }
```

- `basic_attack`은 슬롯 밖 상시 명령이다.
- 직업 스킬은 Lv1·2·5에 자동 해금하며 instance를 만들지 않는다.
- 커스텀 슬롯은 Lv3·7·10에 하나씩 열린다.
- 커스텀 스킬은 획득·구매마다 고유 instance를 만들고 통합 창고 1칸을 사용한다.
- 같은 캐릭터는 동일 skill ID를 중복 장착할 수 없고, 다른 캐릭터는 별도 instance로 같은 스킬을 장착할 수 있다.
- 퀘스트 성공 보상과 상점 offer는 각각 서로 다른 skill ID 3개를 시드 기반 비복원 추출한다.
- 과거 보유 스킬도 다시 등장할 수 있고 상점 offer는 퀘스트 성공마다 갱신한다.

### 15.9 전투 상태·쿨다운·확률

- 쿨다운은 스킬 사용 시 N으로 설정하고 사용 라운드 종료부터 1씩 감소한다.
- 출혈은 대상 턴 시작에 중첩 수만큼 DEF 무시 피해를 주며 최대 5, 치료 전까지 유지된다.
- 기절·마비는 명시된 행동 횟수를 건너뛰고, 도발·약점노출·능력 변화는 명시된 턴 동안 유지한다.
- 수면은 직접 공격 피해 적용 후 제거되며 출혈로는 해제되지 않는다.
- 스킬 효과에 명시된 모든 성공 확률은 보스에게 절반으로 적용한다.
- 성력·마력은 전투별 0에서 시작해 최대 5까지 축적하고 전투 종료 시 제거한다.
- 전투 종료 후 HP만 유지하고 상태·쿨다운·전투 자원은 초기화한다.
- 효과는 제한된 union과 소수의 명시적 resolver로 구현하고 범용 DSL을 만들지 않는다.

### 15.10 탐사·퀘스트 콘텐츠 구조

```text
QuestDefinition → MapDefinition → EncounterDefinition → EnemyDefinition
```

- 맵은 rows, 시작점, 조우 배치, 함정, 비밀문을 데이터로 가진다.
- 파티에 살아 있는 `seek_trap` 보유자가 있으면 함정·비밀문을 자동 발견한다.
- 없으면 함정 진입 시 전원 2 피해, 비밀문은 발견하지 못한다. 함정 피해로 전멸할 수 있다.
- 훈련 폐허는 세 번째 필수 조우 승리, 나머지는 보스 승리 즉시 완료한다.
- 순차 퀘스트 1~5는 한 번씩만 완료할 수 있고, 퀘스트5 후 반복 퀘스트 2개가 열린다.

### 15.11 결정론적 랜덤

- 프로필은 `rootSeed`, 원정 sequence, instance sequence, shop revision을 저장한다.
- 원정·상점·보상 seed는 root seed와 명시적 sequence/ID로 파생한다.
- instance ID는 난수가 아니라 증가 sequence로 생성한다.
- `Math.random()`과 판정용 `Date.now()`를 사용하지 않는다.
- 같은 프로필 seed와 명령 순서에서 조우, 다이스, AI, 상태 확률, 스킬 보상·offer가 재현되어야 한다.

### 15.12 성공·실패 정산

- 실패 시 pending loot·골드·EXP·진행을 폐기하고 이미 사용한 소비 아이템은 복구하지 않는다.
- 성공 시 골드·EXP·레벨·성장·퀘스트·등급·offer를 먼저 영속 적용한다.
- 창고가 실물 보상을 모두 수용하면 자동 저장한다.
- 초과하면 결과 화면에서 사용자가 보관할 장비·아이템·스킬을 선택하고 나머지를 확인 후 포기한다.
- `pendingReward`가 있으면 새 퀘스트·상점·배분을 막고 정산 화면을 복구한다.
- 성공·실패 후 거점 복귀 시 다음 원정 Actor는 full heal 상태로 다시 생성한다.

### 15.13 거점 UI와 화면 흐름

```text
start → profile_create → hub
hub tabs: quest | storage | characters | shop
quest → warning? → exploration ↔ battle → result → hub
```

- 전역 화면 수를 늘리기보다 모바일 가로 거점 탭으로 퀘스트·창고·캐릭터·상점을 묶는다.
- 창고 빈칸이 2칸 이하이면 `계속 진입`/`창고로 돌아가기`를 표시한다.
- 결과 UI는 성공/실패 → 골드/EXP/레벨 → 해금 → 보상/overflow → 거점 복귀 순서다.
- UI와 Phaser는 명령을 보내고 이벤트·상태를 표시할 뿐 수치·확률·소유권을 계산하지 않는다.

### 15.14 호환성·위험

- 현재 코드의 최신 성별·능력치 미반영부터 먼저 해소해야 한다.
- v1 migration을 지원하지 않으므로 v0.2.0 진입 시 기존 진행은 이어지지 않는다.
- 45종 장비, 13종 커스텀 스킬, 16종 적과 7개 맵을 한 번에 구현하지 않고 퀘스트 단계별로 활성화한다.
- 직업 스킬 원시 효과는 현재 엔진보다 넓으므로 전투 엔진 경계를 먼저 교체해야 한다.
- 최종 밸런스는 각 단계 결정론적 전투 테스트와 실제 플레이에서 HP를 10% 단위로 조정하고 DEF 증가는 마지막에 사용한다.
- 최종 적·배경 에셋이 없어도 모든 content ID에 asset ID와 도형·tint fallback을 미리 둔다.

## 16. 전투 UI·스킬 의미 정합성 분석

### 16.1 분석 범위와 근거

- 사용자 확인 결함: 전투 로그 과거 항목 접근 불가, 대상 선택 취소 부재, 일반 공격 다이스 결과 연출 부재, 결과 화면의 skill ID 노출, 패시브 스킬의 액티브 명령 노출, `taunt`의 단일 대상·가짜 피해 처리.
- 코드 근거: `src/ui/{GameHud,BattleCommands,ResultScreen}.tsx`, `src/game/{types,content,combat}.ts`, `src/phaser/BattleScene.ts`, `src/styles.css`.
- 이 절은 현재 구현 사실과 수정 목표를 구분한다. 실제 구현 사양은 `implements.md` 24절을 따른다.

### 16.2 확인된 현재 상태와 원인

#### 전투 로그

- engine session은 로그를 최대 200건 유지하지만 `GameHud`는 `session.logs.slice(-7).reverse()`만 렌더링한다.
- `.log-box`는 `overflow: hidden`이고 내부 목록에도 세로 스크롤이 없다.
- 따라서 오래된 로그는 DOM에 존재하지 않고 CSS 스크롤로도 접근할 수 없다. 이는 저장 용량 문제가 아니라 UI의 7건 절단과 overflow 설정이 함께 만든 결함이다.

#### 대상 선택 취소

- `SELECT_SKILL`은 전투를 `awaiting_target`으로 전환하지만 이 상태를 `awaiting_action`으로 되돌리는 command가 없다.
- `BattleCommands`의 대상 단계에는 대상 버튼만 있고 취소 버튼이 없다.
- 잘못 선택한 스킬은 유효 대상을 선택해 행동을 완료하기 전까지 변경할 수 없다.

#### 일반 공격 다이스 연출

- `BattleScene`은 `combat.pendingRoll`이 존재할 때만 다이스를 그린다.
- 리롤 스킬은 `awaiting_reroll` 동안 pending roll이 상태에 남으므로 화면에 보인다.
- 리롤이 없는 공격은 같은 dispatch 안에서 굴림과 피해 적용이 끝나고 `pendingRoll`이 즉시 `null`이 된다. Scene subscriber가 최종 상태를 받을 때는 표시할 다이스가 없다.
- `DICE_ROLLED` 이벤트는 존재하지만 구조화된 다이스 payload가 없고 Scene은 노란 flash만 실행한다. 메시지 문자열 파싱으로 복구하면 이벤트 계약과 표현 계층 분리 원칙을 위반한다.

#### 레벨업 스킬 표시

- 성장 정산은 `unlockedClassSkillIds`에 영문 skill ID를 저장한다.
- `ResultScreen`이 이 배열을 그대로 `join(', ')`하여 표시하므로 콘텐츠의 표시 이름을 거치지 않는다.
- 데이터 원본은 ID 유지가 맞으며 결함은 UI 표시 변환 누락이다.

#### 패시브 스킬의 명령 노출

- 현재 `Skill` 타입에는 active/passive 구분이 없다.
- `getUnlockedClassSkillIds`는 액티브와 패시브를 함께 Actor의 `skillIds`에 넣고, `BattleCommands`는 `actor.skillIds` 전체를 버튼으로 표시한다.
- `combat.selectSkill`도 소유 여부와 사용 횟수만 검사하고 패시브 여부를 거부하지 않는다.
- 그 결과 `seek_trap`은 0다이스 적 대상 피해 스킬, `protection_pledge`는 0다이스 아군 회복 스킬처럼 잘못 모델링되어 선택 가능하다.
- `seek_trap`의 탐사 자동 발견은 별도 조회로 동작하지만, `protection_pledge`의 아군 직접 피해 감소 효과는 아직 전투 계산에 연결되지 않았다.

#### `taunt` 전체 대상 처리

- 승인 데이터의 `taunt`는 `all_enemies`, 비피해 상태 효과다.
- 현재 제한된 `Skill.target`은 `enemy|ally`만 표현하므로 단일/전체/자신 대상을 구분하지 못한다.
- 모든 액티브 스킬이 `awaiting_target`으로 이동하며 `taunt`도 적 하나를 요구한다.
- `taunt`는 `effect: damage`, `diceCount: 0`으로 등록되어 기본 최소 피해식에 의해 대상 하나에 최소 1 피해를 주고 실제 도발 상태는 만들지 않는다.
- 사용자가 본 노란 flash는 `DICE_ROLLED`에 대한 공통 Phaser 연출이며 도발 효과 적용의 증거가 아니다.

### 16.3 목표 스킬 의미 모델

```text
SkillDefinition
├─ activation: active | passive
├─ targetMode: single_enemy | all_enemies | single_ally | self
├─ resolution: damage | heal | taunt | passive_seek_trap | passive_protection
├─ dice / fixedModifier / rerolls
└─ useLimit
```

- active/passive와 대상 수·대상 진영을 서로 다른 축으로 표현한다.
- Actor는 해금된 전체 skill ID를 보유할 수 있지만 전투 명령 UI와 `selectSkill`은 active만 허용한다.
- passive는 명령으로 실행하지 않고 관련 판정 지점에서 살아 있는 보유자를 조회한다.
- 범용 효과 DSL을 도입하지 않고 현재 필요한 명시적 resolver만 둔다.

### 16.4 패시브와 도발 처리 경계

- `seek_trap`: 현재처럼 탐사 시작의 살아 있는 보유자 조회에만 사용한다. 전투 버튼을 생성하지 않는다.
- `protection_pledge`: 살아 있는 보유자가 있으면 파티가 받는 직접 공격의 최종 피해를 1 감소시킨다. 기존 최소 피해 기준을 유지해 `max(1, calculatedDamage - 1)`로 처리한다.
- 함정, 향후 출혈 등 직접 공격이 아닌 피해에는 `protection_pledge`를 적용하지 않는다.
- `taunt`: 대상 선택과 적용 성공 판정 없이 모든 살아 있는 적에 항상 도발 상태를 기록한다.
- 도발된 각 적이 다음 실제 공격을 시작할 때 서로 독립적으로 50%를 판정한다. 성공하면 시전자가 살아 있는 경우 시전자를 대상으로 삼고, 실패하면 기존 AI 대상 선택을 수행한다.
- 일반 적과 boss 모두 다음 공격 대상 전환 확률은 50%다. 기존 일반 50%/boss 25% 규칙은 도발에는 적용하지 않는다.
- 공격 대상 판정의 성공·실패와 관계없이 해당 적의 도발은 다음 공격 한 번에 소비한다. 시전자가 이미 쓰러졌으면 상태를 제거하고 정상 AI 대상 선택으로 돌아간다.
- `taunt`는 HP를 변경하거나 다이스 굴림·적용 저항 이벤트를 만들지 않는다. 사용 시 전체 적 상태 적용 이벤트, 다음 공격 시 대상 전환 성공·실패 이벤트를 구분한다.

### 16.5 표현과 상태의 분리

- 굴림 완료 시 engine은 메시지와 별도로 actor/target/skill ID, 원본·최종 다이스, 고정 보정, 합계를 가진 구조화 `ROLL_RESOLVED` 이벤트를 낸다.
- Phaser는 combat state의 `pendingRoll`과 별개인 transient overlay queue로 각 굴림 결과를 1초간 표시한다.
- 한 dispatch에 적 연속 행동 등 복수 굴림 이벤트가 있으면 순서대로 각각 1초 표시한다.
- overlay 재생은 이미 확정된 상태를 지연하거나 변경하지 않는다. actor layer 재렌더링과 분리해 `removeAll`에 의해 사라지지 않게 한다.

### 16.6 로그·표시 이름 UI 원칙

- 로그는 engine이 유지한 최대 200건을 시간순으로 렌더링하고 목록 영역만 터치 세로 스크롤을 허용한다.
- 사용자가 최신 위치에 있을 때만 신규 로그를 따라가고, 과거 로그를 읽는 중에는 강제로 최신 위치로 이동시키지 않는다.
- skill ID는 저장·이벤트·정산 원본으로 유지하고 화면은 공통 `getSkillDisplayName(skillId)` selector를 사용한다.
- 알 수 없는 ID만 진단 가능한 fallback으로 원본 ID를 표시한다.

### 16.7 위험과 후속 정합성

- `taunt` 상태는 단계 6의 전체 상태·쿨다운 엔진에 병합할 수 있는 제한된 명시 상태로 구현해야 하며 별도 병렬 상태 체계를 만들지 않는다.
- 피해 연출용 flash와 상태 적용 연출을 구분하지 않으면 비피해 행동이 공격처럼 보일 수 있다.
- 다이스 overlay가 게임 상태 전이를 기다리게 하면 애니메이션 실패가 판정에 영향을 주므로 금지한다.
- 로그 자동 스크롤이 무조건 실행되면 사용자가 과거 항목을 읽을 수 없으므로 bottom 근접 여부를 기준으로 해야 한다.

### 16.8 `cooldown_x` 미작동 원인

- 승인 데이터의 `power_strike`, `quick_stab` 등은 `cooldown_2`지만 실행 `Skill` 타입에는 cooldown이나 use limit union이 없고 `oncePerBattle: boolean`만 있다.
- 현재 콘텐츠는 `power_strike`, `quick_stab`, `aimed_shot`, `holy_strike`, `arcane_bolt`, `taunt`, `find_leak`, `smite`, `lightning_bolt`를 모두 `oncePerBattle: true`로 등록했다.
- `CombatState`도 `usedSkillIdsByActor`만 보관하므로 남은 cooldown, 감소 시점과 재사용 가능 상태를 표현할 수 없다.
- `selectSkill`은 사용 ID가 배열에 있으면 전투 종료까지 계속 거부하고, `resolvePending`은 ID를 추가하지만 제거하지 않는다.
- `stepTurn`은 turn index와 round 숫자만 변경하며 라운드 종료 cooldown 감소를 수행하지 않는다.
- `BattleCommands`도 같은 used 배열로 버튼을 영구 disabled 처리한다. 따라서 엔진과 UI가 모두 전투당 1회라는 잘못된 의미를 중복 반영한다.
- 기존 테스트도 `quick_stab`을 once-per-battle로 가정해 잘못된 동작을 회귀 기준으로 고정한다.

승인 cooldown 의미:

```text
1라운드 actor turn: cooldown_2 사용 → remaining 2
1라운드 종료: remaining 1
2라운드 종료: remaining 0
3라운드 actor turn: 재사용 가능
```

- cooldown은 actor·skill별 숫자 상태로 관리한다.
- 전체 turn order가 wrap되어 새 round가 시작되는 한 지점에서 양수 cooldown을 정확히 한 번 감소시킨다.
- 사용한 라운드 종료 감소에는 방금 사용한 스킬도 포함한다.
- 전투 종료 시 cooldown을 모두 폐기한다.
- 죽은 actor 건너뛰기나 enemy AI 연속 처리로 같은 round에 중복 감소하지 않아야 한다.

### 16.9 전체 콘텐츠 ID 표시 감사

확정된 사용자 노출:

- `ResultScreen`은 신규 직업 스킬 ID를 그대로 join해 `taunt`, `seek_trap`, `protection_pledge` 등을 표시한다.
- 자동 보관된 비스킬 보상은 표시 이름 대신 `rewardId`를 출력해 `secret_goblin_den_secret_1`이 노출될 수 있다.
- quest 해금 이벤트는 `unlockedQuestIds.join`을 사용해 `goblin_den_quest`, `ancient_site_quest`를 메시지에 포함한다.
- 장비 slot은 `weapon`, `offhand`, `head`, `body`, 방향은 `north/east/south/west` 또는 대문자 enum으로 표시된다.
- command 거부 메시지는 command type을 앞에 붙여 `BUY_SKILL: ...` 같은 개발용 식별자를 사용자에게 노출한다.

분산 fallback 위험:

- `StoragePanel`, `CharacterPanel`, `ShopPanel`, `ResultScreen`이 각각 `name ?? contentId` 패턴을 사용한다.
- 정상 데이터에서는 대표명이 표시되지만 신규 콘텐츠 이름 등록 누락 시 영문 ID가 바로 UI에 노출된다.
- `skillInstanceId`는 동일 스킬 사본을 구분하는 고유 identity이므로 필요할 때 보조 진단 정보로 표시할 수 있다. content ID, reward ID, stack ID를 주 라벨로 사용하는 것과 구분한다.
- Phaser asset fallback 경고와 개발자 console의 content/instance ID는 진단 정보이므로 유지한다.

목표 표시 경계:

```text
content ID / instance ID / command type
        │ game state·command·event identity로 유지
        ▼
명시적 display-name selector
        │
        ▼
사용자 UI·로그·결과·알림의 대표명
```

- skill, equipment, item, quest, map, enemy, class, race, slot, direction과 mixed reward에 타입별 표시 이름 selector를 둔다.
- `getRewardDisplayName`은 reward kind를 판별하고 `rewardId`가 아닌 실제 content ID의 대표명을 반환한다.
- 일반 UI의 알 수 없는 값은 `알 수 없는 스킬/장비/아이템`처럼 안전한 문구를 사용하고 원본 ID는 개발 console에 기록한다.
- 퀘스트 탭과 적 이름도 하드코딩·ternary 대신 `QUEST_DATA`, enemy definition의 대표명을 사용해 콘텐츠 확장 시 불일치를 줄인다.

## 17. `seek_trap` 탐사 결과 로그 분석

### 17.1 현재 상태

- `seek_trap`은 `passive/self/passive_seek_trap`으로 정의되어 전투 명령에서는 제외된다.
- `beginExpedition`은 원정 시작 시 살아 있는 `seek_trap` 보유자가 한 명이라도 있으면 맵의 모든 trap ID와 secret ID를 각각 `discoveredTrapIds`, `discoveredSecretIds`에 즉시 기록한다.
- 발견된 함정은 `ExplorationScene`에 표시되고 진입해도 발동하지 않는다. 발견된 비밀문은 벽 판정이 해제되고 Scene에 표시된다.
- 이 자동 발견은 상태만 변경하고 `GameEvent`를 만들지 않는다. 초기 `session.logs`에도 원정 입장 문장만 있어 사용자가 패시브의 발동·발견 결과를 확인할 수 없다.
- 미발견 함정 진입은 이미 `함정 발동: 파티 전원 N 피해` 메시지를 만들지만 이벤트 type이 일반 `DAMAGE_APPLIED`라서 함정 발동과 직접 공격 피해를 구조적으로 구분할 수 없다.
- 비밀방 보상 칸 진입 시 `비밀방 보상을 발견했다.`는 로그가 생성되지만, 이는 비밀문·비밀방 발견 시점이 아니라 이미 발견된 비밀방에서 보상을 획득한 시점이다.

### 17.2 책임과 로그 시점

```text
원정 시작
→ 살아 있는 seek_trap 보유자 조회
→ trap/secret 자동 발견 상태 확정
→ 함정 발견·비밀방 발견 이벤트와 로그

탐사 이동
→ 미발견 함정 칸 진입
→ 함정 발동 이벤트와 피해·전멸 판정

비밀방 보상 칸 진입
→ 보상 발견·획득 이벤트
```

- 자동 발견 규칙 자체는 변경하지 않는다. 위치 접근 시점으로 지연하지 않고 현재와 동일하게 원정 시작 시 전체 배치가 발견된다.
- 복수의 살아 있는 보유자가 있어도 패시브는 중첩하지 않는다. 파티 배열에서 첫 번째 보유자를 결정론적 `sourceActorId`로 사용하고 placement마다 로그를 한 번만 만든다.
- trap placement마다 `TRAP_DISCOVERED`, secret placement마다 `SECRET_ROOM_DISCOVERED`를 생성한다.
- 패시브가 없거나 보유자가 전투불능이면 발견 이벤트를 만들지 않으며 기존 미발견 상태를 유지한다.
- 미발견 함정 진입은 `TRAP_TRIGGERED`로 구분하고 damage를 payload에 포함한다. 사용자 로그는 기존 수치와 조건을 보존한다.
- `TRAP_DISCOVERED`와 `TRAP_TRIGGERED`는 상호 배타적이다. 이미 발견된 함정에 진입할 때 발동 로그나 피해를 만들지 않는다.
- 비밀방 발견 로그와 비밀방 보상 획득 로그는 서로 다른 사실이므로 각각 한 번 유지한다.

### 17.3 사용자 메시지 기준

- 함정 발견: `{캐릭터명}의 함정간파: 함정을 발견했다.`
- 비밀방 발견: `{캐릭터명}의 함정간파: 비밀방을 발견했다.`
- 함정 발동: `함정 발동: 파티 전원 {damage} 피해`
- 비밀방 보상: `비밀방에서 {아이템 대표명}을 발견했다.`
- placement ID, skill ID와 reward ID는 로그에 노출하지 않고 구조화 payload에만 유지한다.
- 같은 종류의 placement가 여러 개라면 각 placement 결과를 각각 기록한다. 현재 고블린 소굴은 함정 1개·비밀방 1개이므로 각 메시지가 최대 한 번 생성된다.

### 17.4 영향과 위험

- `beginExpedition`이 session뿐 아니라 discovery event 목록을 반환하거나 session 생성 직후 공통 `appendEvents`를 사용해야 state와 외부 event 결과가 일치한다.
- `SESSION_STARTED`와 발견 이벤트의 사용자 순서는 `원정 시작 → 함정 발견 → 비밀방 발견`으로 고정한다.
- session 초기 로그에 메시지를 직접 넣고 같은 이벤트를 다시 `appendEvents`하면 중복되므로 단일 경로만 사용해야 한다.
- 함정 발동을 `TRAP_TRIGGERED`로 교체할 때 기존 피해 화면 연출이 `DAMAGE_APPLIED`만 조회한다면 연출이 사라질 수 있다. Phaser가 두 type을 모두 직접 피해 연출 대상으로 처리하거나, 피해 이벤트와 발동 이벤트를 분리하되 사용자 로그 중복을 막아야 한다.
- 현재 profile 저장 형식과 발견 ID 배열 구조는 변경할 필요가 없다.

## 18. 수동 밸런스 변경과 전투 상태 수명 분석

### 18.1 분석 범위와 기준

- 기준 자료는 사용자가 직접 갱신한 `raw_data_table.md` 7.2~7.3절과 22.7~22.9절이다.
- 현재 구현 비교 대상은 `src/game/{types,content,combat,rewards,gameEngine}.ts`와 `src/tests/{combat,stage6,stage7,stage89}.test.ts`다.
- 이번 변경은 전투 판정·경제 수치의 교체이며 profile version, 콘텐츠 ID, 퀘스트 진행 순서와 에셋 registry는 변경하지 않는다.
- `raw_data_table.md`는 원본 승인 데이터로 보존하며 실제 구현은 후속 Coder 작업이다.

### 18.2 승인 데이터와 현재 코드 차이

| 영역 | 수동 승인값 | 현재 코드 상태 | 영향 |
|---|---|---|---|
| `quick_stab` | 일반 bleed 100%, boss 50% | 일반 50%, boss 25% | 상태 확률 resolver·결정론 테스트 수정 |
| `neurotoxin` | 일반 100%, boss 50%; 전투 AGI 50% 감소, 비중첩 | 일반 50%, boss 25%; Actor AGI 값은 감소하지만 고정 turn order에는 반영되지 않음 | 확률·행동 순서 갱신 경계 수정 |
| `find_leak` | 궁수의 다음 행동 종료까지; 그 전에 사망하면 사망 라운드 종료까지 | 대상의 다음 행동 종료 시 만료 | 상태 소유자를 target이 아니라 source 수명과 연결 |
| 퀘스트 골드 | 1100/1100/1800/2700/4000/4000/4000 | 300/320/500/720/1050/760/820 | 콘텐츠·정산·결과·경제 테스트 동기화 |
| 미노타우르스 | ATK8, `3d6+2`, 기절40% | ATK9, `4d6`, 기절50% | 적 정의·스킬 metadata·확률 테스트 수정 |
| 사이클롭스 | ATK10, `3d6+4`, 기절40% | ATK11, `4d6+4`, 기절50% | 적 정의·스킬 metadata·확률 테스트 수정 |
| 스켈레톤 킹 | HP145/ATK9/DEF7, 전체 `2d6` | HP155/ATK11/DEF8, 전체 `3d6` | 적 정의·전체 공격 회귀 테스트 수정 |
| 리치 | HP125/ATK10/DEF7/AGI6, `3d6+4` | 승인값과 동일 | 변경 불필요, 회귀만 유지 |

확인된 코드 상태:

- 상태 확률은 `chanceBySkill`의 일반 확률을 읽고 target이 boss이면 절반으로 변환한다. 따라서 두 도적 스킬의 일반값만 100으로 교체하면 승인된 boss 50%도 같은 공통 규칙으로 얻는다.
- 신경독은 최초 적용 때 `Actor.agi = max(1, floor(agi × 0.5))`를 저장하고 제거 시 `originalAgi`로 복원한다. 수치 자체는 전투 AGI 필드에 반영되지만 `turnOrder`가 전투 시작 후 고정되어 행동 순서 효과가 발생하지 않는다.
- 약점 노출은 target ID 아래 `{ bonusDamage, remainingActions }`만 저장하고 target의 행동 종료 때 감소한다. source 궁수 ID와 적용 라운드 정보가 없어 신규 수명을 표현할 수 없다.
- 골드는 `QUEST_DATA.goldReward`와 각 settlement 함수에 중복된 literal로 존재한다. 한쪽만 변경하면 결과 화면 표시와 실제 profile 지급액이 달라질 수 있다.

### 18.3 신경독과 전투 AGI 흐름

신경독은 파생 기본 능력치를 다시 계산하는 장비·버프가 아니라 현재 전투의 행동 AGI에 직접 적용하는 상태다.

```text
신경독 성공
→ 대상 current battle AGI를 max(1, floor(current AGI × 0.5))로 변경
→ 같은 신경독 재적용은 AGI를 다시 절반으로 만들지 않고 bleed만 +1
→ 아직 행동하지 않은 현재 라운드 tail을 현재 AGI 기준으로 안정 정렬
→ 다음 라운드 시작 시 전체 turn order를 현재 AGI 기준으로 안정 정렬
→ 치료 시 original battle AGI 복원 후 같은 순서 갱신 규칙 적용
```

- 이미 행동을 끝낸 actor를 현재 라운드에서 다시 행동시키거나 건너뛰게 해서는 안 된다. 현재 `turnIndex`까지의 prefix는 고정하고 아직 행동하지 않은 suffix만 재정렬한다.
- 라운드 경계에서는 전체 turn order를 재정렬한다. AGI 동률은 전투 시작 시 시드 RNG로 결정된 기존 상대 순서를 안정 tie-break로 재사용해 추가 RNG 소비 없이 재현성을 유지한다.
- 기존 승인에 따라 근력·민첩 강장제와 능력 강화는 turn order를 재정렬하지 않는다. 순서 갱신 trigger는 신경독 적용·해제에 한정한다.
- 신경독 적용 실패 시 AGI와 bleed 모두 변경하지 않는다. 일반 적은 100%라 항상 성공하고 boss는 공통 저항 적용 후 50%다.

### 18.4 약점 노출 소유권과 만료

신규 상태는 다음 정보를 가져야 한다.

```text
ExposedState {
  sourceActorId
  bonusDamage
  sourceActionsRemaining
  appliedRound
}
```

- 적용 직후 `sourceActionsRemaining = 2`로 둔다. 스킬을 사용한 현재 행동 종료에서 1이 되고, 같은 궁수의 다음 행동 기회 종료에서 0이 되어 제거된다.
- `stun`, `paralysis`, `sleep`으로 다음 행동 기회를 건너뛰어도 그 차례의 종료이므로 만료한다.
- 궁수가 다음 행동 기회 전에 전투불능이 되면 즉시 제거하지 않는다. 사망한 라운드의 남은 행동 동안 유지하고 그 라운드가 끝나는 시점에 제거한다.
- 궁수가 출혈로 자신의 다음 차례 시작에 쓰러지면 행동을 완료하지 못한 사망으로 보고 해당 라운드 종료까지 유지한다.
- 같은 target에 다시 적용하면 기존 상태를 중첩하지 않고 최신 source·bonus로 교체한다. `head_shot` 사용 조건과 직접 피해 보정은 상태가 존재하는 동안 그대로 유지한다.
- target이 먼저 쓰러진 상태는 결과에 영향을 주지 않으며 전투 종료 초기화 경계에서 제거된다.

### 18.5 경제와 보스 수치의 단일 원본

- 실제 지급 골드는 `QuestDefinition.goldReward`를 단일 런타임 원본으로 사용한다.
- settlement summary, profile gold 증가, `GameResult.gold`, `REWARD_GRANTED` 메시지는 같은 quest definition 값을 전달받아야 한다.
- 승인 골드는 순서대로 훈련 폐허 1100, 고블린 소굴 1100, 유적지 1800, 지하 던전 2700, 옛 고성 4000, 화산 동굴 4000, 깊은 숲 폐허 4000이다.
- EXP, 장비 가격, 상점 등급, 반복 횟수와 스킬 가격은 이번 작업에서 변경하지 않는다.
- 후반 보스 변경은 enemy definition과 skill definition 양쪽에 적용한다. 일반 적·중간보스·리치, 피해 공식, boss 확률 절반 공통 규칙은 유지한다.

### 18.6 위험과 회귀 범위

- 행동 순서를 현재 라운드 전체에서 다시 정렬하면 중복 행동·행동 누락이 발생할 수 있으므로 prefix/suffix 경계를 자동 테스트해야 한다.
- 약점 노출을 target 행동 기준에서 source 행동 기준으로 바꾸면 `head_shot` 가능 시간이 길어질 수 있다. source 생존·사망·행동 건너뛰기·라운드 마지막 actor를 각각 검증해야 한다.
- 퀘스트 골드가 여러 settlement에 중복되어 있어 literal 일부만 교체하면 저장 골드와 결과 UI가 불일치한다. 단일 원본 전환과 7개 퀘스트 정산 테스트가 필요하다.
- 보스 피해 하향은 결정론적 전투 snapshot과 기존 수치 assertion을 의도적으로 변경한다. unrelated RNG·상태·전체 공격 대상 수 회귀는 유지해야 한다.
- 기존 profile의 보유 골드는 소급 변경하지 않는다. 변경 이후 완료한 퀘스트부터 신규 보상값을 지급하며 profile version 2를 유지한다.

## 19. 아이템·장비·스킬 대표 아이콘과 등급 색상 분석

### 19.1 분석 범위와 확인된 현재 상태

- 대상은 소비 아이템, 장비, 직업·커스텀 스킬을 표시하는 React UI와 해당 콘텐츠 정의, 에셋 관례다. 게임 판정, Phaser 전투 캐릭터·적·지형 렌더링과 저장 데이터는 변경 대상이 아니다.
- `Rarity`는 `common/uncommon/rare/heroic/legendary` 5단계이며 승인된 의미 색은 각각 흰색/녹색/파랑색/보라색/노랑색이다. 실제 등급 데이터는 `EquipmentData.rarity`에만 존재한다.
- 장비는 9개 `EquipmentFamily`와 45개 정의가 있고, 각 instance는 `equipmentId`만 저장한다. 아이템 stack과 스킬 instance도 각각 콘텐츠 ID만 저장한다.
- 아이템 정의에는 등급·아이콘·family가 없고, 스킬 정의에는 등급은 없지만 `activation: active | passive`가 있다. 존재하지 않는 아이템·스킬 등급을 가격이나 해금 시점으로 추론하면 현재 게임 규칙을 변경하게 된다.
- 상점, 창고, 캐릭터 장착·인벤토리, 탐사 아이템, 전투 명령, 결과·초과 보상은 모두 React DOM 텍스트로 표시된다. 아이콘 registry와 등급 CSS token은 없다.
- 현재 런타임 에셋은 `src/assets/{terrain,enemies,characters}`에만 있고 장비·아이템·스킬 아이콘은 `asset-plan.md`에서 Graphics/텍스트 fallback 대상으로 분류되어 있다.
- `CharacterPanel`의 직업 스킬과 `ResultScreen`의 신규 스킬·자동 보상은 여러 항목을 문자열로 합치므로 항목별 아이콘·색상을 적용하려면 개별 DOM 요소로 바꿔야 한다.

### 19.2 표시 책임과 데이터 흐름

```text
EquipmentInstance / ItemStack / SkillInstance / PendingRewardEntry
→ 콘텐츠 ID로 EQUIPMENT_DATA / ITEM_DATA / SKILLS 조회
→ UI 전용 presentation selector가 iconKey·rarity·표시명을 파생
→ React ContentIcon과 콘텐츠 행이 아이콘·색상·등급명을 표현
→ 이미지 누락·로드 실패 시 텍스트 대체 배지와 기존 표시명 유지
```

- 아이콘과 색상은 표시 정보이며 구매·장착·스킬 사용·보상 선택 판정에 참여하지 않는다.
- `EquipmentInstance`, `ItemStack`, `SkillInstance`, `PendingRewardEntry`에 icon이나 rarity를 중복 저장하지 않는다. 기존 profile version 2와 저장 검증을 그대로 유지한다.
- React UI용 아이콘을 Phaser texture registry에 넣지 않는다. DOM `<img>`와 소형 manifest를 사용하고 기존 Phaser loader 의존성을 추가하지 않는다.
- 콘텐츠 이름은 기존 `displayNames.ts`의 안전한 표시명을 유지한다. 아이콘은 이름을 대체하지 않고 보조한다.

### 19.3 대표 아이콘 계약

승인 요청 범위의 대표 아이콘 key는 다음 11개로 제한한다.

| 대상 | icon key | 파생 기준 | 의미 |
|---|---|---|---|
| 소비 아이템 | `item_potion` | 모든 `ItemId` | 개별 물체가 아닌 소비 아이템 카테고리 대표 |
| 검 | `equipment_sword` | `family=sword` | 한손검 계열 |
| 몽둥이 | `equipment_club` | `family=mace` | 둔기·철퇴·망치 계열 대표 |
| 단검 | `equipment_dagger` | `family=dagger` | 단검 계열 |
| 활 | `equipment_bow` | `family=bow` | 활 계열 |
| 지팡이 | `equipment_staff` | `family=staff | rod` | 지팡이·로드 계열 공용 대표 |
| 방패 | `equipment_shield` | `family=shield` | 보조 장비 방패 계열 |
| 투구 | `equipment_helmet` | `family=head` | 모자·두건·투구·왕관을 포함한 머리 장비 대표 |
| 갑옷 | `equipment_armor` | `family=body` | 몸통 장비 대표 |
| 액티브 스킬 | `skill_active` | `activation=active` | 직접 선택·발동하는 스킬 |
| 패시브 스킬 | `skill_passive` | `activation=passive` | 자동·상시 적용 스킬 |

- 포션 아이콘은 붕대·화염병·분필을 포함한 소비 아이템 전체의 카테고리 표식이다. 개별 아이템 외형을 정확히 나타내는 아이콘으로 해석하지 않는다.
- `rod`는 요청된 지팡이 대표 아이콘과 실루엣이 호환되므로 같은 key를 사용한다.
- `shield`는 정식 `equipment_shield`를 사용한다. 장비 방패는 큰 전면 방패 실루엣, 패시브 스킬은 작은 방패와 외곽 지속 오라로 구분한다.
- 알 수 없는 ID, 누락된 정의와 등록되지 않은 icon key도 같은 대체 배지를 사용하고 원본 ID를 사용자 화면에 노출하지 않는다.

### 19.4 에셋 표현 규칙

- 파일은 `src/assets/icons/<iconKey>.png`, 원본 생성기는 `assets-source/icons/generate_content_icons.mjs`를 사용한다.
- 각 아이콘은 투명 배경의 24×24px 단일 정적 픽셀 이미지, 중앙 anchor 관념, 1px 어두운 외곽선, 제한된 내부 팔레트를 사용한다.
- 아이콘 자체에 등급 색을 굽지 않는다. 동일 대표 아이콘을 모든 등급에서 재사용하고 React 프레임·이름·등급 배지가 rarity 색을 담당한다.
- 도상 기준은 포션 병, 대각 검, 두꺼운 둔기, 짧은 단검, 활과 시위, 오브가 없는 지팡이, 큰 전면형 장비 방패, 정면 투구, 흉갑, 폭발/발동 표식, 작은 방패와 지속 오라 표식이다. 글자와 수치는 이미지에 넣지 않는다.
- DOM 표시 크기는 기본 24px, 조밀한 결과·스킬 목록은 20px까지 축소할 수 있다. CSS `image-rendering: pixelated`를 적용하고 비정수 확대를 피한다.
- 11개 소형 에셋은 초기 UI에서 eager manifest로 읽어도 허용한다. 파일 누락 또는 브라우저 decode 실패는 항목별 대체 배지로 독립 fallback한다.

### 19.5 등급 색상과 접근성

| rarity | 한글 | 의미 색 | 임시 CSS 기준값 |
|---|---|---|---|
| `common` | 일반 | 흰색 | `#f4f4f4` |
| `uncommon` | 고급 | 녹색 | `#62c370` |
| `rare` | 희귀 | 파랑색 | `#5aa9ff` |
| `heroic` | 영웅 | 보라색 | `#b678f2` |
| `legendary` | 전설 | 노랑색 | `#ffd45a` |

- 정확한 HEX는 사용자에게 별도로 지정되지 않았으므로 위 값은 어두운 현행 배경에서 구분 가능한 `임시 결정`이다. 색 이름과 rarity 의미는 확정이며 후속 팔레트 조정은 CSS token만 교체한다.
- 장비의 아이콘 테두리, 이름, 목록 좌측 accent와 등급 배지에 같은 token을 사용한다. 배경 전체를 강한 색으로 채우지 않아 텍스트 대비와 disabled 상태 가독성을 보존한다.
- 아이템·스킬은 등급 데이터가 없으므로 `neutral` 회색 테두리·기존 본문색을 사용하며 `common`으로 표기하지 않는다. 스킬 active/passive는 아이콘과 텍스트로 구분한다.
- 색만으로 등급을 전달하지 않는다. 장비 metadata에 `일반/고급/희귀/영웅/전설`을 함께 표시하고 이미지에는 인접 텍스트가 있으므로 장식 이미지로 처리한다.
- 결과 화면의 `unlockedRarities`도 같은 색 token과 한글 등급 배지를 사용해 별도 ternary 색상을 만들지 않는다.

### 19.6 적용 화면과 구조 영향

| 화면 | 적용 내용 | 구조 변경 |
|---|---|---|
| `ShopPanel` | 장비·아이템·스킬 대표 아이콘, 장비 rarity 이름·색 | 각 상품 identity 영역 추가 |
| `StoragePanel` | 세 종류 보관 항목 아이콘, 장비 rarity 색 | 기존 instance ID와 동작 버튼 유지 |
| `CharacterPanel` | 직업/커스텀 스킬, 장착 장비, 개인 아이템 아이콘 | join 문자열을 스킬별 inline 요소로 변경 |
| `ExplorationItems` | 포션 대표 아이콘 | 대상 버튼과 아이템 이름 영역 분리 |
| `BattleCommands` | 액티브 스킬·포션 대표 아이콘 | 버튼 내부 icon+label 구조, 명령 흐름 유지 |
| `ResultScreen` | 신규 스킬, 자동·초과 보상, 등급 해금 표시 | join 문자열을 항목별 요소로 변경 |

- 전투 명령에는 현재 active 스킬만 나타나지만 공용 selector는 passive도 처리해 거점·결과 화면에서 사용한다.
- 모바일 가로 화면의 42~50px 행과 버튼 높이를 유지한다. 아이콘은 별도 열을 과도하게 차지하지 않고 이름과 metadata를 포함한 `content-identity` 내부에 배치한다.
- 보상 kind에 따라 장비 definition의 rarity, 아이템 대표, 스킬 activation을 조회한다. lookup 실패가 보상 선택 checkbox와 저장을 막아서는 안 된다.

### 19.7 충돌·위험·미확정 사항

- `equipment_shield`가 정식 범위에 포함되어 9개 장비 family가 모두 대표 아이콘으로 해석된다. 방패 파일 누락·decode 실패 때만 다른 장비와 동일하게 `장` 대체 배지를 사용한다.
- 포션 하나로 모든 소비 아이템을 표시하므로 붕대·화염병·분필의 개별 식별력은 이름에 의존한다. 이번 요청은 대표 아이콘 범위로 한정하고 개별 아이템 icon family는 추가하지 않는다.
- 아이템·스킬에 rarity를 부여하지 않으므로 5색은 장비와 장비 등급 해금에만 의미가 있다. 향후 아이템·스킬 등급이 승인되면 콘텐츠 정의부터 별도 설계해야 한다.
- 문자열 join을 개별 DOM으로 바꾸면서 문장 흐름과 모바일 높이가 증가할 수 있다. 640×360, 760px 이하 가로 화면과 결과 보상 다수·스킬 다수 상태를 확인해야 한다.
- disabled opacity, 노랑색과 기존 금색 UI, 흰색과 본문색이 가까워 보일 수 있다. 등급 배지 텍스트와 테두리를 함께 확인하며 색상 대비가 부족하면 CSS token만 조정한다.
- 아이콘은 표시 전용이므로 에셋 실패가 구매·장착·사용·보상 선택·저장 결과에 영향을 주면 안 된다.

## 20. 고정 동료 종족 데이터 반영 분석

### 20.1 기준과 적용 범위

- 사용자가 수동 추가한 `raw_data_table.md` 16절의 `race_id` 열을 최신 동료 종족 기준으로 사용한다.
- 적용값은 브람 `dwarf`, 세라 `human`, 로웬 `elf`다. 직업·배치·이름과 캐릭터 ID는 기존 값을 유지한다.
- 이번 요청은 동료 **종족 데이터** 반영이므로 같은 원시 표의 고정 스킬·고정 장비·계약 열은 기존 Lv1·2·5 직업 스킬 및 시작 무기 규칙을 대체하지 않는다.
- 원시 표의 상태는 `current`지만 현재 코드가 브람·세라·로웬을 모두 `human`으로 생성하므로 문서 주장과 코드 상태를 구분한다. 사용자 지시로 race 값은 후속 구현 기준이 되었고 코드는 아직 미반영 상태다.

### 20.2 현재 코드와 목표값

| characterId | 이름 | 현재 코드 race | 목표 race | 직업 | 성별·배치 유지 |
|---|---|---|---|---|---|
| `party_warrior` | 브람 | `human` | `dwarf` | `warrior` | 남성·전열·2P |
| `party_priest` | 세라 | `human` | `human` | `priest` | 여성·후열·3P |
| `party_archer` | 로웬 | `human` | `elf` | `archer` | 남성·후열·4P |

- `src/game/content.ts`의 `createInitialCharacters`와 `src/ui/SetupScreen.tsx`의 미리보기 값이 각각 하드코딩되어 있어 두 위치를 따로 수정하면 불일치가 재발한다.
- 동료 identity는 `content.ts`의 단일 `COMPANION_DATA`에서 관리하고 프로필 생성과 준비 화면이 같은 정의를 읽는다.
- 성별 값은 원시 표에 없고 이번 요청 범위에도 포함되지 않으므로 현재 `남성/여성/남성` 값을 유지한다. 이는 종족 승인과 별개의 기존 임시값이다.

### 20.3 능력치·인벤토리·전투 영향

종족은 표시 메타데이터가 아니라 다음 판정 흐름의 입력이다.

```text
COMPANION_DATA.raceId
→ ProfileV2.characters[].raceId
→ RACE_DATA + CLASS_DATA + 성장 + 장비
→ 최종 STR/DEX/INT/CON/AGI/LUK
→ HP/ATK/DEF/전투 AGI와 개인 인벤토리 용량
→ Actor.raceId와 캐릭터 texture 선택
```

현재 Lv1 시작 무기를 포함한 목표 수치는 다음과 같다.

| 동료 | 최종 STR/DEX/INT/CON/AGI/LUK | HP | ATK | DEF | 전투 AGI |
|---|---|---:|---:|---:|---:|
| 브람·드워프 전사·`common_sword` | `12/5/3/9/4/5` | 30 | 6 | 4 | 3 |
| 세라·인간 사제·`common_staff` | `5/7/10/5/6/5` | 22 | 3 | 3 | 4 |
| 로웬·엘프 궁수·`common_bow` | `3/11/6/4/10/4` | 20 | 5 | 2 | 6 |

- 로웬의 Lv1 STR은 장비 포함 3으로 개인 인벤토리 용량이 10칸이다. 기존 인간 로웬은 STR 5로 11칸이므로 기존 프로필을 무조건 변환하면 초과 stack이 생길 수 있다.
- 로웬의 전투 AGI가 5에서 6으로 바뀌어 기존 turn order와 동률 seed 기대값이 변경될 수 있다.
- 준비 화면은 현재 시작 무기 modifier를 제외해 실제 Actor와 이미 다른 값을 표시한다. 종족 반영 시 `createInitialCharacters`와 `getFinalAttributes` 결과를 재사용해 시작 장비까지 포함한 동일값을 표시한다.

### 20.4 저장 호환성 결정

- `PersistentCharacter.raceId`는 이미 profile version 2에 저장되고 save validator는 네 종족을 모두 허용한다. schema와 envelope version은 변경하지 않는다.
- 신규 프로필은 즉시 브람 `dwarf`, 세라 `human`, 로웬 `elf`로 생성한다.
- 기존 profile은 저장된 동료 race를 사용자 진행 상태로 보존하고 자동 변환하지 않는다. 이는 로웬 인벤토리 용량 축소와 저장 무효화를 피하는 비파괴 기본값이다.
- 기존 profile에서 신규 종족을 적용하려면 사용자가 프로필을 초기화해 새로 생성한다. 기존 진행을 유지한 강제 변환·overflow 이관은 별도 migration 승인 없이는 추가하지 않는다.
- 이 정책에 따라 생성 시점이 다른 profile은 동료 종족이 다를 수 있으나, 각 profile의 race 값으로 능력치와 에셋을 일관되게 계산한다.

### 20.5 캐릭터 에셋 영향

- 기존 조합형 캐릭터 에셋 registry는 `(raceId, classId, gender, slot)`을 사용하므로 로더·texture key 계약을 변경하지 않는다.
- 목표 파일 `dwarf_warrior_male_p2.png`, `human_priest_female_p3.png`, `elf_archer_male_p4.png`가 모두 존재한다.
- 신규 캐릭터 PNG 생성 없이 profile의 race 값만으로 해당 파일이 지연 로드된다.
- 동료 종족을 바꿔도 1P~4P 색상 띠, fallback geometry, gender 변환과 적·terrain registry에는 영향이 없다.

### 20.6 충돌·위험·미확정 사항

- `raw_data_table.md` 16절의 세라 고정 스킬 `basic_attack,smite`는 승인된 사제 Lv1 `heal`, Lv2 `smite` 규칙과 충돌한다. 이번 반영은 race 열로 한정하고 스킬 진행 규칙은 변경하지 않는다.
- 같은 표의 고정 장비 `N/A`는 현재 공통 시작 무기 자동 장착과 의미가 다를 수 있다. 이번 반영에서는 기존 시작 무기를 유지한다.
- 동료 성별은 현재 코드값을 유지하지만 원시 표에 근거가 없어 최종 설정으로 확정된 것은 아니다.
- 신규 종족으로 AGI·STR이 바뀌므로 turn order, 최종 능력치, 인벤토리 용량, 준비 화면과 스프라이트 key를 집중 회귀해야 한다.
- 기존 profile 자동 변환은 제외했다. 향후 모든 profile의 동료 identity를 강제 통일하려면 overflow를 포함한 별도 migration 설계가 필요하다.

## 21. 퀘스트 결과 화면 모바일 세로 overflow 분석

### 21.1 현상과 분석 근거

- 사용자 확인 현상은 모바일 가로 화면에서 퀘스트 성공·실패 결과 정보가 viewport 높이를 넘어가고, 맨 아래의 확인 동작에 접근할 수 없어 거점 복귀 또는 보상 확정을 진행하지 못하는 것이다.
- `src/ui/ResultScreen.tsx`는 제목, 골드·EXP, 4인 성장·스킬·슬롯 해금, 퀘스트·등급 해금, 자동 보상 또는 창고 초과 보상 목록을 하나의 `main.menu-screen.result-screen` 세로 흐름에 모두 배치한다.
- 일반 결과의 `거점으로`와 overflow 결과의 `선택 보관·나머지 포기` 버튼도 정보 영역의 마지막 자식이다. 따라서 앞선 정보 높이가 증가하면 두 버튼이 함께 viewport 아래로 밀린다.
- `src/styles.css`의 `body`는 게임 화면 스크롤을 막기 위해 `overflow: hidden`이고, `.menu-screen`은 `min-height: 100dvh`, 세로 중앙 정렬을 사용한다. 가로 방향의 `.result-screen`에는 별도 높이 제한·스크롤 영역이 없다.
- `.menu-screen { overflow: auto; }`는 portrait media query에만 존재하며 portrait에서는 회전 안내가 결과 화면을 덮는다. 실제 지원 방향인 landscape에서는 넘친 결과를 body 또는 결과 화면 어디에서도 터치 스크롤할 수 없다.
- `architecture.md` 19.7절에서 아이콘·개별 보상 DOM 전환이 모바일 결과 높이를 증가시킬 위험을 이미 기록했으나, 단계 10 브라우저 검증은 프로필 생성→거점→탐사까지였고 최대 정보량의 결과 화면은 확인하지 않았다.

### 21.2 원인 경계

이 결함은 정산 데이터나 화면 전환 명령의 문제가 아니라 React 결과 문서 구조와 CSS overflow 소유권의 문제다.

```text
GameState.result / profile.pendingReward
→ ResultScreen이 모든 결과 정보를 세로 렌더링
→ body overflow:hidden + landscape 결과 내부 scroll 부재
→ 마지막 action이 viewport 밖으로 이동
→ RETURN_TO_HUB 또는 CONFIRM_REWARD_SELECTION을 터치할 수 없음
```

- `RETURN_TO_HUB`는 결과 화면이며 `pendingReward`가 없을 때만 허용되고, `CONFIRM_REWARD_SELECTION`은 pending reward를 확정한다. 두 command guard는 정상이며 변경 대상이 아니다.
- 결과 내용, 성장·보상 정산 순서, 선택값과 저장 처리는 유지한다. UI가 정산 결과를 축약하거나 일부 항목을 숨기는 방식으로 해결하지 않는다.
- 전역 `body` 또는 모든 `.menu-screen`에 스크롤을 허용하면 탐사·전투의 제스처 차단과 시작·거점 화면 레이아웃에 영향을 줄 수 있으므로 결과 화면만 overflow를 소유해야 한다.

### 21.3 목표 구조와 동작

결과 화면을 viewport 안의 두 영역으로 분리한다.

```text
result-screen: 100dvh, overflow hidden
├─ result-scroll-region: 남은 높이, touch 세로 스크롤
│  └─ result-content
│     ├─ 성공/실패 제목과 설명
│     ├─ 골드·EXP·합계
│     ├─ 성장·해금
│     └─ 자동 보상 또는 overflow 선택 목록
└─ result-actions: 고정 비스크롤 영역 + bottom safe area
   └─ pending이면 보상 확정 / 아니면 거점으로
```

- 정보가 짧으면 `result-content`를 기존처럼 화면 중심에 배치하고, 길면 콘텐츠 높이만큼 자연스럽게 확장해 처음부터 끝까지 스크롤한다.
- action 영역은 scroll region의 형제가 되어 정보 스크롤 위치와 무관하게 항상 viewport 안에 남는다. `position: fixed`로 전역 좌표를 사용하는 대신 결과 화면 flex 레이아웃의 비축소 footer로 둔다.
- pending reward 상태에서는 `선택 보관·나머지 포기`를 footer에 표시한다. 확정 후 `pendingReward`가 사라지면 같은 자리의 action이 `거점으로`로 바뀌는 기존 2단계 흐름을 유지한다.
- pending이 없는 성공과 실패 결과는 모두 `거점으로`를 표시한다. 버튼은 기존 command payload와 최소 44px 터치 높이를 유지한다.
- 결과 정보의 시각적 순서인 성공/실패 → 골드/EXP → 성장·해금 → 보상/선택은 변경하지 않는다.

### 21.4 모바일 레이아웃·입력 제약

- `.result-screen`은 `height/max-height: 100dvh`, `min-height: 0`, `overflow: hidden`, `justify-content: flex-start`로 viewport 경계를 확정한다.
- scroll region은 `min-height: 0`, `flex: 1`, `overflow-y: auto`, `overscroll-behavior: contain`, `-webkit-overflow-scrolling: touch`, `touch-action: pan-y`를 사용해 결과 정보에서만 네이티브 터치 스크롤을 허용한다.
- action footer는 `flex: 0 0 auto`로 축소되지 않아야 하며 결과 정보 폭과 맞추고 버튼을 가용 폭 전체에 배치한다. 하단 safe area는 기존 `.menu-screen` padding 안에 포함한다.
- 현재 `max-height: 500px` media query가 `.menu-screen`의 상하 padding을 10px로 덮으므로 result 전용 규칙에서 `env(safe-area-inset-top/bottom)`을 다시 보존한다.
- 낮은 landscape에서는 제목, reward card margin·padding을 줄일 수 있으나 텍스트·보상 항목을 제거하지 않는다. 공간 최적화와 무관하게 scroll+footer 구조가 접근 가능성을 보장해야 한다.
- portrait 회전 안내, `body`의 전역 스크롤 차단, 게임 화면의 확대·선택 방지 정책은 유지한다.

### 21.5 위험과 검증 공백

- scroll 영역과 footer에 `min-height: 0`·비축소 설정이 빠지면 flex item 기본 최소 높이 때문에 main이 다시 viewport보다 커질 수 있다.
- action을 scroll 영역 안에서 `sticky`로만 처리하면 부모 overflow·Safari 동작에 의존하므로 형제 footer 구조보다 회귀 위험이 높다.
- overflow 선택 목록을 스크롤 영역 밖으로 옮기면 checkbox는 보이지만 항목을 읽지 못하거나 footer와 겹칠 수 있다. 선택 목록은 정보 scroll 영역에 유지하고 확정 버튼만 분리한다.
- 긴 결과의 기준은 4인 성장 결과, 신규 스킬·커스텀 슬롯, 퀘스트·등급 해금과 다수 보상이 동시에 있는 성공 상태다. 일반 성공만 검증하면 재발 가능성이 있다.
- 실제 Android Chrome과 iOS Safari의 주소창 변화·safe area·관성 스크롤은 문서·Node 테스트만으로 확정할 수 없어 모바일 실기 또는 동일 엔진의 responsive browser 검증이 필요하다.

## 22. Android Chrome 전체 화면 토글 분석

### 22.1 목적과 현재 경계

- 사용자 확인 기준으로 PC와 iPhone Safari는 현재 화면 사용에 문제가 없고, Android phone Chrome의 landscape에서 URL 입력창·브라우저 UI가 가용 높이를 줄여 콘텐츠 일부를 가리는 현상이 개선 대상이다.
- 현재 CSS는 menu·game·result shell에 `100dvh`를 사용해 브라우저가 보고한 동적 viewport 안에서 배치하지만 브라우저 UI 자체를 애플리케이션이 CSS로 제거하지는 못한다.
- 현재 코드에는 Fullscreen API 사용, 전체 화면 상태, 토글 UI와 오류 처리가 없다. Phaser는 내부 논리 해상도 `640×360`과 `Phaser.Scale.FIT`을 사용하므로 viewport가 커지면 게임 판정을 변경하지 않고 canvas 표시 크기만 다시 맞출 수 있다.
- 목표는 사용자의 명시적인 터치로 웹 문서 전체를 fullscreen으로 전환해 Android Chrome의 주소창·탐색 UI 영역을 게임에 할당하고, 같은 UI로 일반 화면으로 복귀하는 것이다.

### 22.2 Web API 근거와 제약

MDN Fullscreen API와 `requestFullscreen()` 기준:

- `document.documentElement.requestFullscreen({ navigationUI: 'hide' })`는 루트 HTML과 모든 React·Phaser 자식을 전체 화면 대상으로 요청한다.
- `document.exitFullscreen()`은 일반 화면 복귀를 요청한다.
- 실제 상태는 Promise 성공 추정이 아니라 `document.fullscreenElement`와 `fullscreenchange` 이벤트로 동기화한다. Android back, 시스템 제스처, 탭·앱 전환 등 외부 종료도 같은 이벤트로 반영한다.
- 진입에는 transient user activation이 필요하므로 버튼의 직접 `click` 처리 안에서 요청해야 한다. 최초 접속, 화면 전환 또는 저장 복원 시 자동 진입하지 않는다.
- `document.fullscreenEnabled`가 false이거나 표준 method가 없으면 기능을 사용할 수 없다. Fullscreen API는 브라우저 전체에서 Baseline이 아니므로 미지원 환경에서는 무동작 버튼을 남기지 않는다.
- top-level same-origin 문서는 기본 Permissions Policy `fullscreen=(self)` 범위지만 iframe 배포라면 상위 frame의 `allow="fullscreen"`이 별도로 필요하다. 현재 `/pn/` 직접 배포는 top-level 문서를 기준으로 한다.
- 참고 근거: `https://developer.mozilla.org/en-US/docs/Web/API/Fullscreen_API`, `https://developer.mozilla.org/en-US/docs/Web/API/Element/requestFullscreen`, `https://developer.mozilla.org/en-US/docs/Web/API/Document/fullscreenchange_event`.

### 22.3 오른쪽 하단 고정 배치 충돌 분석과 결정

요청된 전역 오른쪽 하단 fixed 배치는 실제 플레이 UI와 충돌하므로 사용자가 허용한 대체 배치를 적용한다.

| 화면 | 오른쪽 하단의 현재 책임 | 전역 fixed 버튼 위험 | 결정 위치 |
|---|---|---|---|
| 시작 | 이어하기·초기화 action 영역과 가까움 | 낮은 landscape에서 action 가림 | `menu-actions` 안의 우측 정렬 도구 |
| 프로필 생성 | `프로필 생성` 버튼 | 필수 생성 action 터치 방해 | `setup-header` 우측 도구 그룹 |
| 거점 | panel action과 우측 하단 `프로필 초기화` | 목록·관리 버튼 또는 초기화와 중첩 | `hub-header` 우측 도구 그룹 |
| 탐사·전투 | 우측 HUD 하단의 스킬·대상·리롤·아이템 명령 | 핵심 전투·탐사 입력 직접 차단 | `game-stage` 내부 우측 상단 overlay |
| 결과 | 하단 고정 `거점으로`/보상 확정 footer | 다음 단계 action 재차 차단 | `result-actions`의 우측 보조 버튼 |

- 탐사 Phaser의 하단에는 좌회전·후진·전진·우회전 버튼이 있고 우측 HUD 하단에는 React 명령이 있으므로 gameplay 중 오른쪽 하단은 사용할 수 없다.
- game-stage 우측 상단은 현재 탐사 위치 표시(좌측 상단), 중앙 map/조우 제목, 하단 이동 버튼과 겹치지 않는 최소 충돌 위치다. Phaser canvas 위에 React 버튼을 두되 클릭이 canvas로 전달되지 않게 독립 touch target으로 처리한다.
- 메뉴·거점·결과에서는 기존 flow 안에 배치해 내용이나 action을 덮지 않는다. 화면별 물리 위치는 달라지지만 모든 화면에서 동일 아이콘·label·동작을 제공한다.
- 버튼은 최소 `44×44px`, safe area 안쪽, 다른 필수 action보다 높은 시각 우선순위를 갖지 않는 중립 색상으로 한다.

### 22.4 컴포넌트와 상태 흐름

Fullscreen 상태는 game engine·store가 아니라 브라우저 UI adapter인 React `FullscreenToggle`이 소유한다.

```text
사용자 터치
→ document.fullscreenElement 확인
├─ null: documentElement.requestFullscreen({ navigationUI:'hide' })
└─ value: document.exitFullscreen()
→ fullscreenchange
→ 실제 isFullscreen 갱신
→ enter/exit 아이콘·aria-label 동기화
```

- `supported`, `isFullscreen`, `transitioning`, `error`만 component local state로 둔다.
- profile·session·localStorage에는 fullscreen 상태를 저장하지 않는다. 새로고침 후 일반 화면에서 시작하고 사용자가 다시 선택한다.
- screen 전환으로 toggle component가 재마운트되더라도 초기값을 `document.fullscreenElement`에서 읽어 fullscreen 상태를 이어 표시한다.
- 요청 중에는 중복 터치를 막는다. Promise rejection과 `fullscreenerror`는 처리해 unhandled rejection을 만들지 않고 버튼을 재활성화한다.
- 화면 방향 강제 고정은 이번 범위에서 제외한다. 기존 portrait 회전 안내와 사용자의 물리적 landscape 전환을 유지한다.

### 22.5 viewport·Phaser·입력 영향

- fullscreen 진입·종료 시 `100dvh`, menu/result 높이와 game-shell이 새 viewport로 재계산된다.
- `.game-stage`의 aspect ratio와 Phaser `FIT`·CSS 100% canvas 계약을 유지한다. Fullscreen 전환을 이유로 Phaser instance, scene, game state를 재생성하지 않는다.
- game-stage overlay 버튼은 `z-index`와 `pointer-events`를 갖고 canvas의 `pointerdown`으로 이벤트가 전달되지 않아야 한다. 이동·전투 명령과 fullscreen 토글이 동시에 발생하면 실패다.
- portrait에서는 회전 안내가 최우선이므로 fullscreen toggle을 숨긴다. landscape 복귀 후 지원 환경이면 다시 표시한다.
- fullscreen 중에도 safe-area inset을 유지하고, 버튼은 Android back이나 브라우저 강제 종료 후 즉시 일반 화면 아이콘으로 돌아와야 한다.

### 22.6 위험·미확정 사항

- `navigationUI: 'hide'`는 브라우저에 전달하는 요청 옵션이며 최종 표시 정책은 user agent가 통제한다. Android Chrome 실기에서 URL 입력창 제거와 실제 viewport 증가를 확인해야 한다.
- 전체 화면 전환은 사용자의 직접 터치 없이는 거부된다. 자동 재시도·자동 전체 화면은 추가하지 않는다.
- 미지원 iPhone Safari 등에서 버튼을 숨기므로 기존 정상 흐름은 유지되지만, 브라우저별 지원 여부는 `fullscreenEnabled`로 런타임 판정해야 한다.
- fullscreen 전환 중 화면 회전·앱 전환·빠른 연속 터치가 겹치면 Promise rejection이나 외부 종료가 발생할 수 있으므로 event 기반 상태 동기화가 필수다.
- game-stage 우측 상단이 향후 Phaser 정보·명령 위치로 사용되면 별도 slot 재검토가 필요하다. 현재 코드 기준으로는 오른쪽 하단보다 gameplay 영향이 작다.

## 23. 결과·창고·캐릭터 정보 위계 개선 분석

### 23.1 요청 범위와 현재 상태

이번 변경은 결과·거점 React UI의 표시 문구와 시각적 정보 위계에 한정한다. 정산, 장착 가능 판정, 장비·스킬 instance, command와 저장 데이터는 변경하지 않는다.

현재 코드 근거:

- `ResultScreen`은 `summary.unlockedRarities`를 `신규 장비 등급:` label 뒤의 rarity badge 목록으로 표시한다.
- `StoragePanel`의 스킬 행은 스킬 이름 아래에 내부 식별자인 `instance.skillInstanceId`와 현재 선택 캐릭터 기준 `장착 가능/직업 제한`을 표시한다.
- `CharacterPanel`의 커스텀 스킬 행은 `커스텀 N`을 `<b>`, 장착 스킬 이름을 `<small>`에 배치해 슬롯 번호가 이름보다 크게 보인다.
- 같은 화면의 장비 행은 장비 슬롯명을 `<b>`, 장착 장비 이름·rarity를 `<small>`에 배치해 슬롯명이 실제 장비보다 크게 보인다.
- 공통 `.management-list small`은 10px이고 `<b>`에는 별도 축소 규칙이 없어 현재 역전된 크기 관계가 DOM element 선택에 의해 발생한다.

### 23.2 결과 화면 상점 입하 알림

- `unlockedRarities`의 의미는 단순 enum 해금보다 해당 등급 장비가 상점에 새로 제공된다는 사용자 행동 변화다.
- 각 해금 rarity를 다음 완전한 문장으로 표시한다.

```text
상점에 신규 [고급]등급 장비가 입하 되었습니다.
```

- 대괄호 안은 `getRarityDisplayName(rarity)`의 `일반/고급/희귀/영웅/전설`을 사용한다. 사용자가 제시한 문구에 따라 대괄호와 `입하 되었습니다.` 띄어쓰기를 그대로 사용한다.
- 배열에 복수 rarity가 있으면 하나를 누락하거나 합치지 않고 rarity마다 문장 한 줄을 렌더링한다.
- rarity 색상 token은 대괄호 안 등급명에 유지하되 기존 `신규 장비 등급:` label은 제거한다.
- 이 변경은 결과 요약의 `unlockedRarities` 값, 상점 해금 판정과 저장 상태를 변경하지 않는다.

### 23.3 창고 스킬 장착 직업군 표시

`CUSTOM_SKILL_ALLOWED_CLASSES`가 표시의 단일 기준이다.

```text
skillId
→ CUSTOM_SKILL_ALLOWED_CLASSES[skillId]
→ CLASS_DATA[classId].name
→ 공용 또는 직업명 목록
```

- 허용 목록이 `warrior/rogue/archer/paladin/priest/mage` 여섯 직업 전체를 포함하면 `공용`으로 표시한다. 현재 데이터에는 문자열 sentinel `all`이 없으므로 **전체 ClassId 집합 포함 여부**가 all 의미다.
- 전체가 아니면 원본 배열 순서대로 `전사 · 도적` 형식의 한글 직업명을 표시한다. 현재 전용 스킬은 대부분 한 직업이지만 다중 직업 데이터도 처리한다.
- unknown skill 또는 허용 목록 누락은 내부 ID를 노출하지 않고 `장착 직업 정보 없음`으로 표시한다.
- 스킬 행 metadata는 `장착 직업: 공용 · 장착 가능` 또는 `장착 직업: 전사 · 직업 제한` 형식으로 한다. 현재 선택 캐릭터의 compatibility 안내는 유지한다.
- `instance.skillInstanceId`는 command key와 저장 identity로 계속 사용하지만 사용자에게 표시하지 않는다.
- 장착 판정 자체는 `inventory.ts`와 같은 `CUSTOM_SKILL_ALLOWED_CLASSES`를 사용하므로 UI helper가 별도 허용 규칙을 만들면 안 된다.

### 23.4 캐릭터 커스텀 스킬 정보 위계

각 custom slot 행의 정보 우선순위를 다음처럼 바꾼다.

```text
작은 보조 label: 커스텀 1
큰 주요 identity: [아이콘] 응급 치료
우측 action: 해제
```

- `커스텀 N`은 9~10px monospace 보조 label로 표시한다.
- 장착된 스킬 이름은 12~13px, 본문 대비가 높은 굵은 글씨로 표시하고 기존 active/passive 아이콘을 유지한다.
- 빈 슬롯의 `비어 있음`, 잠긴 슬롯의 `LvN 잠금`은 스킬 이름이 아니므로 큰 강조를 사용하지 않고 muted 보조 상태로 유지한다.
- slot index, unlock level `[3,7,10]`, instance와 `UNEQUIP_CUSTOM_SKILL` payload는 변경하지 않는다.

### 23.5 캐릭터 장비 정보 위계

각 장비 slot 행의 정보 우선순위를 다음처럼 바꾼다.

```text
작은 보조 label: 무기
큰 주요 identity: [아이콘] 철제 한손검 [일반]
우측 action: 해제
```

- `무기/보조 장비/머리/몸통` slot명은 9~10px monospace 보조 label로 표시한다.
- 장착 장비 이름은 12~13px 굵은 주요 text로 올리고 아이콘·rarity 색·한글 badge를 같은 줄에 유지한다.
- 빈 slot은 `비어 있음` muted 상태를 유지하며 존재하지 않는 장비 이름처럼 강조하지 않는다.
- slot 순서, 장비 instance, rarity, `UNEQUIP_ITEM` command는 변경하지 않는다.

### 23.6 공통 CSS 경계와 위험

- 공통 `.management-list small`과 모든 `.content-copy b`를 뒤집으면 창고·상점·개인 아이템까지 변하므로 `character-loadout-label`, `character-loadout-name`, `character-loadout-empty`처럼 캐릭터 장착 행 전용 class를 사용한다.
- 큰 스킬·장비 이름은 `overflow-wrap:anywhere`, `min-width:0`을 유지하고 action 버튼을 축소하지 않는다.
- `management-list compact`의 2열 구조에서 이름·badge·해제 버튼이 겹칠 수 있으므로 640×360과 760px 이하 landscape에서 긴 이름을 확인한다.
- 결과 화면은 최근 내부 scroll 구조를 사용하므로 복수 입하 문장이 action footer를 밀지는 않지만 scroll 높이와 문장 줄바꿈을 확인해야 한다.
- 표시 helper는 UI presentation 계층에 두고 game engine·profile에 `공용` 문자열이나 직업 표시명을 저장하지 않는다.

## 24. 전투 결과 확인 텀·적 공격 예고·상태 결과 로그 분석

### 24.1 분석 범위와 확인 근거

- 대상은 전투 command 이후의 순수 엔진 전이, store event 전달, Phaser 전투 연출, React HUD·명령·로그와 전투 종료 화면 전환이다.
- 기준 코드는 `src/game/{types,combat,gameEngine}.ts`, `src/app/{gameStore,App}.tsx/ts`, `src/phaser/{PhaserGame,BattleScene}.ts`, `src/ui/{GameHud,BattleCommands}.tsx`, `src/styles.css`와 관련 combat·engine·store 테스트다.
- 기존 `implements.md` 24절은 모든 `ROLL_RESOLVED`를 판정과 독립된 Phaser queue에서 1초 표시하고 애니메이션 완료를 기다리지 않고 상태·승패를 확정하도록 정한다. 이번 변경도 이 원칙을 유지해야 한다.
- 사용자 요청의 “던전 이동 화면”은 실제 코드에서 두 목적지로 나뉜다. 일반 조우 승리는 `exploration`, 퀘스트 완료 조우 승리는 정산 후 `result`로 즉시 전환된다.

### 24.2 현재 동기 전투 흐름과 인지 문제

리롤 없는 플레이어 공격 한 번은 현재 다음 호출을 같은 dispatch 안에서 끝낸다.

```text
SELECT_TARGET
→ beginRoll
→ resolvePending
→ 피해·회복·상태·쿨다운·승패 확정
→ stepTurn
→ advanceToPlayer
   → 적 대상·스킬 선택과 공격 즉시 확정
   → 다음 적 공격 즉시 확정
   → 다음 플레이어 TURN_STARTED
→ 최종 GameState와 전체 GameEvent[]를 subscriber에 한 번 전달
```

- `combat.advanceToPlayer`는 적 actor를 만나면 `beginRoll`을 즉시 호출하며, 적이 여러 명이어도 다음 플레이어 차례까지 한 reducer 호출에서 모두 처리한다.
- `TURN_STARTED`는 현재 플레이어 차례에만 생성되고 actor ID도 없어 적 공격 시작 전 1초 예고를 구조적으로 식별할 수 없다.
- `BattleCommands`의 `적 행동 중` 분기는 존재하지만 React가 관측하는 state는 이미 다음 플레이어 차례인 경우가 대부분이라 실제 인지용 텀으로 기능하지 않는다.
- `BattleScene`은 한 event batch의 `ROLL_RESOLVED`를 순서대로 1초씩 보여 주지만 actor HP와 HUD는 subscriber가 받은 최종 state로 먼저 바뀐다. 현재 queue는 판정 지연이 아니라 이미 확정된 결과의 후행 표현이다.

### 24.3 마지막 적 사망과 Scene 수명

- 마지막 적 HP가 0이면 `resolvePending`, 적 차례 시작 출혈 처리 또는 전투 아이템 처리에서 즉시 `BATTLE_WON`이 생성된다.
- `gameEngine.applyCombatUpdate`는 같은 dispatch 안에서 일반 조우의 `combat`을 제거하고 `exploration`으로 전환하거나, 완료 조우를 정산해 `session`을 제거하고 `result`로 전환한다.
- `App`은 `state.screen`으로 Phaser를 keying하고 결과 화면에서는 Phaser 자체를 렌더링하지 않는다. 따라서 마지막 굴림 queue와 timer는 Scene shutdown 때 폐기된다.
- 단순히 `BattleScene`의 1000ms를 2000ms로 바꾸면 Scene이 먼저 파괴되므로 요청을 해결하지 못한다.
- 화염병 또는 출혈 tick으로 마지막 적이 쓰러지는 경로에는 terminal `ROLL_RESOLVED`가 없으므로 “마지막 다이스”를 임의 재사용해서도 안 된다. 이 경우 최종 쓰러짐 상태·전투 승리 문구·로그를 2초 동안 유지하는 fallback이 필요하다.

### 24.4 상태 이상 성공·실패 로그의 현재 상태

현재 확률 상태 적용 대상은 플레이어와 적 양쪽에 걸쳐 있다.

| 상태 | 대표 스킬 |
|---|---|
| 기절 | `power_strike`, `ogre_smash`, `minotaur_gore`, `crushing_blow` |
| 출혈 | `quick_stab`, `rending_bite`, `neurotoxin`의 부가 출혈 |
| 마비 | `lightning_bolt`, `paralyzing_claw` |
| 신경독 | `neurotoxin` |
| 수면 | `sleep` |
| 약점 노출 | `find_leak` |
| 도발 | `taunt` |

- `applyStatus`는 성공 시 `STATUS_APPLIED`와 “적용했다” 메시지를 만들지만 `statusId`가 없어 UI·테스트가 메시지 없이 상태 종류를 판별할 수 없다.
- `chanceBySkill`의 실패 branch는 이벤트를 생성하지 않아 사용자는 저항·부여 실패와 처리 누락을 구분할 수 없다.
- `find_leak`은 약점 노출 state를 확정 적용하지만 `STATUS_APPLIED`를 만들지 않는다.
- `taunt`는 대상마다 확정 적용 event가 있고 다음 공격 대상 전환 성공·실패는 별도의 `TAUNT_TARGET_RESOLVED`로 기록된다. 도발 부여 성공과 후속 대상 전환 판정을 혼합하면 안 된다.
- `neurotoxin` 성공은 신경독과 부가 출혈을 각각 적용한다. 주 상태 판정 실패 시 두 상태 모두 적용하지 않고 실패 event는 신경독 판정 한 건으로 남겨야 한다. 성공 시에는 실제로 적용된 신경독·출혈 두 상태를 각각 구조화해 현재 동작을 설명한다.
- 공격으로 이미 쓰러진 대상에는 상태 적용 판정을 하지 않는 현재 조건을 유지하므로 성공·실패 로그도 만들지 않는다.
- buff, 상태 제거와 cooldown은 이번 “디버프 부여 성공/실패” 범위가 아니며 기존 event를 유지한다.

### 24.5 로그 색상 정보 유실

- live `GameEvent`에는 `DAMAGE_APPLIED`, `HEAL_APPLIED`, `TRAP_TRIGGERED` 같은 type이 있으나 `appendEvents`가 `event.message`만 `string[]`에 저장한다.
- `GameHud`는 문자열만 받으므로 메시지의 `피해`, `회복` 단어를 파싱하지 않는 한 색을 결정할 수 없다. 문자열 파싱은 기존 구조화 event 원칙과 문구 변경 안정성을 위반한다.
- `.log-box p:last-child`는 마지막 로그를 공통 밝은색으로 덮으므로 damage·heal 전용 색을 추가할 때 selector 우선순위도 함께 정리해야 한다.
- 세션 로그는 profile v2에 저장되지 않는 원정 메모리 상태이므로 구조화 entry로 바꾸어도 profile migration은 필요하지 않다.

### 24.6 목표 책임 분리

게임 판정과 사용자에게 보이는 진행 시간을 다음처럼 분리한다.

```text
사용자 command
→ 순수 reducer가 피해·RNG·턴·승패·정산과 canonical screen을 즉시 확정
→ DispatchEnvelope { sequence, previousState, state, events, terminalBattleFrame? }
→ 전투 presentation plan
   ├─ 실제 공격하는 적의 TURN_STARTED 앞 1000ms 예고
   ├─ 기존 ROLL_RESOLVED 1000ms 표시
   ├─ BATTLE_WON terminal battle 최소 2000ms 유지
   └─ plan 재생 중 전투 command 입력 잠금
→ plan 종료 후 이미 확정된 canonical screen/state를 표시
```

- engine에는 `setTimeout`을 넣지 않고 피해·RNG·AI·정산을 지연하지 않는다.
- 애니메이션 callback이 `BATTLE_WON`, 보상 지급 또는 screen destination을 결정하지 않는다. canonical state와 profile 저장은 dispatch 시점에 이미 완료한다.
- terminal battle frame은 저장하지 않는 읽기 전용 표시 snapshot이다. 마지막 `CombatState`와 해당 event가 반영된 구조화 로그만 보존하며 profile·session 원본으로 되돌려 쓰지 않는다.
- 일반 조우와 완료 조우 모두 같은 2초 전투 화면 유지를 거친 뒤 reducer가 이미 정한 `exploration` 또는 `result`로 간다.
- 적 공격 예고 중 canonical state가 다음 플레이어 차례여도 `BattleCommands`는 presentation busy 상태에서 모든 전투 입력을 숨기거나 비활성화한다. Fullscreen 같은 게임 command가 아닌 브라우저 UI는 유지할 수 있다.
- 새 dispatch sequence가 들어오거나 component·Scene이 unmount되면 이전 timer와 queue를 취소한다. timer가 지연되거나 페이지가 새로고침되면 canonical state가 그대로 복구되어 게임 결과가 유실되지 않는다.

### 24.7 적 공격 예고 의미

- 실제로 공격할 살아 있는 적이 상태 tick·기절·마비·수면 검사를 통과한 뒤 `TURN_STARTED { actorId, actorSide:'enemy' }`를 생성한다.
- presentation은 해당 event에서 적 이름과 “공격 준비”를 약 1000ms 표시한 뒤 그 적의 기존 roll overlay를 재생한다.
- 복수 적은 원본 event 순서대로 각각 `예고 1000ms → 굴림 1000ms`를 재생한다. 첫 적에만 텀을 주고 뒤의 적을 한꺼번에 표시하지 않는다.
- 행동 불능으로 `TURN_SKIPPED`된 적은 공격을 시작하지 않으므로 1초 공격 예고를 만들지 않는다.
- 적이 먼저 행동하는 조우의 초기 event batch도 새 `BattleScene` 생성 전에 유실되지 않도록 store가 마지막 dispatch envelope를 읽을 수 있어야 한다.
- 이 텀은 사용자의 인지와 입력 잠금을 위한 presentation 시간이며 turn order, cooldown 감소, 상태 수명과 RNG 소비 순서는 현재 reducer 결과를 유지한다.

### 24.8 구조화 상태 결과와 로그 표시

- `STATUS_APPLIED`와 신규 `STATUS_RESISTED`는 `actorId`, `targetId`, `skillId`, `statusId`를 공통으로 가진다.
- 성공 메시지는 `{시전자}의 {스킬}: {대상}에게 {상태} 부여 성공.`, 실패 메시지는 `{시전자}의 {스킬}: {대상}에게 {상태} 부여 실패.` 형식을 사용한다.
- 메시지에는 사용자가 제외한 잔여 행동·라운드 수를 넣지 않는다.
- 상태명은 제한된 map으로 `기절/출혈/마비/신경독/수면/약점 노출/도발`만 제공하고 범용 상태 DSL을 만들지 않는다.
- `find_leak`과 `taunt`처럼 적용 저항이 없는 상태는 성공 event만 만들며 가짜 실패 RNG를 소비하지 않는다.
- `GameLogEntry`는 적어도 `message`, 원본 `eventType`, `kind: default|damage|heal`을 보존한다. `DAMAGE_APPLIED`와 `TRAP_TRIGGERED`는 damage, `HEAL_APPLIED`는 heal, 나머지는 default다.
- GameHud는 `kind`를 DOM class 또는 `data-kind`로 전달하고 damage는 붉은색, heal은 녹색으로 표시한다. event message 내용은 색상 판정에 사용하지 않는다.

### 24.9 위험과 회귀 경계

- store listener 계약을 확장할 때 React, BattleScene, ExplorationScene와 store 테스트를 동시에 바꾸지 않으면 event가 중복 또는 유실될 수 있다. monotonic sequence로 한 dispatch를 한 번만 재생해야 한다.
- terminal snapshot이 pre-attack state를 가리키면 마지막 적 HP와 로그가 잘못 보인다. 반드시 `update.combat`과 `appendEvents` 적용 후, canonical screen 전환 전에 capture해야 한다.
- 화면만 battle로 유지한 동안 canonical result의 `RETURN_TO_HUB` 등 다음 화면 command가 노출되면 안 된다. 표시 state와 입력 가능 state를 함께 presentation busy로 잠근다.
- 브라우저 timer는 정확한 frame clock이 아니므로 1000ms·2000ms는 최소 체감 기준이며 background tab에서는 늦게 끝날 수 있다. 이를 보정하려고 게임 RNG나 turn을 wall-clock에 연결하지 않는다.
- 적 event를 한꺼번에 받은 최종 HP snapshot은 유지된다. 이번 범위는 공격 전 예고·굴림 순서·로그 인지를 보장하며 event별 중간 HP를 역산하는 별도 replay state machine은 만들지 않는다.
- 기존 200건 상한, 로그 시간순·bottom 조건부 auto-follow, profile v2, 전투 공식, 상태 확률, boss 절반 규칙과 정산 결과는 변경하지 않는다.

## 25. 탐사 통로 원근 경사·viewport 외부 노출 분석

### 25.1 분석 범위와 실제 렌더 경계

- 대상은 `src/phaser/ExplorationScene.ts`의 640×360 논리 캔버스 안에서 terrain floor·ceiling·side/front wall·marker·외곽 frame·좌표·던전명·이동 버튼을 합성하는 경계다.
- terrain PNG와 `terrainAssets.ts` registry는 7개 map의 32×32 반복 타일을 정상 공급한다. 이번 현상은 이미지 내용이나 registry가 아니라 Scene의 geometry mask와 clipping 좌표에서 발생한다.
- 실제 탐사 시야 외곽은 `FRAMES[0] = { left:70, right:570, top:62, bottom:258 }`이고 크기는 500×196이다.
- 논리 캔버스의 상단 `(y<62)`에는 위치·던전명, 하단에는 y=261~315 범위의 이동 버튼이 있다. 좌우 `x<70`, `x>570`도 탐사 시야 틀 밖이다.

현재 깊이 frame:

| depth | 좌·우 | 위·아래 | 의미 |
|---:|---|---|---|
| 0 | 70~570 | 62~258 | 현재 칸 외곽 |
| 1 | 166~474 | 92~232 | 한 칸 앞 |
| 2 | 238~402 | 120~207 | 두 칸 앞 |
| virtual 3 | 284~356 | 148~196 | 마지막 원근 수렴 frame |

- 소실점은 `(320,172)`다.
- side wall이 존재할 때는 `wallQuadPoints(current, far)`가 각 frame의 실제 꼭지점을 연결하므로 좌·우 막힌 벽의 위·아래 모서리는 정상적으로 이어진다.
- side가 열려 있으면 별도 통로 polygon을 그리지 않고 side wall을 생략한다. 그 자리에 뒤쪽 전역 floor·ceiling mask가 노출되어 열린 통로의 위·아래 경계로 보인다.

### 25.2 두 칸 앞 정면 벽 조건과 불일치 원인

Scene의 depth별 판정은 다음과 같다.

```text
cx = current.x + forward.x × depth
cy = current.y + forward.y × depth

leftWall  = isWall(cx - right.x, cy - right.y)
rightWall = isWall(cx + right.x, cy + right.y)
frontWall = isWall(cx + forward.x, cy + forward.y)
```

- 사용자가 지적한 “두 칸 앞이 막히고 좌/우 통로가 열림”은 `depth === 1`, `frontWall === true`, `leftWall/rightWall === false`인 상태다.
- depth 1의 정면 벽은 `farEdgeFor(1) = FRAMES[2]`이므로 `(238,120)~(402,207)`에 정확히 놓인다.
- 현재 ceiling mask는 `(0,44) → (320,172) → (640,44)`, floor mask는 `(0,266) → (320,172) → (640,266)`인 단일 삼각형이다.
- 단일 삼각형의 사선은 frame별 꼭지점을 통과하지 않는다. 왼쪽 기준 ceiling은 x=238에서 y=139.2지만 목표 정면 벽 꼭지점은 y=120이고, floor는 y=196.09지만 목표는 y=207이다.
- 따라서 열린 side에서 보이는 위 경계는 정면 벽의 상단 꼭지점보다 약 19.2px 아래, 아래 경계는 하단 꼭지점보다 약 10.9px 위에 닿아 통로가 중앙의 좁은 띠처럼 왜곡된다.
- 이 차이는 특정 depth의 조건문이나 벽 texture 음영 문제가 아니다. `FRAMES[0..2]`와 `VANISH_FRAME`의 모서리는 하나의 직선 위에 있지 않으므로 단일 삼각형 slope 하나로 모든 깊이를 맞출 수 없다.

대표 재현 상태:

```text
training_ruins 위치 (3,4), south
depth 1 셀       (3,5)
두 칸 앞         (3,6) = 벽
depth 1 좌측     (4,5) = 통로
depth 1 우측     (2,5) = 통로
```

### 25.3 목표 frame-chain polygon

floor·ceiling 경계를 각 원근 frame의 실제 모서리를 차례대로 통과하는 polygon으로 교체한다.

천장 polygon:

```text
(70,62)
→ (570,62)
→ (474,92)
→ (402,120)
→ (356,148)
→ (320,172)
→ (284,148)
→ (238,120)
→ (166,92)
→ close
```

바닥 polygon:

```text
(70,258)
→ (166,232)
→ (238,207)
→ (284,196)
→ (320,172)
→ (356,196)
→ (402,207)
→ (474,232)
→ (570,258)
→ close
```

- 이 polygon은 depth 0→1, 1→2, 2→virtual frame의 side wall 상·하단과 동일한 선분을 사용한다.
- 두 칸 앞 정면 벽의 네 꼭지점 `(238,120)`, `(402,120)`, `(238,207)`, `(402,207)`을 정확히 공유하므로 요청한 더 가파른 경사가 조건별 보정 band 없이 만들어진다.
- loaded TileSprite의 GeometryMask와 texture fallback Graphics가 반드시 같은 point 배열을 사용해야 에셋 로딩 성공 여부에 따른 모양 차이가 없다.
- TileSprite의 현재 640px bounds와 tile position은 유지하고 mask만 바꾼다. sprite bounds를 함께 줄이면 반복 무늬 원점이 달라져 기존 map별 tile 위상이 바뀔 수 있다.
- 기존 `FRAMES`, `VANISH_FRAME`, front wall 크기, side wall 판정과 `DEEP_SHADE_ALPHA`는 변경하지 않는다.

### 25.4 viewport 밖 terrain 노출 원인

현재 terrain 배경 범위는 탐사 frame보다 크다.

| 요소 | 현재 영역 |
|---|---|
| camera | 0~640 × 0~360 |
| background wash | x=0~640, y=44~266 |
| ceiling TileSprite | x=0~640, y=44~172 |
| floor TileSprite | x=0~640, y=172~266 |
| 탐사 외곽 frame | x=70~570, y=62~258 |

- floor는 frame bottom 258보다 8px 아래까지 존재하고 이동 버튼은 y=261부터 시작하므로 버튼 사이와 주변에 terrain이 보일 수 있다.
- ceiling도 frame top 62보다 18px 위에서 시작해 좌표·던전명 영역 아래로 texture가 노출된다.
- canvas 전체를 Phaser `FIT`으로 표시하는 CSS는 내부 `(70,62)~(570,258)` 영역을 crop하지 않는다.
- side/front wall mask는 frame 안이지만 공통 background wash와 floor·ceiling 삼각형은 외곽 frame을 기준으로 하지 않는다. 앞으로 추가되는 렌더 오브젝트도 공통 clip이 없으면 같은 문제가 반복될 수 있다.

### 25.5 목표 viewport clipping 구조

두 단계로 외곽 노출을 차단한다.

1. background wash와 floor·ceiling polygon 자체를 `FRAMES[0]` 범위 안으로 제한한다.
2. terrain·wall 렌더 위, marker·UI 아래에 불투명한 네 방향 cover를 둬 시야 틀 밖 픽셀을 최종 차단한다.

```text
top cover    x=0   y=0   width=640 height=62
bottom cover x=0   y=258 width=640 height=102
left cover   x=0   y=62  width=70  height=196
right cover  x=570 y=62  width=70  height=196
```

권장 depth:

```text
background wash      -3
ceiling TileSprite    -2
floor TileSprite      -1
world fallback         0
wall TileSprite      3~10
viewport cover        15
outer frame           16
encounter/boss marker 20
좌표·던전명·버튼       50
```

- cover 색은 camera background와 같은 `#0b0b13`을 사용해 상단·하단 HUD 배경을 균일하게 만든다.
- cover는 interactive가 아니므로 이동 버튼 pointer event를 막지 않는다.
- 현재 marker 최대 영역은 frame 내부 y≈254 이하이므로 depth 20에서 cover 위에 두어도 잘리지 않는다.
- 기존 outer frame stroke는 depth 0의 `world`에 포함되어 cover와 겹칠 수 있다. 외곽 frame만 별도 Graphics depth 16에서 다시 그리고, 내부 depth frame stroke는 기존 world에 유지한다.
- cover를 CSS overlay로 만들지 않는다. 640×360 논리 좌표에서 Phaser 오브젝트와 동일하게 scale되어야 Fullscreen·화면비 변화에서도 경계가 유지된다.

### 25.6 책임·호환성 경계

- 원근 polygon·viewport·depth 수치는 순수 geometry 상수로 분리해 Scene mask와 테스트가 같은 값을 사용하도록 한다.
- `isWall`, 비밀문 발견, encounter 순서, 탐사 좌표·방향과 이동 command는 변경하지 않는다.
- terrain PNG, 생성 스크립트, map별 registry와 asset catalog는 변경할 필요가 없다.
- floor·ceiling fallback도 동일 polygon을 사용하고 texture 미로딩 warning·fallback 흐름을 보존한다.
- Phaser canvas 640×360, FIT/CENTER_BOTH, pixelArt, marker·버튼 위치와 Fullscreen toggle은 유지한다.

### 25.7 위험과 검증 공백

- cover가 outer frame보다 높은 depth에 있고 frame을 다시 그리지 않으면 3px 테두리의 바깥 절반이 사라질 수 있다.
- cover를 marker보다 높게 두면 depth 0 근거리 encounter/boss marker 하단이 잘릴 수 있고, UI보다 높게 두면 버튼·좌표를 가릴 수 있다.
- mask polygon과 fallback polygon을 별도로 하드코딩하면 한 경로만 수정되는 회귀가 생긴다. 단일 geometry export를 사용해야 한다.
- 현재 자동 테스트는 이동·벽 판정만 다루며 Phaser mask·depth를 검증하지 않는다. pure geometry 테스트와 실제 Scene screenshot 검증을 함께 둬야 한다.
- frame-chain은 기존 단일 직선보다 깊이별 선분 각도가 달라진다. 이는 요청한 꼭지점 정합을 위한 의도된 변화이며 7개 terrain 테마 모두에서 tile seam·aliasing을 확인해야 한다.

## 26. 초기 화면 License 버튼·모달 분석

### 26.1 분석 범위와 근거

- 최종 게임 화면 기준은 프로젝트 루트의 `GAME_LICENSE_SCREEN_TEXT.md`다. 기존 `GAME_LICENSE_SCREEN_CONTENT.md`는 사용자가 폐기·삭제했으며 현재 파일도 존재하지 않으므로 설계·구현·테스트에서 더 이상 참조하지 않는다.
- 정식 프로젝트 권리 문서는 `LICENSE`, 배포 제3자 고지 원문은 `THIRD_PARTY_NOTICES.md`, 내부 검수 결과는 `LICENSE_AUDIT_SUMMARY.md`다. 네 문서는 현재 Git 미추적 상태이며 이번 Planner 작업에서는 내용을 읽기만 하고 수정하지 않는다.
- 실제 초기 화면은 `src/ui/SetupScreen.tsx`에서 `state.screen === 'start'`일 때 렌더된다. 시작 동작은 profile 유무에 따라 `새 게임` 또는 `이어하기`이며, 같은 `.menu-actions` 안에 조건부 `저장 초기화`와 `FullscreenToggle`이 있다.
- 현재 `.menu-actions`는 폭 `min(320px, 80vw)`의 1열 grid라 버튼을 단순 추가하면 시작 버튼의 오른쪽이 아니라 다음 행에 배치된다.
- 프로젝트에는 전역 modal 계층이나 공용 dialog 컴포넌트가 없다. `HubScreen`의 창고 경고는 현재 panel 안에서 `role="dialog"`만 사용하는 인라인 경고이므로, 긴 License 전문을 viewport 위에 띄우는 modal과 구조·scroll 요구가 다르다.
- `body`는 `overflow:hidden`이고 `.menu-screen`은 `user-select:none`이다. 따라서 modal 본문 자체에 독립적인 세로 scroll, touch pan, 텍스트 선택 허용을 지정해야 한다.
- `src/vite-env.d.ts`가 `vite/client`를 참조하므로 화면 기준본과 notice를 `?raw`, notice 배포 파일을 `?url&no-inline`으로 읽을 수 있다. 현재 package에는 Markdown renderer가 없고 새 dependency를 추가할 필요도 없다.

### 26.2 최종 문서 역할과 화면 범위

| 문서 | 역할 | runtime 화면 처리 |
|---|---|---|
| `GAME_LICENSE_SCREEN_TEXT.md` | 게임 내 표시 문구의 유일한 기준본 | 기본 화면·Third-Party Software 두 `text` block과 표시 구조를 그대로 사용 |
| `THIRD_PARTY_NOTICES.md` | runtime·vendored 구성요소의 저작권 및 MIT/ISC 원문 | sections 2~7을 `Third-Party License Texts` scroll 영역에 표시하고 별도 배포 파일도 유지 |
| `LICENSE` | Party Night 자체 코드·문서·에셋의 정식 proprietary 권리 문서 | modal 문구를 덮어쓰지 않고 저장소 정식 권리 기준으로만 유지 |
| `LICENSE_AUDIT_SUMMARY.md` | v0.2.0 package·vendored source·asset 감사 내부 요약 | 게임 화면에 표시하거나 runtime bundle에 import하지 않음 |

`GAME_LICENSE_SCREEN_TEXT.md`에는 placeholder가 없고 다음 문구가 최종 승인돼 있다.

```text
Party Night

Copyright © 2026 orcspy.
All Rights Reserved.

Party Night의 자체 코드, 문서 및 자체 제작 에셋에 대한 권리는
orcspy에게 있습니다.

제3자 소프트웨어는 각 구성요소의 원 라이선스가 적용됩니다.
```

- 이전 설계처럼 `LICENSE`에서 `송현도 (orcspy)`를 추출해 화면 저작권 문구를 치환하지 않는다. 정식 문서와 게임 표시용 이름의 표현 차이는 새 화면 기준본이 의도적으로 확정한 값으로 본다.
- 화면 기본 목록은 개발 도구를 제외하고 React, React DOM, Scheduler, Phaser, EventEmitter3와 Phaser distributed/vendored Matter.js, poly-decomp.js, Earcut 8종을 표시한다.
- `Third-Party License Texts`는 `THIRD_PARTY_NOTICES.md`의 runtime notice sections와 동일한 저작권·허가 원문을 제공한다. 개발 전용 section 8과 project asset·maintenance sections 9~10은 게임 modal에 넣지 않는다.

### 26.3 최종 검수 문서 정합성

- `LICENSE_AUDIT_SUMMARY.md`는 runtime npm 5종이 MIT이고 Phaser vendored source로 Matter.js·poly-decomp.js(MIT), Earcut(ISC)를 고지 대상으로 확정한다.
- `THIRD_PARTY_NOTICES.md`는 위 8종의 이름·version·license, 원 copyright와 MIT/ISC 허가·면책 원문을 sections 1~7에 보존한다.
- `GAME_LICENSE_SCREEN_TEXT.md`의 Third-Party Software 목록은 audit와 notice의 8종 이름·version·license에 일치한다.
- 사용자가 asset 문서와 실제 asset의 파일 수·크기·registry를 대조했으며 외부 media asset 고지 대상을 발견하지 않았다. notice section 9와 audit summary도 같은 결론을 기록한다.
- lockfile 전체 license 식별자는 사용자 검수상 MIT, Apache-2.0, ISC, BSD-3-Clause, CC-BY-4.0 범위이며 audit는 현재 runtime에서 강한 copyleft 충돌이 없다고 판정한다.
- `THIRD_PARTY_NOTICES.md`는 더 이상 metadata template이 아니라 현재 v0.2.0 배포 고지 원문이다. 구현 단계에서 내용을 재생성·번역·요약하거나 설치 package LICENSE로 다시 덮어쓰지 않는다.
- `LICENSE_AUDIT_SUMMARY.md`는 `party_night-v0.2.0.zip` 기준 내부 검수 기록이라고 명시한다. 실제 공개 핵심 파일은 `LICENSE`와 `THIRD_PARTY_NOTICES.md`이며 audit summary 자체를 License modal에 노출하지 않는다.

### 26.4 목표 화면 구조

초기 화면 동작 영역을 다음처럼 바꾼다.

```text
.menu-actions
├─ .start-primary-row
│  ├─ 새 게임 또는 이어하기  (남은 폭 사용)
│  └─ License                (오른쪽 고정 폭)
├─ 저장 초기화               (profile이 있을 때만)
└─ FullscreenToggle          (지원 환경에서만)
```

- `License` 버튼은 profile 유무와 관계없이 항상 시작 버튼 오른쪽에 있다.
- 시작 버튼의 label·dispatch와 저장 초기화·Fullscreen의 기존 순서 및 기능은 바꾸지 않는다.
- License 열기/닫기는 UI local state이며 `GameCommand`, game store, profile, RNG와 persistence에 연결하지 않는다.
- modal이 열리면 fixed backdrop이 현재 시작 화면을 덮고 License panel만 입력 가능하게 한다. Fullscreen 상태에서 열어도 같은 document 안에서 표시된다.

### 26.5 콘텐츠 데이터 흐름

최종 흐름은 다음과 같다.

```text
GAME_LICENSE_SCREEN_TEXT.md?raw
  → 기본 화면 text block
  → Third-Party Software text block

THIRD_PARTY_NOTICES.md?raw
  → sections 2~7 copyright/license text 추출

THIRD_PARTY_NOTICES.md?url&no-inline
  → production build의 별도 notice asset URL
  → 전체 notice 별도 열기

두 표시 원문 정합성 검증
  → React text node·preformatted license text로 렌더
  → LicenseModal scroll body
```

- `dangerouslySetInnerHTML`을 사용하지 않는다. 최종 화면 text와 notice 원문을 React text node로 표시한다.
- copyright holder 치환, Markdown template 해석, dependency metadata 재조합을 하지 않는다. 화면 기준본의 두 `text` block을 줄바꿈까지 보존한다.
- notice는 heading `## 2. React / React DOM / Scheduler`부터 `## 7. Earcut` 끝까지만 runtime text로 추출한다. section 8 이후를 modal에 섞지 않는다.
- 두 문서의 8종 이름·version·license가 다르거나 필수 section이 사라지면 일부 내용만 표시하지 않고 “라이선스 정보를 불러오지 못했습니다.” fallback을 사용한다.
- `GAME_LICENSE_SCREEN_TEXT.md`, `THIRD_PARTY_NOTICES.md`가 runtime build 입력이며 네 신규 root 문서는 모두 이번 커밋에 포함한다.
- `?url&no-inline`을 사용해 notice가 data URI나 JS 문자열에만 묻히지 않고 `dist/assets`의 독립 파일로 생성되게 한다. modal은 `base:'/pn/'`이 반영된 Vite 생성 URL만 사용한다.

### 26.6 modal 접근성·입력·scroll

- overlay 안 panel에 `role="dialog"`, `aria-modal="true"`, `aria-labelledby="license-modal-title"`을 둔다.
- 열릴 때 License trigger를 기억하고 panel의 첫 닫기 버튼으로 focus를 이동한다. 닫을 때 trigger로 focus를 돌려준다.
- `Escape`, 상·하단 닫기 버튼, backdrop 자체 클릭으로 닫는다. panel 내부 클릭은 닫히지 않는다.
- modal 내부의 focusable 요소를 기준으로 Tab/Shift+Tab 순환을 제한한다. 시작 버튼·저장 초기화·Fullscreen에는 modal이 열린 동안 keyboard focus가 도달하지 않아야 한다.
- panel은 `max-height` 안에서 header/footer를 고정하고 본문만 `overflow-y:auto`, `overscroll-behavior:contain`, `-webkit-overflow-scrolling:touch`, `touch-action:pan-y`로 움직인다.
- License 본문은 `user-select:text`와 `-webkit-user-select:text`로 저작권·라이선스 문구를 선택할 수 있게 한다.
- overlay padding에 네 방향 safe-area를 반영한다. portrait에서는 기존 회전 안내가 modal보다 위에서 입력을 차단하도록 stacking 순서를 명시한다.

### 26.7 호환성·위험·검증 공백

- native `<dialog>`의 top-layer·모바일 Safari 버전별 동작에 의존하지 않고 기존 React/CSS 방식의 fixed ARIA modal을 사용한다.
- 시작 화면은 작은 landscape 높이에서 이미 제목을 축소한다. 320px action 폭에 `License`를 나란히 놓을 때 시작 버튼 label이 줄바꿈되지 않는지 640×360과 844×390에서 확인해야 한다.
- backdrop z-index가 portrait warning보다 높으면 세로 회전 차단을 우회한다. portrait warning을 최상위로 유지하거나 modal overlay를 그보다 낮게 배치해야 한다.
- 두 화면 입력 문서의 heading·code fence·section 구조가 바뀌면 추출이 실패할 수 있다. 전체 문서를 대신 표시하는 fallback은 금지하고 parser test 실패로 변경을 드러낸다.
- package·Phaser·asset 구성이 변경되면 audit summary의 유지 규칙에 따라 세 License 문서를 먼저 재검수한 뒤 화면 기준본을 동기화해야 한다.
- 개발 전용 section 8을 player runtime 목록에 다시 포함하거나 audit summary를 사용자 화면에 노출하지 않는다.
- minifier가 bundle comment를 제거할 수 있으므로 bundle 내부 주석 존재만으로 완료 처리하지 않는다. 별도 notice asset이 production output에 있고 HTTPS에서 열려야 한다.
- 이 modal은 정보 표시 기능이다. 동의 체크, 첫 실행 강제 표시, 개인정보 수집, 외부 URL 이동, profile 저장 필드는 추가하지 않는다.
