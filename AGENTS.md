# AGENTS.md

## 1. 문서 목적과 최우선 원칙

이 문서는 AI 에이전트와 개발자가 프로젝트를 구현할 때 따라야 할 최소 규칙을 정의한다.

이 프로젝트의 최우선 목표는 **짧은 기간 안에 모바일 웹 브라우저에서 실제로 실행하고 플레이할 수 있는 산출물**을 만드는 것이다.

다음 원칙을 항상 우선한다.

1. 전체 시스템의 완성도보다 처음부터 끝까지 동작하는 플레이 루프를 먼저 만든다.
2. 현재 산출물에 필요하지 않은 기능, 추상화, 라이브러리 및 문서를 추가하지 않는다.
3. 임시 데이터와 임시 도트 이미지를 사용할 수 있다. 다만 교체 위치는 명확히 분리한다.
4. 각 작업 후에는 실행 가능한 상태를 유지한다.
5. 확정되지 않은 세부 규칙은 구현을 막지 않는 범위에서 가장 단순한 기본값을 적용하고 `임시 결정`으로 기록한다.
6. 장기 확장을 이유로 현재 구현을 복잡하게 만들지 않는다.

---

## 2. 궁극적 목표

도트 스타일의 캐릭터를 이용한 4인 파티 기반의 던전·필드 탐사 및 다이스 중심 턴제 전투 게임을 제작한다.

게임은 별도의 APK 또는 IPA 없이 모바일 웹 브라우저에서 실행한다.

최종 게임의 기본 흐름은 다음과 같다.

```text
캐릭터 생성
→ 4인 파티 구성
→ 장비와 스킬 준비
→ 퀘스트 선택
→ 던전 또는 필드 탐사
→ 적 조우
→ 다이스 기반 턴제 전투
→ 보상 획득
→ 준비 화면 복귀
```

---

## 3. 현재 작업 기준: v0.2.0 확장

v0.1.0의 처음부터 끝까지 동작하는 MVP 플레이 루프는 기준선으로 유지한다. 현재 목표 버전은 `0.2.0`, 작업 이름은 **7개 퀘스트와 레벨 10 성장**이다.

```text
프로필 생성
→ 4인 파티·장비·아이템·스킬 준비
→ 퀘스트 선택
→ 격자형 탐사·조우
→ 다이스 기반 턴제 전투
→ 성공 또는 실패 정산
→ 성장·해금·보상 반영
→ 거점 복귀
```

### 3.1 v0.1.0 기준선

- 모바일 가로 화면의 시작·준비·탐사·전투·결과 전체 루프를 깨뜨리지 않는다.
- 4인 파티, 1인칭 격자 탐사, AGI 행동 순서, d6·리롤·피해·승패, 골드·EXP와 브라우저 저장을 회귀 기준으로 유지한다.
- 기존 에셋이 없는 신규 콘텐츠는 도형·tint fallback으로 플레이 가능해야 한다.

### 3.2 v0.2.0에 반드시 포함할 기능

- 순차 퀘스트 5개와 반복 퀘스트 2개
- 훈련 폐허 3조우와 나머지 퀘스트의 보스 완료 구조
- 캐릭터별 Lv1~10, 직업별 성장표와 EXP 정산
- 직업 스킬 Lv1·2·5 해금과 커스텀 스킬 슬롯 Lv3·7·10 해금
- 고유 ID를 가진 커스텀 스킬 인스턴스
- 무기·보조 장비·머리·몸통 장비와 직업·양손 제한
- 장비·소비 아이템·스킬을 함께 보관하는 100칸 통합 공용 창고
- 장비·아이템·스킬 상점, 등급 해금과 창고 항목 판매
- 캐릭터 개인 소비 아이템 인벤토리와 전투 중 아이템 사용
- 쿨다운, 출혈·기절·마비·도발·약점 노출·수면·능력 변화 등 승인된 상태 규칙
- 함정·비밀문·비밀방 보상과 `seek_trap` 자동 발견
- profile version 2 저장, 성공·실패·창고 초과 보상 정산
- 각 퀘스트 단계 종료 시 시작부터 거점 복귀까지 플레이 가능한 상태

### 3.3 v0.2.0에서 제외할 기능

- 사용자 계정·로그인·서버 저장·서버 권위 판정
- WebSocket, 복수 기기 동기화, 온라인 세션, PWA 완성
- 길드 평판·등급, 파티원 계약 기간과 고용 경제
- 장비 강화·인챈트·내구도·무작위 옵션·세트 효과
- 스킬 레벨·강화와 범용 효과 DSL
- 퀘스트 다중 분기, 자동 이동, 자동 지도, 횃불·시간대
- 필드와 던전의 별도 렌더링 엔진
- 복잡한 적 AI와 사망 후 영구 손실
- 범용 콘텐츠 에디터·관리자 도구
- 사운드 완성도, 최종 그래픽과 최종 밸런스

제외 기능은 빈 인터페이스나 미사용 추상 계층으로 미리 구현하지 않는다.

---

## 4. 기술 구성

- Vite
- React
- TypeScript
- Phaser
- HTML/CSS
- Vitest

별도 요구가 없으면 새로운 상태 관리 라이브러리, 게임 프레임워크 또는 서버 프레임워크를 추가하지 않는다.

### 4.1 React 역할

React는 다음 UI를 담당한다.

- 시작 화면
- 프로필 생성과 캐릭터·파티 준비
- 거점의 퀘스트·창고·캐릭터·상점 탭
- 장비·아이템 배분과 스킬 선택
- 탐사 및 전투 HUD
- 전투 명령 버튼
- 리롤 대상 선택
- 전투 로그
- 성장·해금·창고 초과 보상 선택을 포함한 결과 화면

### 4.2 Phaser 역할

Phaser는 다음 렌더링과 연출을 담당한다.

- 1인칭 격자 탐사 화면
- 캐릭터 및 적 스프라이트
- 조우·함정·비밀문·출구 표현
- 다이스 굴림 연출
- 공격, 피격 및 화면 전환 연출
- 게임 화면 내부의 터치 입력

### 4.3 순수 TypeScript 게임 엔진 역할

게임 엔진은 React와 Phaser에 의존하지 않는다.

- 캐릭터 및 파티 상태
- 영속 프로필과 원정 세션 상태 전이
- 성장·장비·창고·개인 인벤토리·상점 판정
- 탐사 위치와 이동 판정
- 퀘스트·조우·함정·비밀문 판정
- 행동 순서
- 스킬·아이템 사용 가능 여부와 상태·쿨다운
- 다이스 생성, 굴림 및 리롤
- 보정값 적용
- 피해 계산
- 승패 판정
- 보상·해금·창고 초과 정산
- 시드 기반 난수

---

## 5. 필수 아키텍처 규칙

의존 방향은 다음을 유지한다.

```text
React UI
   │ GameCommand
   ▼
순수 TypeScript 게임 엔진
   │ GameEvent / 변경된 상태
   ├──────────────┐
   ▼              ▼
React 갱신     Phaser 연출
```

### 반드시 지킬 사항

- React 컴포넌트가 전투 결과를 직접 계산하지 않는다.
- Phaser Scene 또는 Sprite가 HP, 다이스 결과 및 승패를 직접 변경하지 않는다.
- 게임 결과를 먼저 계산하고 애니메이션은 그 결과를 표현한다.
- 애니메이션 실패 또는 생략이 게임 상태에 영향을 주지 않는다.
- 게임 판정에 `Math.random()`을 직접 사용하지 않는다.
- 동일한 게임 상태를 React와 Phaser에 각각 별도 원본으로 보관하지 않는다.

### v0.2.0에서 허용하는 단순화

- 하나의 순수 `gameEngine` reducer와 얇은 store adapter를 사용할 수 있다.
- 콘텐츠 데이터는 TypeScript 객체 또는 간단한 JSON으로 작성할 수 있다.
- 범용 효과 DSL이나 플러그인 시스템을 만들지 않는다.
- 현재 콘텐츠에 필요한 제한된 effect union과 명시적인 resolver 함수를 사용한다.
- 데이터베이스 없이 메모리 세션과 `localStorage` profile version 2를 사용한다.

---

## 6. 게임 규칙 기준선

이 절은 v0.1.0에서 검증된 기본 규칙과 v0.2.0 공통 규칙을 정의한다. 상세 타입·수치·콘텐츠 ID는 `implements.md` 23절과 `raw_data_table.md` 22절을 따른다.

### 6.1 캐릭터와 파티

- 파티는 메인 캐릭터 1명과 동료 3명으로 구성한다.
- 종족: 인간, 엘프, 드워프, 하플링
- 직업: 전사, 도적, 궁수, 성기사, 사제, 마법사
- 성별: 남성, 여성
- 성별은 시스템 수치에 영향을 주지 않는다.
- 메인 캐릭터는 사용자가 생성한다.
- 동료 3명은 현재 고정 캐릭터를 사용한다.
- 전열 2명, 후열 2명으로 고정한다.
- 직업 중복을 허용한다.

캐릭터는 다음 원본 능력치를 가진다.

- STR
- DEX
- INT
- CON
- AGI
- LUK

- 최종 기본 능력치는 종족 기본값, 직업 보정, 레벨 성장, 장비 보정을 합산한다.
- HP·ATK·DEF·전투 AGI는 최종 기본 능력치에서 파생한다.
- 캐릭터는 Lv1부터 Lv10까지 성장하고 직업별 레벨 성장표를 사용한다.
- LUK은 데이터와 UI에는 유지하지만 별도 승인 전까지 판정에 사용하지 않는다.

### 6.2 탐사

- 각 퀘스트는 단일 층의 소형 격자 맵을 사용한다.
- 던전과 필드는 동일한 탐사 시스템으로 처리한다.
- 시점은 2D 이미지 합성 방식의 1인칭 격자 시점이다.
- 파티 상태는 `x`, `y`, `direction`으로 관리한다.
- 이동 입력은 전진, 후진, 좌회전, 우회전을 사용한다.
- 조우, 함정, 비밀문과 완료 지점은 콘텐츠 데이터로 배치한다.
- 훈련 폐허는 3번째 필수 조우 승리, 나머지 퀘스트는 보스 승리로 완료한다.
- 살아 있는 파티원에게 `seek_trap`이 있으면 함정·비밀문을 자동 발견한다.

### 6.3 전투

- 행동 순서는 AGI 내림차순으로 결정한다.
- AGI가 같으면 시드 기반 다이스로 순서를 결정한다.
- 캐릭터 단위 턴을 사용한다.
- 각 캐릭터는 최소한 기본 공격 스킬 1개를 가진다.
- 기본 공격은 스킬 슬롯 밖 상시 명령이다.
- 직업 스킬은 Lv1·2·5에 자동 해금한다.
- 커스텀 스킬 슬롯은 Lv3·7·10에 하나씩 열린다.
- 전투 중 스킬 교체는 불가능하다.
- 적 AI는 사용 가능한 공격 중 하나를 선택하고 유효한 대상을 공격하는 수준으로 제한한다.

기본 전투 처리 순서:

```text
행동 선택
→ 대상 선택
→ 기본 다이스 풀 생성
→ 사전 보정 적용
→ 다이스 굴림
→ 선택 리롤
→ 결과 총합 계산
→ 공격/방어 보정
→ 피해 적용
→ 승패 확인
```

기본 다이스 규칙:

- 기본 다이스는 d6를 사용한다.
- 스킬이 다이스 개수와 고정 보정값을 정의한다.
- 결과는 기본적으로 다이스 합계 방식으로 평가한다.
- 리롤 가능 스킬 또는 장비는 지정된 횟수만큼 다이스 하나를 다시 굴린다.
- 리롤한 이전 값은 폐기한다.
- 리롤을 다시 리롤하는 기능은 허용하지 않는다.
- 크리티컬, 대실패, 다이스 잠금 및 다이스 복제는 별도 승인 전까지 제외한다.

기본 피해식은 단순하게 유지한다.

```text
damage = max(1, rollTotal + attackModifier - targetDEF)
```

### 6.4 승패와 보상

- 모든 적의 HP가 0이면 승리한다.
- 모든 파티원의 HP가 0이면 패배한다.
- 성공 시 골드·경험치·성장·해금·실물 보상을 정산한다.
- 실패 시 원정 보상과 진행은 폐기하고 이미 소비한 아이템은 복구하지 않는다.
- 영구 사망과 장비 손실은 적용하지 않는다.
- 전투 종료 후 HP는 유지하되 상태·쿨다운·전투 자원은 초기화한다.
- 거점 복귀 후 다음 원정은 파티 전원의 HP가 회복된 상태로 시작한다.

### 6.5 영속 상태와 공용 창고

- profile version 2와 원정 세션을 분리하고 현재 HP·탐사·전투 상태는 영속 프로필에 저장하지 않는다.
- 공용 창고는 장비 instance, 소비 아이템 stack, 커스텀 스킬 instance를 합해 100칸이다.
- 장비·스킬 instance는 각각 1칸, 동일 소비 아이템은 최대 10개가 1칸을 사용한다.
- 모든 구매와 실물 보상은 공용 창고로 들어가며 자동 장착하지 않는다.
- 장비·아이템·스킬의 캐릭터 배분과 회수는 거점에서만 가능하다.
- 창고 항목만 판매할 수 있고 판매가는 구매가의 50%를 내림한 값이다.
- 창고 초과 퀘스트 보상은 결과 화면에서 사용자가 보관 항목을 선택하고 나머지를 확인 후 포기한다.

---

## 7. 화면과 입력 기준

- 기준 논리 해상도는 `640 × 360`으로 한다.
- 가로 화면을 기준으로 구성한다.
- 도트 이미지 필터는 nearest-neighbor를 사용한다.
- Phaser 오브젝트는 가능한 한 정수 좌표에 배치한다.
- React UI는 다양한 모바일 화면비의 남는 영역에 맞춰 확장한다.
- 주요 터치 버튼은 최소한 손가락으로 구분 가능한 크기를 확보한다.
- 게임 화면에서 브라우저 스크롤, 확대 제스처 및 텍스트 선택이 오작동하지 않게 처리한다.
- iOS Safari와 Android Chrome에서 기본 플레이 루프를 확인한다.

최종 도트 에셋이 없어도 임시 사각형, 단색 이미지 또는 공개 사용 가능한 임시 에셋으로 진행할 수 있다.

---

## 8. 프로젝트 구조 기준

기존 구조를 유지하면서 v0.2.0 책임이 분명할 때만 다음 순수 게임 모듈과 UI를 추가한다.

```text
src/
├─ app/
│  ├─ App.tsx
│  └─ gameStore.ts
├─ game/
│  ├─ types.ts
│  ├─ rng.ts
│  ├─ gameEngine.ts
│  ├─ characters.ts
│  ├─ inventory.ts
│  ├─ shop.ts
│  ├─ rewards.ts
│  ├─ exploration.ts
│  ├─ combat.ts
│  └─ content.ts
├─ phaser/
│  ├─ PhaserGame.ts
│  ├─ ExplorationScene.ts
│  └─ BattleScene.ts
├─ ui/
│  ├─ HubScreen.tsx
│  ├─ QuestPanel.tsx
│  ├─ StoragePanel.tsx
│  ├─ CharacterPanel.tsx
│  ├─ ShopPanel.tsx
│  ├─ GameHud.tsx
│  ├─ BattleCommands.tsx
│  └─ ResultScreen.tsx
├─ assets/
└─ tests/
```

파일이 지나치게 커지거나 책임이 명확히 분리될 때만 하위 모듈로 분리한다.

---

## 9. 코딩 규칙

- TypeScript `strict` 모드를 유지한다.
- `any`는 외부 라이브러리 경계 등 불가피한 경우에만 사용한다.
- 게임 규칙은 가능한 한 입력 상태를 받아 새로운 상태 또는 이벤트를 반환하는 함수로 작성한다.
- React 컴포넌트는 화면 표시와 사용자 입력 전달에 집중한다.
- Phaser Scene은 렌더링과 연출에 집중한다.
- 게임 데이터 ID는 영문 소문자 `snake_case`를 사용한다.
- React 컴포넌트는 `PascalCase.tsx`를 사용한다.
- 일반 TypeScript 파일은 `camelCase.ts`를 사용한다.
- 요청 범위를 벗어난 리팩터링을 하지 않는다.
- 현재 사용하지 않는 범용 인터페이스와 추상 클래스를 만들지 않는다.
- v0.2.0 임시 구현은 `TODO(v0.2.0)` 주석으로 교체 지점을 표시한다. 기존 `TODO(MVP)`는 관련 코드를 수정할 때만 정리한다.

---

## 10. 테스트와 완료 기준

### 10.1 반드시 자동 테스트할 규칙

- 동일 시드에서 동일한 다이스 결과가 생성되는지
- 리롤 시 지정한 다이스만 변경되는지
- AGI 행동 순서가 올바른지
- 피해가 최소 1 이상으로 계산되는지
- 적 전멸과 파티 전멸이 올바르게 판정되는지
- 벽이 있는 칸으로 이동할 수 없는지
- profile v2 저장 round-trip과 불변식 위반 저장 거부
- 창고 100칸·아이템 10중첩·instance 위치 유일성과 원자 이동
- 직업·양손 장비 제한과 개인 STR 기반 인벤토리 용량
- 순차 5회 Lv6, 반복 5회 Lv10과 전투불능 캐릭터 동일 EXP
- Lv1·2·5 직업 스킬과 Lv3·7·10 커스텀 슬롯 해금
- 상태·쿨다운·보스 확률과 동일 시드 재현
- 함정·비밀문·퀘스트 순서·보스 완료 판정
- 상점 등급·판매·스킬 offer와 창고 초과 보상 정산
- 실패 보상 폐기, 소비 아이템 미복구와 거점 복귀 회복

### 10.2 v0.2.0 단계별 완료 기준

각 단계는 다음 조건을 모두 만족해야 한다.

1. `npm install` 후 명시된 단일 명령으로 개발 실행이 가능하다.
2. `npm run build`가 성공한다.
3. TypeScript 타입 검사가 성공한다.
4. 핵심 규칙 테스트가 성공한다.
5. 모바일 크기의 브라우저에서 프로필 생성부터 해당 퀘스트 결과와 거점 복귀까지 플레이할 수 있다.
6. 탐사, 조우, 전투, 리롤, 승패, 성장·해금 및 보상이 실제 상태에 반영된다.
7. 진행을 막는 오류가 없다.
8. 임시 데이터와 미구현 기능이 README 또는 구현 기록에 명시되어 있다.
9. 저장 후 새로고침해도 영속 프로필 불변식이 유지된다.
10. 신규 콘텐츠 ID마다 에셋 또는 도형·tint fallback이 존재한다.

최종 그래픽과 최종 밸런스는 v0.2.0 단계별 완료 조건이 아니다.

---

## 11. 에이전트 작업 절차

### 작업 시작 전

- `AGENTS.md`를 읽는다.
- 요청과 직접 관련된 코드만 확인한다.
- 현재 실행 및 테스트 명령을 확인한다.

### 구현 중

- 가장 짧은 경로로 동작하는 결과를 만든다.
- 한 번에 하나의 플레이 가능한 기능 단위를 완성한다.
- 기능 추가 후 즉시 실행 또는 테스트한다.
- 문제가 없는 기존 기능을 불필요하게 다시 작성하지 않는다.
- 디자인이 불명확해도 구현을 막지 않는 단순한 임시값을 사용할 수 있다.
- 단, 사용자 요구를 변경하는 임시 결정은 작업 결과에 명시한다.

### 구현 후

- 빌드, 타입 검사 및 관련 테스트를 실행한다.
- 실제 브라우저에서 변경 기능을 확인한다.
- 변경 파일, 구현 내용, 임시 결정 및 남은 문제를 짧게 정리한다.

---

## 12. 금지 사항

- v0.2.0에 서버·계정·온라인 동기화 시스템을 추가하지 않는다.
- 범용 콘텐츠 에디터나 관리자 도구를 만들지 않는다.
- 7개 퀘스트 콘텐츠를 한 번에 구현하고 마지막에 통합하려 하지 않는다.
- 게임 규칙을 React 또는 Phaser에 중복 구현하지 않는다.
- 게임 판정에 `Math.random()`을 직접 사용하지 않는다.
- 애니메이션 완료 시점에 게임 결과를 확정하지 않는다.
- 사용하지 않는 대규모 라이브러리를 추가하지 않는다.
- 미래 기능을 위한 과도한 추상화와 폴더 분리를 하지 않는다.
- 범용 효과 DSL, 장비 강화·내구도·무작위 옵션·세트 효과를 추가하지 않는다.
- 테스트나 실행 확인 없이 완료로 처리하지 않는다.
- PC 브라우저에서만 확인하고 모바일 대응 완료로 처리하지 않는다.

---

## 13. 구현 순서

다음 순서를 우선한다.

1. 최신 성별·종족·직업 능력치 코드 기준선 정합화
2. profile v2, 순수 game engine, save adapter와 거점 shell
3. 통합 창고·장착·일반 등급 상점·개인 인벤토리
4. 훈련 폐허 3조우·Lv2·고급 해금·보상 overflow
5. 고블린 소굴·보스 완료·Lv3 슬롯·함정·비밀문
6. 유적지·전체 소비 아이템·희귀 해금
7. 지하 던전·중간보스·상태·쿨다운·영웅 해금
8. 옛 고성·Lv6·전설 해금·반복 퀘스트 해금
9. 화산 동굴·깊은 숲 폐허·반복 성장 Lv10
10. 전체 모바일·저장·overflow·장시간 반복 회귀

각 단계가 끝날 때 실행 가능한 상태를 유지한다.
