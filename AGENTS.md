# AGENTS.md

## 1. 문서 목적과 최우선 원칙

이 문서는 AI 에이전트와 개발자가 프로젝트를 구현할 때 따라야 할 최소 규칙을 정의한다.

이 프로젝트의 최우선 목표는 **짧은 기간 안에 모바일 웹 브라우저에서 실제로 실행하고 플레이할 수 있는 산출물**을 만드는 것이다.

다음 원칙을 항상 우선한다.

1. 전체 시스템의 완성도보다 처음부터 끝까지 동작하는 플레이 루프를 먼저 만든다.
2. 현재 산출물에 필요하지 않은 기능, 추상화, 라이브러리 및 문서를 추가하지 않는다.
3. 임시 데이터와 임시 도트 이미지를 사용할 수 있다. 다만 교체 위치는 명확히 분리한다.
4. 각 작업 후에는 실행 가능한 상태를 유지한다.
5. 확정되지 않은 세부 규칙은 구현을 막지 않는 범위에서 가장 단순한 기본값을 적용하고 `임시 결정`으로 기록한다.
6. 장기 확장을 이유로 현재 구현을 복잡하게 만들지 않는다.

---

## 2. 궁극적 목표

도트 스타일의 캐릭터를 이용한 4인 파티 기반의 던전·필드 탐사 및 다이스 중심 턴제 전투 게임을 제작한다.

게임은 별도의 APK 또는 IPA 없이 모바일 웹 브라우저에서 실행한다.

최종 게임의 기본 흐름은 다음과 같다.

```text
캐릭터 생성
→ 4인 파티 구성
→ 장비와 스킬 준비
→ 퀘스트 선택
→ 던전 또는 필드 탐사
→ 적 조우
→ 다이스 기반 턴제 전투
→ 보상 획득
→ 준비 화면 복귀
```

---

## 3. 첫 번째 동작 산출물(MVP)

첫 번째 산출물은 아래의 한 사이클을 처음부터 끝까지 플레이할 수 있어야 한다.

```text
시작 화면
→ 간소화된 파티 준비
→ 단일 퀘스트 입장
→ 격자형 탐사
→ 고정 또는 무작위 조우
→ 턴제 전투
→ 승리 또는 패배
→ 보상 결과
→ 준비 화면 복귀
```

### 3.1 MVP에 반드시 포함할 기능

- 모바일 가로 화면 대응
- 4인 파티 표시
- 메인 캐릭터 이름, 종족, 직업, 성별 설정
- 나머지 파티원 3명 자동 또는 간소화된 방식으로 구성
- 캐릭터별 최소 스킬 세팅
- 단일 던전 또는 필드 맵 1개
- 전진, 후진, 좌회전, 우회전
- 2D 위저드리식 1인칭 탐사 화면
- 적 조우
- AGI 기반 캐릭터 단위 행동 순서
- 기본 공격 또는 직업 기본 스킬
- 다이스 굴림
- 능력치 또는 장비 보정값
- 최소 1종의 선택 리롤
- 피해 계산과 HP 반영
- 승리 및 전멸 판정
- 골드 또는 경험치 보상
- 전투 및 다이스 결과 로그
- 다시 시작 또는 준비 화면 복귀

### 3.2 MVP에서 제외할 기능

다음은 첫 번째 동작 산출물에 포함하지 않는다.

- 사용자 계정과 로그인
- 서버 저장 및 서버 권위 판정
- WebSocket 세션 관리
- 복수 기기 동기화
- 오프라인 플레이 및 PWA 완성
- 상점 전체 기능
- 길드 평판 및 등급
- 파티원 계약 기간과 고용 경제
- 장비 강화 및 인챈트
- 스킬 레벨과 강화
- 퀘스트 다중 분기
- 자동 이동
- 자동 지도 작성
- 횃불 및 시간대
- 함정과 비밀문
- 필드와 던전의 별도 렌더링 체계
- 고급 상태 효과
- 복잡한 적 AI
- 사망 후 영구 손실 처리
- 사운드 완성도 작업
- 최종 그래픽과 최종 밸런스

제외 기능은 현재 코드에 빈 인터페이스나 미사용 추상 계층으로 미리 구현하지 않는다.

---

## 4. 기술 구성

- Vite
- React
- TypeScript
- Phaser
- HTML/CSS
- Vitest

별도 요구가 없으면 새로운 상태 관리 라이브러리, 게임 프레임워크 또는 서버 프레임워크를 추가하지 않는다.

### 4.1 React 역할

React는 다음 UI를 담당한다.

- 시작 화면
- 캐릭터 및 파티 준비
- 스킬 선택
- 탐사 및 전투 HUD
- 전투 명령 버튼
- 리롤 대상 선택
- 전투 로그
- 결과 화면

### 4.2 Phaser 역할

Phaser는 다음 렌더링과 연출을 담당한다.

- 1인칭 격자 탐사 화면
- 캐릭터 및 적 스프라이트
- 다이스 굴림 연출
- 공격, 피격 및 화면 전환 연출
- 게임 화면 내부의 터치 입력

### 4.3 순수 TypeScript 게임 엔진 역할

게임 엔진은 React와 Phaser에 의존하지 않는다.

- 캐릭터 및 파티 상태
- 탐사 위치와 이동 판정
- 조우 판정
- 행동 순서
- 스킬 사용 가능 여부
- 다이스 생성, 굴림 및 리롤
- 보정값 적용
- 피해 계산
- 승패 판정
- 보상 계산
- 시드 기반 난수

---

## 5. 필수 아키텍처 규칙

의존 방향은 다음을 유지한다.

```text
React UI
   │ GameCommand
   ▼
순수 TypeScript 게임 엔진
   │ GameEvent / 변경된 상태
   ├──────────────┐
   ▼              ▼
React 갱신     Phaser 연출
```

### 반드시 지킬 사항

- React 컴포넌트가 전투 결과를 직접 계산하지 않는다.
- Phaser Scene 또는 Sprite가 HP, 다이스 결과 및 승패를 직접 변경하지 않는다.
- 게임 결과를 먼저 계산하고 애니메이션은 그 결과를 표현한다.
- 애니메이션 실패 또는 생략이 게임 상태에 영향을 주지 않는다.
- 게임 판정에 `Math.random()`을 직접 사용하지 않는다.
- 동일한 게임 상태를 React와 Phaser에 각각 별도 원본으로 보관하지 않는다.

### MVP에서 허용하는 단순화

- 하나의 게임 세션 store 또는 reducer를 사용할 수 있다.
- 콘텐츠 데이터는 TypeScript 객체 또는 간단한 JSON으로 작성할 수 있다.
- 범용 효과 DSL이나 플러그인 시스템을 만들지 않아도 된다.
- 스킬별 특수 처리는 적은 수라면 명시적인 resolver 함수로 구현할 수 있다.
- 데이터베이스 없이 메모리 상태와 간단한 브라우저 저장을 사용할 수 있다.

---

## 6. MVP 게임 규칙

이 절의 수치는 첫 동작 산출물을 위한 임시 기본값이다. 별도 설계가 확정되면 교체할 수 있다.

### 6.1 캐릭터와 파티

- 파티는 메인 캐릭터 1명과 동료 3명으로 구성한다.
- 종족: 인간, 엘프, 드워프, 하플링
- 직업: 전사, 도적, 궁수, 성기사, 사제, 마법사
- 성별은 시스템 수치에 영향을 주지 않는다.
- 메인 캐릭터는 사용자가 생성한다.
- 동료 3명은 MVP에서 미리 정의된 캐릭터 또는 단순 무작위 생성으로 제공한다.
- 전열 2명, 후열 2명으로 고정한다.
- 직업 중복을 허용한다.

MVP 능력치는 다음만 사용한다.

- HP
- ATK
- DEF
- AGI

마법 또는 스킬 자원이 반드시 필요한 경우에만 MP를 추가한다.

### 6.2 탐사

- 단일 층의 소형 격자 맵을 사용한다.
- 던전과 필드는 MVP에서 동일한 탐사 시스템으로 처리한다.
- 시점은 2D 이미지 합성 방식의 1인칭 격자 시점이다.
- 파티 상태는 `x`, `y`, `direction`으로 관리한다.
- 이동 입력은 전진, 후진, 좌회전, 우회전만 우선 구현한다.
- 조우는 고정 조우 지점 또는 단순 확률 판정 중 하나로 구현한다.
- 맵의 시작점, 조우 지점 및 종료 지점을 명확히 둔다.

### 6.3 전투

- 행동 순서는 AGI 내림차순으로 결정한다.
- AGI가 같으면 시드 기반 다이스로 순서를 결정한다.
- 캐릭터 단위 턴을 사용한다.
- 각 캐릭터는 최소한 기본 공격 스킬 1개를 가진다.
- 추가 클래스 스킬은 캐릭터당 1개만 우선 구현한다.
- 전투 중 스킬 교체는 불가능하다.
- 적 AI는 사용 가능한 공격 중 하나를 선택하고 유효한 대상을 공격하는 수준으로 제한한다.

기본 전투 처리 순서:

```text
행동 선택
→ 대상 선택
→ 기본 다이스 풀 생성
→ 사전 보정 적용
→ 다이스 굴림
→ 선택 리롤
→ 결과 총합 계산
→ 공격/방어 보정
→ 피해 적용
→ 승패 확인
```

MVP 다이스 규칙:

- 기본 다이스는 d6를 사용한다.
- 스킬이 다이스 개수와 고정 보정값을 정의한다.
- 결과는 기본적으로 다이스 합계 방식으로 평가한다.
- 리롤 가능 스킬 또는 장비는 지정된 횟수만큼 다이스 하나를 다시 굴린다.
- 리롤한 이전 값은 폐기한다.
- 리롤을 다시 리롤하는 기능은 MVP에서 허용하지 않는다.
- 크리티컬, 대실패, 다이스 잠금 및 다이스 복제는 MVP에서 제외한다.

기본 피해식은 단순하게 유지한다.

```text
damage = max(1, rollTotal + attackModifier - targetDEF)
```

### 6.4 승패와 보상

- 모든 적의 HP가 0이면 승리한다.
- 모든 파티원의 HP가 0이면 패배한다.
- 승리 시 골드와 경험치를 지급한다.
- 패배 시 MVP에서는 영구 사망과 장비 손실을 적용하지 않는다.
- 준비 화면 복귀 시 파티 HP를 전부 회복한다.

---

## 7. 화면과 입력 기준

- 기준 논리 해상도는 `640 × 360`으로 한다.
- 가로 화면을 기준으로 구성한다.
- 도트 이미지 필터는 nearest-neighbor를 사용한다.
- Phaser 오브젝트는 가능한 한 정수 좌표에 배치한다.
- React UI는 다양한 모바일 화면비의 남는 영역에 맞춰 확장한다.
- 주요 터치 버튼은 최소한 손가락으로 구분 가능한 크기를 확보한다.
- 게임 화면에서 브라우저 스크롤, 확대 제스처 및 텍스트 선택이 오작동하지 않게 처리한다.
- iOS Safari와 Android Chrome에서 기본 플레이 루프를 확인한다.

최종 도트 에셋이 없어도 임시 사각형, 단색 이미지 또는 공개 사용 가능한 임시 에셋으로 진행할 수 있다.

---

## 8. 최소 프로젝트 구조

초기에는 다음 구조보다 세분화하지 않는다.

```text
src/
├─ app/
│  ├─ App.tsx
│  └─ gameStore.ts
├─ game/
│  ├─ types.ts
│  ├─ rng.ts
│  ├─ exploration.ts
│  ├─ combat.ts
│  └─ content.ts
├─ phaser/
│  ├─ PhaserGame.ts
│  ├─ ExplorationScene.ts
│  └─ BattleScene.ts
├─ ui/
│  ├─ SetupScreen.tsx
│  ├─ GameHud.tsx
│  ├─ BattleCommands.tsx
│  └─ ResultScreen.tsx
├─ assets/
└─ tests/
```

파일이 지나치게 커지거나 책임이 명확히 분리될 때만 하위 모듈로 분리한다.

---

## 9. 코딩 규칙

- TypeScript `strict` 모드를 유지한다.
- `any`는 외부 라이브러리 경계 등 불가피한 경우에만 사용한다.
- 게임 규칙은 가능한 한 입력 상태를 받아 새로운 상태 또는 이벤트를 반환하는 함수로 작성한다.
- React 컴포넌트는 화면 표시와 사용자 입력 전달에 집중한다.
- Phaser Scene은 렌더링과 연출에 집중한다.
- 게임 데이터 ID는 영문 소문자 `snake_case`를 사용한다.
- React 컴포넌트는 `PascalCase.tsx`를 사용한다.
- 일반 TypeScript 파일은 `camelCase.ts`를 사용한다.
- 요청 범위를 벗어난 리팩터링을 하지 않는다.
- 현재 사용하지 않는 범용 인터페이스와 추상 클래스를 만들지 않는다.
- 임시 구현은 `TODO(MVP)` 주석으로 교체 지점을 표시한다.

---

## 10. 테스트와 완료 기준

### 10.1 반드시 자동 테스트할 규칙

- 동일 시드에서 동일한 다이스 결과가 생성되는지
- 리롤 시 지정한 다이스만 변경되는지
- AGI 행동 순서가 올바른지
- 피해가 최소 1 이상으로 계산되는지
- 적 전멸과 파티 전멸이 올바르게 판정되는지
- 벽이 있는 칸으로 이동할 수 없는지

### 10.2 첫 산출물 완료 기준

다음 조건을 모두 만족하면 첫 동작 산출물로 본다.

1. `npm install` 후 명시된 단일 명령으로 개발 실행이 가능하다.
2. `npm run build`가 성공한다.
3. TypeScript 타입 검사가 성공한다.
4. 핵심 규칙 테스트가 성공한다.
5. 모바일 크기의 브라우저에서 처음부터 결과 화면까지 플레이할 수 있다.
6. 탐사, 조우, 전투, 리롤, 승패 및 보상이 실제 상태에 반영된다.
7. 진행을 막는 오류가 없다.
8. 임시 데이터와 미구현 기능이 README 또는 구현 기록에 명시되어 있다.

그래픽 완성도, 콘텐츠 양 및 최종 밸런스는 첫 산출물 완료 조건이 아니다.

---

## 11. 에이전트 작업 절차

### 작업 시작 전

- `AGENTS.md`를 읽는다.
- 요청과 직접 관련된 코드만 확인한다.
- 현재 실행 및 테스트 명령을 확인한다.

### 구현 중

- 가장 짧은 경로로 동작하는 결과를 만든다.
- 한 번에 하나의 플레이 가능한 기능 단위를 완성한다.
- 기능 추가 후 즉시 실행 또는 테스트한다.
- 문제가 없는 기존 기능을 불필요하게 다시 작성하지 않는다.
- 디자인이 불명확해도 구현을 막지 않는 단순한 임시값을 사용할 수 있다.
- 단, 사용자 요구를 변경하는 임시 결정은 작업 결과에 명시한다.

### 구현 후

- 빌드, 타입 검사 및 관련 테스트를 실행한다.
- 실제 브라우저에서 변경 기능을 확인한다.
- 변경 파일, 구현 내용, 임시 결정 및 남은 문제를 짧게 정리한다.

---

## 12. 금지 사항

- 첫 산출물 전에 전체 서버 시스템을 먼저 구현하지 않는다.
- 첫 산출물 전에 범용 콘텐츠 에디터나 관리자 도구를 만들지 않는다.
- 첫 산출물 전에 모든 종족, 직업, 스킬 및 장비 콘텐츠를 완성하려 하지 않는다.
- 게임 규칙을 React 또는 Phaser에 중복 구현하지 않는다.
- 게임 판정에 `Math.random()`을 직접 사용하지 않는다.
- 애니메이션 완료 시점에 게임 결과를 확정하지 않는다.
- 사용하지 않는 대규모 라이브러리를 추가하지 않는다.
- 미래 기능을 위한 과도한 추상화와 폴더 분리를 하지 않는다.
- 테스트나 실행 확인 없이 완료로 처리하지 않는다.
- PC 브라우저에서만 확인하고 모바일 대응 완료로 처리하지 않는다.

---

## 13. 구현 순서

다음 순서를 우선한다.

1. Vite + React + TypeScript + Phaser 기본 실행
2. 순수 TypeScript 다이스와 전투 규칙
3. React 기반 전투 명령 및 로그
4. Phaser 기반 전투 표시와 최소 연출
5. 격자 탐사 상태와 이동
6. Phaser 기반 1인칭 탐사 화면
7. 탐사에서 전투로 전환하고 다시 복귀
8. 간소화된 캐릭터 및 파티 준비
9. 보상과 기본 저장
10. 실제 모바일 브라우저 검증과 수정

각 단계가 끝날 때 실행 가능한 상태를 유지한다.
